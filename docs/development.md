# MiniLang 开发指南

本文档是 MiniLang IDE 开发文档的导航入口。详细的架构设计、决策记录与测试方法请参见各子文档。

## 文档导航

| 文档 | 说明 |
|------|------|
| [快速开始](getting-started.md) | 构建、运行、开发环境配置（Windows/Linux/macOS/Docker） |
| [核心架构](architecture.md) | 编译管线、NaN-boxing、三后端、内存模型、安全约束 |
| [测试指南](testing.md) | 三后端一致性验证方法论、测试分类与编写指南 |
| [设计决策 (ADR)](adr/) | 架构决策记录，详见下方索引 |
| [贡献指南](../CONTRIBUTING.md) | 环境搭建、编码规范、PR 流程 |

## 设计决策记录 (ADR)

| ADR | 主题 | 状态 |
|-----|------|------|
| [ADR-001](adr/ADR-001-nan-boxing.md) | NaN-boxing 值表示 | Accepted |
| [ADR-002](adr/ADR-002-triple-backend.md) | 三后端并存策略 | Accepted |
| [ADR-003](adr/ADR-003-ir-layer.md) | IR 中间层 | Accepted |
| [ADR-004](adr/ADR-004-cow-memory.md) | COW 内存模型 | Accepted |
| [ADR-005](adr/ADR-005-debug-consistency.md) | 调试一致性 | Accepted |

## 核心设计原则

- **PERF-12 NaN-boxing**：8 字节值编码，标量零分配，堆类型侵入式引用计数
- **PERF-13 VMStack**：定长数组 1024×8B + 栈顶指针，消除 push_back 开销
- **PERF-14 寄存器式 VM**：32 虚拟寄存器 R0-R31，50+ RegOp，IR 直接 lowering
- **PERF-15 IR 复制传播**：vreg→常量等价替换，仅寄存器后端启用
- **COW 优化**：修改前检查独占所有权，避免不必要的深拷贝

## 安全约束摘要

| 类别 | 措施 |
|------|------|
| DoS 防护 | 源码 ≤10MB、Token ≤100 万、循环 ≤1000 万次 |
| 递归保护 | Parser/Compiler/Formatter 深度限制 512/256 |
| 线程安全 | mutex + atomic、localtime_s/localtime_r |
| 异常安全 | UI 槽 try/catch、unique_ptr 防 bad_alloc 泄漏 |
| 内存安全 | 运行时越界检查、NaN-box 类型前置校验、value-initialize |
| 生命周期 | 无裸指针、shared_ptr 共享所有权、QThread unique_ptr 管理 |

## 开发日志

以下为按时间倒序排列的开发变更记录摘要，完整内容已拆分至各子文档。

### 2026-07-09 变更摘要

| 类别 | 内容 | 详情 |
|------|------|------|
| 第五十二轮审计：教学模块深度审计与修复（P2 × 7 + P3 × 10，共 17 项） | 四模块并行 agent 深度审计（场景库内容正确性、编辑器/REPL/调试面板、核心可视化面板、教学导航/辅助面板），覆盖 30+ GUI 文件，修复 17 项。P2：(1) CodeSnippetEngine var 模板语法错误（var name: int 不被 Parser 支持）。(2) DebugPanel onStackFrameSelected 缺 toString 异常防护。(3) BreakpointConditionPanel 缺 Interpreter 调试 resume 状态守卫。(4) MemoryModelPanel populateNanBoxDetail sourceExpr 含 < 未 HTML 转义。(5) MemoryModelPanel buildAnimPage heapObjectTypes 含 <Value> 未转义。(6) ProfileDashboardPanel buildAnalysis errorMessage 未转义。(7) LearningPathPanel markActivityCompleted 幂等检查阻止 UI 刷新。P3：(8-12) 多面板 HTML 转义一致性补全（VariableInspectorPanel/CallStackPanel/GlossaryPanel/ProfileDashboardPanel）。(13) AstViewer 空状态提示。(14) ErrorHintEngine 死代码清理。(15) BreakpointConditionPanel setOpenExternalLinks 一致性。(16) BugHuntVariantLibrary 内容错误修正（嵌套层数 4→5、局部变量数 8→0）。(17) LabManualPanel codeBlockBg 主题对齐。已知限制：AstViewer setDarkTheme 悬垂 root_ 需 shared_ptr 重构暂不处理。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第五十一轮审计：教学模块深度审计与修复（P1 × 4 + P2 × 10 + P3 × 3，共 17 项） | 对教学板块 40+ 面板文件进行四模块并行 agent 深度审计（数据/内容完整性、交互逻辑、进度追踪/导航、渲染/引擎），修复 17 项问题。P1：(1) MarkdownRenderer 关键字 "class" 腐蚀 span 标签——改用 \uE000/\uE001 私用区字符占位符法。(2) BytecodeTracePanel deque 容量满后表格永久陈旧——添加 dequeShifted_ 标志触发全量重建。(3) LearnerProgressStore save 原子写入——改用 QSaveFile 替代 remove→rename。(4) LearnerProgressStore load 字段类型覆盖——添加顶层字段类型校验拒绝加载。P2：(5) BackendComparePanel comparing_ 守卫失效。(6) BugHuntPanel 搜索与难度筛选不联动。(7) GuidedTour singleShot 旧 targetRect 捕获。(8) ActivityBar checkable toggle 副作用。(9) SyntaxProductionLibrary string-interp EBNF ${}→{}.(10) LabManualContent lab-03 MAX_INTERP_DEPTH→MAX_RECURSION_DEPTH。(11) MarkdownRenderer 表格单元格 escapeHtml。(12) IRTransformPanel populateLoweringDetail HTML 转义。(13) ClosureInspectorPanel/ExceptionFlowPanel setOpenExternalLinks(false)。(14) LabManualContent lab-07 BUG-INTP-3 引用移除。P3：(15-17) MarkdownRenderer URL 引号转义 + 死代码清理。另含第五十轮回退 AUDIT-P3.13（空模块源码合法化）修复 3 个 ModuleNotFound 测试。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第五十轮审计：VM 非变异方法写回与 GUI 信号守卫修复（P1 × 3 + P3 × 3，共 6 项） | 对 VM/IR 后端、Interpreter/Value/GC、Debugger/GUI 线程安全三模块并行 agent 深度审计，修复 6 项。(1) **StackVM 非变异内置方法未设置 lastMutatedReceiver_（P1）**——`compiler/VM.cpp` finishSharedBuiltin/dispatchStringBuiltin。IR 路径对所有 isVarRef 方法调用无条件 LOAD_MUTATED+STORE，非变异方法（len/contains/join/keys/values/has/get + 全部字符串方法）未设置 lastMutatedReceiver_，导致 LOAD_MUTATED 读到旧值覆盖接收者变量。修复：pop() 前添加 `lastMutatedReceiver_ = peek(0)`，对齐 RegisterVM L1736-1737。(2) **handleVmStepResult 守卫误杀 FINISHED/ERROR/NOT_READY（P1）**——`app/ide.cpp`。VmStepper 在 emit FINISHED/ERROR 前已合法置 isVmInitialized_=isVmRunning_=false，守卫 `!isVmInitialized() && !isVmRunning()` 命中 return 跳过 UI 更新，Step 模式下所有 VM 按钮禁用无法恢复。修复：守卫收窄到仅 PAUSED_AT_BREAKPOINT 路径。(3) **callInstanceMethod 绑定循环 argValues 越界（P1）**——`interpreter/Interpreter.cpp`。ROUND49 第二次重查找后 method 参数可能增多但无 bounds check。修复：添加 `if (i < argValues.size())` 越界保护，对齐 constructClassInstance。(4) **VmStepper 预执行断点 hitCount 未递增（P3）**——`app/VmStepper.cpp` stepByMode。修复：预执行命中分支补充 `vmBreakpointHitCounts_[initLine]++`。(5) **CodeEditor 单行偏移未发射 breakpointsChanged（P3）**——`gui/CodeEditor.cpp` onContentsChange。修复：单行偏移路径末尾补充 `emit breakpointsChanged()`。(6) **VMCalls 帧布局损坏错误路径栈未清理（P3）**——`compiler/VMCalls.cpp` executeMethodCall。修复：return 前添加 `popN(1 + fieldCount + argCount)`。P2 保留现状：visitVarDecl 自动实例化 #2 fix + callInstanceMethod argCount 重校验（极端边界场景）。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第四十九轮审计：沙箱安全与格式化正确性修复（P0 × 1 + P1 × 2 + P2 × 2，共 5 项） | 针对条件断点沙箱深拷贝的环检测缺失、SandboxGuard 状态恢复遗漏、Formatter try/catch/finally 注释注入缺少尾部换行、VM RUN 模式断点不同步、Lexer Unicode 转义错误恢复路径不一致等问题进行修复。另有两项 ROUND49 修复（IR 路径索引重复求值、模块词法/语法错误检查）因引入回归而回退，留待后续重新实现。(1) **沙箱深拷贝无环检测，自环容器触发无限递归栈溢出（P0）**——`interpreter/Interpreter.cpp` deepCloneForSandbox。修复：添加 visited 集合 + 深度保护（上限 256），对 array/dict/instance/closure 四种堆类型检测环，对齐 Value::cloneImpl 模式。(2) **SandboxGuard 析构未恢复 evaluationStepCount_（P1）**——`interpreter/Interpreter.cpp` evaluateCondition。条件求值返回后 evaluationStepCount_ 仍 > 0，后续正常执行累计超 MAX_CONDITION_STEPS 后抛虚假 RuntimeError 冻结主程序。修复：SandboxGuard 析构中恢复 evaluationStepCount_ = 0。(3) **try/catch/finally 间注释注入缺少尾部换行（P1）**——`formatter/Formatter.cpp` visitTryStmt。reindentBlockComment 不添加尾部换行，导致 catch/finally 关键字被并入注释行。修复：补尾部 "\n"，对齐 formatBlock/formatClassDecl 模式。catch 和 finally 两处同步修复。(4) **VM RUN 模式下断点变更不同步到 VmStepper（P2）**——`app/ide.cpp` createNewEditorTab。编辑器断点变更回调仅检查 isDebugPaused()，VM RUN 模式下始终为 false。修复：补充 VM 模式同步分支，isVmInitialized()||isVmRunning() 时调用 syncVmBreakpoints()。(5) **Unicode 转义错误恢复路径与 \xNN/\uXXXX 不一致（P2）**——`lexer/Lexer.cpp` string。\u{...} 三处码点验证失败路径直接 return 未跳过到字符串结束。修复：统一标记 codepointError 后复用与 hexError 相同的跳过到字符串结束逻辑。回退的修复：R1 `compiler/IR.cpp` BUG-IR-REVAL-1 fix（索引重复求值，导致 StackVM IR 回归，a[0].push(99) 报"该类型不支持索引赋值"）/ R2 `interpreter/InterpreterModules.cpp` AUDIT-P2-ROUND49 fix（模块词法/语法错误检查，导致模块 throw "fail" 被误报为语法错误，AuditF8_ModuleExceptionNoCrash 测试失败）。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |

### 2026-07-08 变更摘要

| 类别 | 内容 | 详情 |
|------|------|------|
| CI 跨平台流水线全面修复（16 项根因 + 1 项后端 bug，共 17 项） | GitHub Actions CI 全平台失败（Windows/Ubuntu/macOS/Docker/clang-format），经多轮排查定位并修复 16 项 CI 基础设施根因与 1 项 RegVM IR 后端 bug。(1) **install-qt-action v4 输出变量名误用（CI/变量引用）**——`.github/workflows/ci.yml` 原使用 `${Qt6_DIR}` bash 变量语法，第一轮改为 `${{ env.Qt6_DIR }}` GitHub Actions 表达式语法，但 install-qt-action v4 实际设置的输出变量名是 `QT_ROOT_DIR`（非 `Qt6_DIR`），导致 macOS 上仍展开为空。修复：全部 7 处统一改为 `${{ env.QT_ROOT_DIR }}`。(2) **打包步骤缺少 runner.os 条件（CI/条件缺失）**——三个打包步骤仅 `if: success()`，Windows runner 上 Linux 打包步骤运行时因 `out/build/linux-release` 不存在而失败。修复：添加 `runner.os == 'Windows'/'Linux'/'macOS'` 条件。(3) **Ubuntu headless runner 无 X11 显示（CI/headless）**——Qt GUI 测试（50+ 个面板 E2E 测试）因 `qt.qpa.xcb: could not connect to display` 全部 Subprocess aborted。修复：Linux 测试步骤添加 `QT_QPA_PLATFORM: offscreen` 环境变量（coverage-linux 作业同步）。(4) **qtsvg 模块在 Qt 6.8.3 的 aqtinstall 元数据中不可用（Docker/模块不可用）**——Docker 构建时 `aqt install-qt ... -m qtsvg` 报 `packages ['qtsvg'] were not found`。修复：移除 `-m qtsvg`，仅安装 base Qt（与 CI install-qt-action 一致）。(5) **clang-format 格式违规（代码风格）**——`gui/MarkdownRenderer.cpp` 被修改但未执行 `clang-format -i`，CI 的 clang-format 检查报 571-575 行格式违规。修复：本地执行 `clang-format -i`（版本 22.1.5，与 CI 一致）。(6) **macOS 26/Xcode 26 移除 AGL 框架（CI/macOS 链接）**——Qt 6.8.x 的 FindWrapOpenGL.cmake 仍引用 AGL 导致链接失败。上轮已添加 sed 修补步骤但因变量名问题（#1）未执行。修复 #1 后本步骤正常执行。(7) **Dockerfile 未安装 cmake（Docker/依赖缺失）**——builder 阶段执行 `cmake --preset linux-gcc-release` 报 `cmake: not found`（exit code 127）。base 阶段 apt-get install 列表遗漏 cmake。修复：添加 `cmake` 到 apt-get install 列表。(8) **METHOD_CALL 的 obj vreg 寄存器过早释放（P1/寄存器生命周期）**——`compiler/RegisterBytecodeBackend.cpp` collectVRegLastUse 中 `this.arr.push(4)` 编译为 METHOD_CALL + LOAD_LOCAL + LOAD_MUTATED 序列，METHOD_CALL 的 obj vreg 在 METHOD_CALL 后立即释放，中间的 LOAD_LOCAL 复用该寄存器覆盖变异后的数组，LOAD_MUTATED 读到错误值。MSVC 因 `unordered_map` 迭代顺序恰好不触发，GCC 触发导致 `MethodCallProbe.NestedMemberAccessPush` 测试失败。修复：为 METHOD_CALL/SUPER_CALL 的 obj vreg 设默认最后使用为当前指令，若后续有 LOAD_MUTATED 则覆盖延长到 LOAD_MUTATED，非变异方法调用（如 len/substr）不延长。(9) **aqtinstall 安装目录名与输入架构名不一致（Docker/路径错误）**——`Dockerfile` QTDIR 环境变量。aqtinstall 3.3.0 接受 `linux_gcc_64` 作为输入参数（用于 Qt 6.7+ 包查找），但实际安装目录仍为 `gcc_64`（Qt 包内部使用旧目录名）。Dockerfile 的 QTDIR 指向 `/opt/qt6/6.8.3/linux_gcc_64`（不存在），导致 `find_package(Qt6)` 找不到 Qt6Config.cmake。修复：QTDIR 改为 `/opt/qt6/6.8.3/gcc_64`。(10) **AGL framework 引用来自 Qt 二进制 framework 文件的 LC_LOAD_DYLIB（CI/macOS 链接）**——`.github/workflows/ci.yml` macOS AGL stub framework 步骤。前两轮尝试用 sed 修补 .cmake/.prl 配置文件中的 `-framework AGL` 字符串，但实际 AGL 引用来自 QtGui.framework/QtGui 等二进制文件的链接依赖（LC_LOAD_DYLIB），sed 无法修改二进制文件。链接器传递依赖时仍报 `ld: framework 'AGL' not found`。修复：在 `/Library/Frameworks/` 创建空的 AGL stub framework（用 clang 编译空 dylib 作为 AGL 二进制），链接器能找到 AGL（虽然为空），运行时不需要 AGL（Qt 6.8 不再调用 AGL API）。(11) **ROUND49 修复引入的代码未执行 clang-format（代码风格）**——`interpreter/Interpreter.cpp` CI 报 396 行和 2404 行格式违规。修复：本地执行 `clang-format -i`（版本 22.1.5，与 CI 一致）。(12) **Docker 容器缺少 OpenGL 开发头文件（Docker/依赖缺失）**——`Dockerfile` base 阶段 apt-get install。Qt6Gui 的 CMake 配置依赖 `find_package(OpenGL)`，但 Dockerfile 仅安装 `libgl1`（运行时库），缺少开发头文件，报 `Could NOT find OpenGL (missing: OPENGL_opengl_LIBRARY OPENGL_glx_LIBRARY OPENGL_INCLUDE_DIR)`，进而 `Qt6Gui could not be found because dependency WrapOpenGL could not be found`。修复：添加 `libgl-dev` / `libgles-dev` / `libegl-dev` 开发头文件，同时添加 `libglib2.0-dev` / `libfontconfig1-dev` / `libfreetype6-dev` 满足 Qt6Gui 字体子系统依赖。(13) **Docker COPY 文件与目录混合导致目录扁平化（Docker/COPY 扁平化）**——`Dockerfile` builder 阶段 `COPY CMakeLists.txt CMakePresets.json cmake/ ./` 将多个源路径混合 COPY 到 `./` 时，目录源 `cmake/` 内容被扁平化到 `./` 而非保持 `./cmake/` 子目录结构，导致 `include(cmake/minilang_core.cmake)` 找不到文件。修复：拆分为 `COPY CMakeLists.txt CMakePresets.json ./` + `COPY cmake/ ./cmake/`。(14) **macOS arm64 上 `using Base::Base` 继承构造函数不生成符号 + QFluentKit patch 机制（CI/macOS 链接 + patch 机制）**——`.github/workflows/ci.yml` + `Dockerfile` + `patches/qfluentkit-local-fixes.patch` + `third_party/QFluentKit/QFluent/src/QFluent/SpinBox.h`。QFluentKit 的 SpinBox.h 中 5 个子类使用 `using Base::Base` 继承构造函数，MOC 生成的元类型代码引用该构造函数，但 macOS arm64 Clang 不为 `using Base::Base` 生成符号，导致 `Undefined symbols for architecture arm64` 链接失败。修复：将 5 个子类的 `using Base::Base` 替换为显式构造函数，同时将 InlineSpinBoxBase 方法从 .cpp 声明改为 .h 内联实现。因 QFluentKit 是第三方 submodule（无法直接推送修改），采用 patch 文件机制：`git -C third_party/QFluentKit diff` 导出为 `patches/qfluentkit-local-fixes.patch`，在 ci.yml 的 build-test/coverage/coverage-linux/test-harness 作业和 Dockerfile 中添加 `git apply --directory=third_party/QFluentKit patches/qfluentkit-local-fixes.patch` 步骤，在 CMake 配置之前应用。(15) **AGL stub dylib 缺少 -install_name 导致运行时 dyld 找不到库（CI/macOS 运行时）**——`.github/workflows/ci.yml` macOS AGL stub framework 步骤。前一轮创建 AGL stub framework 时先在 `/tmp/AGL.framework/` 创建 dylib 再 `mv` 到 `/Library/Frameworks/`，但 `clang -dynamiclib` 默认 install_name 是输出路径（`/tmp/AGL.framework/Versions/A/AGL`），链接器把这个临时路径写入 minilang_tests 可执行文件的 LC_LOAD_DYLIB。运行时 dyld 按该路径查找，但 `/tmp/AGL.framework` 已被 mv 走，报 `dyld: Library not loaded: /tmp/AGL.framework/Versions/A/AGL`，GoogleTestAddTests.cmake 报 `Subprocess aborted`。修复：(1) 直接在 `/Library/Frameworks/AGL.framework/` 创建 dylib（用 sudo）；(2) 用 `-install_name /Library/Frameworks/AGL.framework/Versions/A/AGL` 设置最终路径，链接器写入正确路径，运行时 dyld 能找到。(16) **Docker 容器缺少 git 导致 `git apply` 失败（Docker/依赖缺失）**——`Dockerfile` base 阶段 apt-get install 列表遗漏 git，builder 阶段执行 `cd third_party/QFluentKit && git apply /app/patches/qfluentkit-local-fixes.patch` 报 exit code 127（command not found）。修复：添加 `git` 到 apt-get install 列表。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第四十八轮：第四十五轮保留现状问题一次性修复（P1 × 2 + P2 × 7 + P3 × 3，共 12 项） | 将第四十五轮审计中标记为"保留现状"的 12 项问题一次性全部修复。(1) **条件断点求值无步数上限，while(true){} 冻结 UI（P1）**——`interpreter/Interpreter.h` + `interpreter/Interpreter.cpp` evaluateCondition 沙箱化求值条件断点表达式但无步数限制。修复：新增 `evaluationStepCount_` 成员 + `MAX_CONDITION_STEPS = 100000` 常量，evaluate 入口单点计数，正常执行零开销。(2) **onContentsChange 多行删除断点行号偏移错误（P1）**——`gui/CodeEditor.cpp` 原实现仅处理单行插入/删除。修复：利用 charsAdded 推导 linesRemoved，三分处理断点（保持/删除/平移）。(3) **closeUpvaluesFrom slot 越界仅打 Warning（P2）**——`compiler/VM.cpp` + `compiler/RegisterVM.cpp` 三处 slot 越界仅打 Logger::Warning。修复：改为 runtimeError + hasError_=true，仍执行 isClosed+erase 防悬垂。(4) **SandboxGuard 闭包 closureEnv 与外层环境共享（P2）**——`interpreter/Interpreter.cpp` deepCloneForSandbox + `interpreter/Value.h` setClosureEnv。修复：从 capturedVars 重建新的 closureEnv，断开与外层环境共享。(5) **writeBackCapturedVars 将局部重声明变量写回 capturedVars（P2）**——`interpreter/Environment.h` + `interpreter/InterpreterCalls.cpp`。修复：Environment 新增 capturedVarNames_ 字段，rebuildEnvFromSnapshot 导入时 markAsCaptured，visitVarDecl 重声明时 unmarkCaptured，writeBackCapturedVars 仅写回 isCapturedVar 为 true 的变量。(6) **typeMatch 不支持 dict[K:V]、fun(params):ret、T?（P2）**——`interpreter/Interpreter.cpp` typeMatch + `parser/Parser.cpp` parseTypeAnnotation。修复：typeMatch 开头新增三种匹配。(7) **AST 节点缺少闭合行号字段（P2）**——`ast/ASTNode.h` + `parser/Parser.cpp`。修复：ClassDecl 新增 closingBraceLine，TryStmt 新增 catchKeywordLine/finallyKeywordLine，InterpolatedString 新增 endLine。(8) **Formatter 丢弃类/try/插值字符串尾注释（P2）**——`formatter/Formatter.cpp`。修复：利用新增的闭合行号字段精确注入尾注释。(9) **closeEvent 期间 pending 信号导致 stale UI 更新（P2）**——`app/ide.cpp`。修复：handleVmStepResult/runtimeError/genericError 添加状态守卫。(10) **Formatter escapeStringContent 未转义控制字符和非 BMP 字符（P3）**——`formatter/Formatter.cpp`。修复：0x01-0x1F 和 0x7F 用 \xNN 转义，非 BMP 用 \u{XXXXXX} 转义。(11) **Lexer \uXXXX 仅支持 4 位 BMP 码点（P3）**——`lexer/Lexer.cpp`。修复：case 'u' 开头检查 `{`，读取 1-6 位十六进制，支持 0x000000-0x10FFFF。(12) **空模块源码被当作加载失败（P3，已回退）**——`interpreter/InterpreterModules.cpp` + `compiler/Compiler.cpp` + `compiler/IR.cpp`。修复（已回退）：对空源码构造空 AST 跳过词法/语法分析。回退原因：production loader 在文件不存在时返回空字符串，无法与真正的 0 字节文件区分，导致 import "nonexistent" 静默成功。回退到原始语义（空源码 = 加载失败）。保留现状：P1.1 break/continue finally 三后端不一致（需续跳机制，8+ 文件全链路改动风险过高）/ P3.14 Lexer \xNN 语义不一致（建议保持现状）。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1753/1753 测试通过（排除未构建的 minilang_perf_test 占位） | [CHANGELOG](../CHANGELOG.md) |
| CI 流水线跨平台修复（aqtinstall 架构名 + macOS ar + Windows 翻译部署 + clang-format 版本固定） | 修复 GitHub Actions CI 全部跨平台作业失败（Windows / Ubuntu / macOS / Docker）。5 个独立根因：(1) **Qt 6.7+ Linux 架构名变更**——`gcc_64` 改为 `linux_gcc_64`，aqtinstall ≤3.2.1 仍用旧名导致 `qt_base not found`（[aqtinstall#908](https://github.com/miurahr/aqtinstall/issues/908)，PR #909 修复含于 3.3.0）；修复：`AQT_VERSION===3.3.0` + Linux arch 改为 `linux_gcc_64`。此前误诊为"aqtinstall 3.2.0+ 回归，3.1.21 可用"——实测 3.1.21 同样失败。(2) **qttools 模块不可用**——Qt 6.8.3 的 aqtinstall 元数据中 `qttools` 缺失（所有平台报 `packages ['qttools'] were not found`）；修复：移除全部 5 处 `modules: qttools`，CMake 对 LinguistTools 缺失有优雅降级。(3) **macOS ar @response_file**——`CMAKE_NINJA_FORCE_RESPONSE_FILE` 在 macOS 强制开启后系统 `ar` 不支持 `@rsp` 语法；修复：CMakeLists.txt 添加 `AND NOT APPLE` 条件。(4) **Windows copy_if_different 空列表**——`MINILANG_QM_FILES` 为空时 `cmake -E copy_if_different` 参数不足报 usage 退出 1；修复：条件添加 `AND MINILANG_QM_FILES` 检查。(5) **clang-format 版本漂移**——CI 用 apt.llvm.org snapshot 版本与本地 pip 22.1.5 不一致；修复：CI 改用 `pip install clang-format==22.1.5`，并对全部 .cpp/.h 执行 `clang-format -i` 统一格式化。配套：docker-compose.yml QT_VERSION 回退值 6.10.2→6.8.3。 | [CHANGELOG](../CHANGELOG.md) |
| 第四十七轮：教学板块第四轮深度审计与修复（P1×7 + P2×9，共 16 项） | 对教学板块进行第四轮深度审计与优化，采用四模块并行 agent 策略（数据/内容完整性、交互面板逻辑、学习进度追踪与导航、视觉渲染与教学引擎），共发现 63 项问题（P1×7，P2×24，P3×32）。本轮修复 P1 全部 7 项 + P2 关键 9 项，共 16 项。(1) **BugHuntLibrary BUG-READ-03 字典键非法 + 预期答案错误（P1）**——`gui/BugHuntLibrary.cpp` sourceCode `var x={a:1,b:2};` 使用未加引号字典键违反 Interpreter.cpp:1888 强制要求（字典键必须是字符串），三后端均报运行时错误；预期答案 B（多行展开加引号）与 Formatter 实际输出（单行无引号）矛盾。修复：sourceCode 改为 `var x={"a":1,"b":2};`，预期答案改为 A（一行），explanation 说明 Formatter 保持单行格式。(2) **LabManualContent lab-02 优先级链 term/factor 注释完全说反（P1）**——`gui/LabManualContent.cpp` 练习解释称「term(*,/) → factor(+,-)，term 比 factor 更内层」，但同章主体内容和 SyntaxProductionLibrary EBNF 均为 term 管 +−、factor 管 */%、factor 在 term 内部。修复：改为「term(+,-) → factor(*,/,%)，factor 比 term 更内层，所以 * 先结合」。(3) **TokenPuzzlePanel onCheckAnswer 错误返回路径未恢复 busy_ 守卫（P1）**——`gui/TokenPuzzlePanel.cpp` 入口设置 busy_=true + checkBtn_ 禁用，但「数量不对」和「第 N 个 token 不对」两条错误返回路径直接 return 未恢复。一次错误尝试后按钮永久禁用，游戏不可玩。修复：两条错误返回前恢复 busy_=false + setEnabled(true)。(4) **LearnerProgress save 非原子写入导致全部进度丢失（P1）**——`gui/LearnerProgress.cpp` 直接以 WriteOnly\|Truncate 打开目标文件写入，崩溃/断电/磁盘满时文件被截断为部分内容或空文件。修复：改为「写 .tmp → flush → close → remove 旧 → rename」原子写入模式，保证写入原子性。同时写入 schemaVersion 字段支持未来 schema 迁移。(5) **CodeJourneyInfoPanel markCompleted 完成态持久化断裂（P1）**——`gui/CodeJourneyInfoPanel.cpp` journeyCompleted_ 是纯内存标志，持久化完全依赖外部 connect 信号到 learningPathPanel_->markActivityCompleted，但该连接要求 learningPathPanel_ 已构造（懒加载）。不经学习路径地图直接打开面板时信号无接收方，完成态丢失。修复：markCompleted 内直接调用 LearnerProgressStore::markCompleted + save，信号仅用于 UI 刷新。(6) **ProfileDashboardPanel runProfile 重入竞态（P1）**——`gui/ProfileDashboardPanel.cpp` processEvents(ExcludeUserInputEvents) 不阻止定时器/信号槽/DeferredDelete，定时器触发的信号链路可能间接调用 runProfile 导致重入，lastResults_ 被覆盖、状态混乱。runProfileBtn_ setEnabled(false) 只防直接点击。修复：增加 profiling_ 成员 + 入口 if(profiling_) return 守卫，RAII 风格在所有出口恢复（错误返回点 + 正常结束点）。(7) **LearningPathPanel onResetProgress 重置顺序错误导致「假重置」（P1）**——`gui/LearningPathPanel.cpp` 先 reset()（清空内存）再 save()（写入磁盘），save 失败时内存已清空但磁盘仍是旧数据，当前会话显示已重置但重启后旧数据回归。修复：save 失败时弹窗告知用户「进度文件写入失败，内存已重置但未落盘，重启后进度将恢复」。(8) **LabManualContent lab-01 引用不存在的 println 内建方法（P2）**——`gui/LabManualContent.cpp` 解释文本称「println 会自动换行」，但 MiniLang 无 println 内建方法。修复：删除「，println 会自动换行」。(9) **VmStackSandboxPanel onCompileAndLoad 缺 traceRunning_ 守卫（P2）**——`gui/VmStackSandboxPanel.cpp` onTraceStep/onTraceReset 都有守卫，但 onCompileAndLoad 遗漏。loadBytecodeFromCurrentLevel 内部调用 vmReset()，会在追踪运行期间重置 VM。修复：入口添加 if(traceRunning_) return。(10) **AstBuilderToyPanel markCurrentCompleted 非幂等（P2）**——`gui/AstBuilderToyPanel.cpp` 幂等守卫只保护 completedLevels_.push_back，未保护 markLevelStars/save/emit。重复检查会重复发射信号 + 重复磁盘写入。修复：将信号发射和持久化纳入幂等守卫，if(!alreadyDone) { save + emit }。(11) **BackendComparePanel buildDiffSummary 三后端全错误时误报「一致性 PASS」（P2）**——`gui/BackendComparePanel.cpp` 只比较 outputLines 不检查 status，三后端均报相同错误时 diffCount=0 误报 PASS。修复：先检查 allOk = (a.status==b.status==c.status=="OK")，三后端均失败时返回「请修复代码」。(12) **GlossaryPanel relatedPanelFor 6 个术语无映射（P2）**——`gui/GlossaryPanel.cpp` kTermToPanel 遗漏 debugger/line-info/module-loader/output-capture/error-recovery/string-interpolation，点击这些术语无反应。修复：补全 6 个术语→面板映射（debugger→breakpoint-condition, line-info→bytecode-trace, module-loader→pipeline, output-capture→vm-sandbox, error-recovery→syntax-explorer, string-interpolation→vm-sandbox）。(13) **BreakpointConditionPanel populateScenarioDetail HTML 转义缺失（P2）**——`gui/BreakpointConditionPanel.cpp` sampleCode 含 < > 字符（如 if (n < 0)），condition 含 < >（如 a > 0 && b < 100），未转义导致 HTML 标签注入/渲染错误。修复：对 title/condition/expectedBehavior/sampleCode 四个字段统一调用 escapeHtml（内联实现，转义 &<>"'）。(14) **PipelineViewer setController 跨阶段数据同步缺失（P2）**——`gui/PipelineViewer.cpp` setController 是纯赋值，未注册 vmStateChanged 监听器。源码页编辑后切到 Token/AST/IR/Bytecode 页显示陈旧数据，用户误以为是最新代码的分析结果。修复：setController 中注册 addVmStateChangedListener(this, [this]{ reloadCurrentStep(); })，析构函数反注册（.h 添加析构函数声明 + setController 改为声明，.cpp 实现）。(15) **PanelAnimator animateShow/animateHide 等四函数无动画去重（P2）**——`gui/PanelAnimator.h` fadeInWidget/slideInWidget 已在第 41 轮修复动画去重，但这 4 个函数遗漏。快速连续点击时多个 QPropertyAnimation 同时写入 maximumHeight/maximumWidth 导致抖动、目标高度不正确。修复：对齐 fadeInWidget/slideInWidget 的 findChildren<QPropertyAnimation*>() 停止旧动画模式。(16) **SyntaxExplorerPanel 可编辑代码区 + 无保护执行死循环冻结 IDE（P2）**——`gui/SyntaxExplorerPanel.cpp` codeEditor_ 是可编辑 QTextEdit，onRunSample 同步执行 Lexer→Parser→Interpreter，无 busy_ 守卫、无超时、无步骤上限。输入 while(true){} 点击运行 → IDE 完全冻结只能强制杀死。修复：codeEditor_ 设为只读，样例代码通过产生式选择切换。关键决策：TokenPuzzle 错误路径恢复守卫（RAII 对称出口）/ LearnerProgress 原子写入（写 .tmp → flush → close → remove → rename + schemaVersion）/ CodeJourneyInfoPanel 持久化与信号解耦（持久化是数据层，信号是 UI 层）/ ProfileDashboardPanel profiling_ 守卫（processEvents 重入标准防御）/ LearningPathPanel save 失败反馈（可接受降级优于假重置）/ BackendComparePanel 一致性判定前置状态过滤（allOk 先检查）/ PipelineViewer 跨阶段数据同步（setController 注册监听器 + 析构反注册）/ SyntaxExplorerPanel 只读方案（最简方案，未来 Worker 线程化时一并处理）。保留现状：LearnerProgress save 返回值调用方忽略（P2 小规模）/ LearningPathPanel refresh O(n) 全量重建（P2 中规模）/ BytecodeTracePanel erase(begin()) O(n) 移位（P2 中规模）/ CallStackPanel 无栈深度截断（P2 小规模）/ MarkdownRenderer 不支持嵌套列表（P2 中规模）/ PipelineViewer populateBytecode 无缓存（P2 小规模）/ VariableInspectorPanel 全局/局部变量排序不一致（P2 小规模）/ LabManualContent lab-01 token 数量 4 vs 5 矛盾（P3 文档修正）/ LearningPathData lab-08 前置不含 lab-07（P3 课程设计调整）/ AstToyLevels 题目 5 根节点命名风格不一致（P3 命名调整）/ TokenPuzzleData 关卡 4 注释作为主流 token（P3 课程设计调整）/ 其余 P3 项按批次归档。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide` + `minilang_tests` 构建通过，全量 1763/1763 测试通过（较第四十六轮无增减） | [CHANGELOG](../CHANGELOG.md) |
| 第四十六轮：super 上下文栈定义类修复 + 行首插入断点回归 + for 作用域隔离 + envPool REPL 清空 + callStack GC roots 防御 | 继续四模块并行 agent 深度审计（VM/IR 后端、Interpreter/GC、Parser/Lexer/Formatter/Module、Debugger/GUI 线程安全）。本轮修复 5 项明确低风险的真实可触发问题（P1 × 2，P3 × 3），其中 super 上下文栈问题经两个 agent 独立确认且修复方案一致。(1) **super 上下文栈压入搜索起始类而非方法定义所在类（P1）**——`interpreter/Interpreter.cpp` callInstanceMethod + `interpreter/InterpreterCalls.cpp` constructClassInstance + `interpreter/Interpreter.cpp` visitVarDecl 三处 classContextStack_.push_back 压入 searchClass->name/cls->name（搜索起始类/实例类），当中间类未定义方法时 findMethod 沿继承链向上找到祖先类的方法，但栈中压入中间类名，导致后续 super 调用从错误的类开始搜索，可能找到同一个方法形成无限递归（C←B←A，B 无 greet，C.greet 调用 super.greet 找到 A.greet，A.greet 的 super 又从 B 搜索再次找到 A.greet）。StackVM/RegisterVM 因编译时编码类名（currentClassName_）而正确，仅 Interpreter 路径有此 Bug。修复：新增 `findMethodDefiningClassName` 辅助方法沿继承链查找方法实际定义所在的类名（与 findMethod 遍历逻辑完全一致，仅额外返回 ClassInfo::name），三处 push 点改用该方法。不走缓存（缓存只存方法指针不存定义类）。(2) **行首插入断点回归（P1）**——`gui/CodeEditor.cpp` onContentsChange 第四十三轮的 BUG-GUI-AUDIT-2 修复用 `position >= block.position() + block.length() - 1` 判断行末插入，但 contentsChange 在变更后发射，行首插入时新块为空（length==1），条件恒为 true，导致行首插入被误判为行末插入，startLine 错误 +1，原行断点未随内容下移。修复：添加 `block.length() > 1` 条件区分非空块（行末插入，原块有内容）与空块（行首插入，新块无内容）。(3) **for 循环作用域隔离错误（P3）**——`common/TypeChecker.cpp` visitForStmt 原实现（AUDIT-BUG-E1 fix）错误地将 for 的 initializer/condition/update/body 各部分隔离为独立作用域，导致 initializer 声明的变量（如 `var i: int = 0`）在 condition/update/body 中不可见，类型检查被跳过。MiniLang（与 C/Java/JS 一致）中 for 循环四部分共享同一作用域。修复：删除各部分之间的 `varTypes = saved` 恢复，仅保留循环结束后的恢复。(4) **envPool_ 无界增长（P3）**——`interpreter/Interpreter.cpp` executeRepl 未清空 envPool_（execute() 有清空），导致 REPL 模式下 envPool_ 跨次累积。修复：executeRepl 中添加 `envPool_.clear()`，与 execute() 对齐。(5) **collectCycle 期间 callStack_ 不在 roots 中（P3）**——`interpreter/Interpreter.cpp` execute 的 GC roots 收集未收集 callStack_。当前 callStack_ 在 collectCycle 前已 clear()，此处为空，但防御性收集保证未来安全。修复：追加遍历 callStack_ 中每个 frame.env 的局部变量作为 GC roots。关键决策：super 压入方法定义所在类而非搜索起始类（新增 findMethodDefiningClassName 不走缓存）/ 行首插入用 block.length() > 1 区分（空块 length==1）/ for 循环四部分共享作用域（body 若为 Block 内部 save/restore 不泄漏）/ executeRepl 清空 envPool_ 对齐 execute() / callStack_ 防御性收集 GC roots（当前为空保证未来安全）。验证排除：IR 路径栈平衡遗留（P3，needsPopForExprStmt 16 种节点与 Compiler.cpp 完全一致，已完全修复）/ Parser C 风格数组语法（已完全清除）/ Parser synchronize 同步点（24 个关键字完整无遗漏）/ breakpointsChanged 信号覆盖（编辑器只读 + VM syncVmBreakpoints 覆盖）/ VmStateChangedListener 反注册（6 面板均完整）/ waitCallbacksIdle 超时（所有调用点均有 3 秒超时）。保留现状：break/continue finally 三后端不一致（P1 需续跳机制）/ 条件断点求值无超时（P1 需步数上限）/ onContentsChange 多行删除断点偏移（P1 需重新设计偏移逻辑）/ closeUpvaluesFrom 越界只打 Warning（P2 需改返回类型）/ SandboxGuard 闭包 env 共享（P2 需 setClosureEnv 接口）/ writeBackCapturedVars 局部重声明（P2 需 capturedVarNames_ 字段）/ typeMatch 不支持 dict[K:V] 等（P2 需三后端同步）/ AST 节点缺 endLine/endColumn（P2 需字段扩展）/ Formatter 注释丢失（P2 需 closingBraceLine 字段）/ closeEvent pending 信号（P2 需逐个状态检查）/ Formatter escapeString 控制字符（P3 小规模）/ \uXXXX 仅 BMP（P3 需 \u{XXXXXX}）/ 空模块加载失败（P3 需改签名）/ Lexer \xNN 语义不一致（P3 保持现状）。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_tests` 构建通过，全量 1763/1763 测试通过（较第四十五轮 1753 增加 10 个测试） | [CHANGELOG](../CHANGELOG.md) |
| 第四十五轮：IR catch upvalue 关闭时机 + 沙箱 instSnaps 悬垂防御 + 类构造参数重校验 + 非 REPL GC roots + Worker 析构兜底 | 继续对 MiniLang IDE 项目进行深度 Bug 排查，采用四模块并行 agent 审计策略（VM/IR 后端、Interpreter/GC、Parser/Lexer/Formatter/Module、Debugger/GUI 线程安全）。本轮修复 5 项明确低风险的真实可触发问题（P2 × 4，P3 × 1）。(1) **IR catch 变量 upvalue 关闭时机不一致（P2）**——`compiler/IR.cpp` visitTryStmt 将 catch 变量 slot 记录到外层 BlockScope.localSlots，依赖外层块退出时 leaveBlockScope 统一关闭；而 Compiler.cpp 直接路径在 catch 块后立即发射 OP_CLOSE_UPVALUE（行 2141-2144）。三后端 upvalue 快照时机不一致——StackVM 直接路径立即关闭，IR 路径延迟到外层块退出。修复：在 visitTryStmt catch 块编译后立即发射 `CLOSE_UPVALUE(slot)`，对齐直接路径。CLOSE_UPVALUE(slot) 关闭所有 slot >= catchVarSlot 的 open upvalue，此时 catch 块内部 slot 已由 catch 块的 leaveBlockScope 关闭，仅 catch 变量本身未关闭。重复关闭是 no-op（closeUpvaluesFrom 检查 isClosed 标志 + openUpvalues_.erase）。(2) **evaluateCondition instSnaps Value* 悬垂风险（P2）**——`interpreter/Interpreter.cpp` evaluateCondition 的 instSnaps 存储 `Value* inst` 指针，inst 来自 `getBoundInstance()` 指向 `variables["this"]` map 条目。条件求值期间若 `unordered_map` rehash 会导致指针悬垂。修复：将 instSnaps 值类型改为 `InstSnapEntry { Environment* env; const void* gcRoot; std::unordered_map<std::string, Value> fields; }`，快照阶段存储 env 而非 inst 指针，析构阶段从 `snap.env->getBoundInstance()` 重新获取 inst。Environment 由 shared_ptr 管理，对象地址稳定；而 Value* 指向 map 条目可能因 rehash 失效。同时通过 gcRootPtr() 比对验证实例身份。(3) **constructClassInstance 类重定义后参数签名未重新校验（P2）**——`interpreter/InterpreterCalls.cpp` constructClassInstance 在参数求值前用旧 initMethod 校验 argValues 数量，求值后重新查找 initMethod 但未重新校验 `argValues.size()`。若 init 参数在求值期间因类重定义而变化（参数更少时多余参数被静默忽略，参数更多时缺失参数走 null 路径），导致三后端语义不一致。修复：在重新查找 initMethod 后补充参数数量校验，错误消息明确指出"类在构造期间被重定义"。(4) **非 REPL 模式 GC roots 为空集（P2）**——`interpreter/Interpreter.cpp` execute() 中仅 REPL 模式收集 gcRoots，非 REPL 模式传入空根集。若 Interpreter 被复用（如测试场景），上一轮残留的循环引用容器在 tracked_ 中，空根集会导致 Phase 2 误判所有 tracked 容器为不可达孤岛并清空子元素。即使 IdeController 每次 Run 创建新 Interpreter（tracked_ 为空，collectCycle 提前返回），此处防御性收集可保证复用场景的正确性。修复：非 REPL 模式也收集 globalEnv_ 顶层变量作为 GC roots。(5) **InterpreterWorker 析构回调兜底（P3）**——`app/InterpreterWorker.h` run() 末尾清空 interp_ 回调，但异常终止路径可能跳过。新增析构函数兜底清空 outputCallback_ 和 inputCallback_，防止未来回归——若 worker 异常终止未走 run() 末尾的清空路径，interp_ 仍持有捕获 this 的悬垂 lambda。关键决策：IR catch upvalue 关闭时机对齐直接路径而非依赖外层块 / instSnaps 存储 Environment* 而非 Value*（Environment 地址稳定，map 条目可能 rehash 失效）/ 类重定义后重新校验参数签名而非假设签名不变 / 非 REPL 模式也收集 GC roots 保证复用场景正确性 / InterpreterWorker 析构兜底防御未来回归。验证排除：AUDIT-PLM-01 ModuleIsolation 默认参数表达式作用域——经核实不是 Bug，MiniLang 默认参数在闭包环境求值（非函数作用域），popScope 后处理 defaultValues 的行为正确。保留现状：break/continue finally 三后端不一致（P1）/ 条件断点求值无超时机制（P1）/ onContentsChange 多行插入/删除断点行号偏移错误（P1）/ super 上下文栈压入搜索起始类而非定义类（P1）/ writeBackCapturedVars 将局部重声明变量写回 capturedVars（P2）/ SandboxGuard 闭包 env 共享导致沙箱副作用泄漏（P2）/ typeMatch 不支持 dict[K:V]、函数类型、可选类型（P2）/ AST 节点缺少 endLine/endColumn（P2）/ RegisterVM closeUpvaluesFrom slot 越界只打 Warning（P2）/ collectCycle 期间 callStack_ 不在 roots 中（P3）/ Formatter escapeString 未转义所有控制字符（P3）/ envPool_ 无界增长（P3）/ 空模块源码被当作加载失败（P3）/ \uXXXX 仅支持 BMP（P3）。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_tests` 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第四十四轮：REPL 状态对称性 + 条件断点沙箱完整性 + 多维数组门控 + 默认参数类型检查 + Formatter 转义去重 | 补档此前一轮审计中已落地但未及时记录到 CHANGELOG 的 8 项修复（P1 × 1，P2 × 7）。本轮审计聚焦 REPL 状态保存/恢复字段对称性、条件断点沙箱副作用隔离完整性、多维数组类型注解门控一致性、默认参数即时类型检查对齐、Formatter 字符串转义规则单一来源。(1) **saveReplState 缺失 moduleMtimes_ 保存（P1）**——`interpreter/Interpreter.cpp` restoreReplState 读取 savedModuleMtimes 但 saveReplState 未写入，用空默认值覆盖 moduleMtimes_，导致后续 import 命中缓存时跳过 mtime 变更检查；添加 `replState_.savedModuleMtimes = moduleMtimes_;` 维持字段对称性。(2) **BytecodeTracePanel captureCurrentState 缺少 isVmRunning() 守卫（P2）**——`gui/BytecodeTracePanel.cpp` 对齐 CallStack/VariableInspector/BreakpointCondition/MemoryModel 4 面板的 isVmRunning() 守卫，VM RUN 批之间捕获轨迹会混入中间状态数据。(3) **isClassTypeDeclStart 多维数组门控（P2，重新应用）**——`parser/Parser.cpp` 第四十三轮已修复 parseTypeAnnotation 多维支持，但此门控函数仍用单次 [] 检查，导致 ClassName[][] paramName 在函数参数处不被识别；改为 while 循环消费连续 [] 后缀（此修复曾被回退，本轮重新应用）。(4) **classDecl 多维数组字段（P2）**——`parser/Parser.cpp` classDecl 字段解析单次 [] 改为 while 循环。(5) **TypeChecker visitAssignment 缺少 NullLiteral 分支（P2）**——`common/TypeChecker.cpp` 对齐 checkVarDecl 的 NullLiteral 分支。(6) **evaluateCondition 沙箱不完整（P2）**——`interpreter/Interpreter.cpp` SandboxGuard 未保存/恢复 loopFlow_ 和模块状态（moduleCache_/moduleExports_/moduleMtimes_/exportedNames_），条件断点求值包含 import 或循环语句时副作用泄漏；新增 5 个成员构造时保存、析构时恢复。(7) **callInstanceMethod 默认参数类型检查时机（P2）**——`interpreter/Interpreter.cpp` 默认参数评估后立即 checkType，对齐 constructClassInstance。(8) **Formatter escapeString 转义规则重复（P2）**——`formatter/Formatter.cpp` 提取公共函数 escapeStringContent，formatStringLiteral 和 formatInterpolatedString 共用。关键决策：saveReplState 字段对称性而非功能修复 / evaluateCondition 沙箱保存模块状态而非禁止 import / isClassTypeDeclStart 多维门控重新应用（三函数一致） / 默认参数即时类型检查对齐 constructClassInstance / escapeStringContent 提取为文件级 static 函数。保留现状：try-finally break/continue 不一致（P1）/ ClosureData 未注册 GcManager（P2）/ Formatter 注释丢失与插值注释错位（P2 需 AST 字段）。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide`+`minilang_tests` 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第四十三轮：RuntimeError finally 语义修复 + 多维数组类型注解 + GC vmClosure upvalues + 前导零检测 + 断点同步 + stale 信号防御 | 继续对 MiniLang IDE 项目进行深度 Bug 排查，采用四模块并行 agent 审计策略（VM/IR 后端、Interpreter/Value、Parser/Lexer/Formatter、Debugger/GUI 线程安全），共发现约 46 个问题（P1 × 6 含 3 项已知保留，P2 × 19，P3 × 16+）。本轮修复 10 项明确低风险的真实可触发问题（P1 × 2，P2 × 8，含 1 项预先存在的编译错误）。(1) **RuntimeError 绕过 finally 块（P1）**——`interpreter/Interpreter.cpp` visitTryStmt 外层 catch (const ThrowException&) 仅捕获 throw 语句异常，RuntimeError（数组越界/类型不匹配/除零等）不被捕获直接传播跳过 finally，三后端不一致（VM 路径 OP_TRY_BEGIN 捕获所有运行时错误会执行 finally）；新增 catch (const ReturnException&) + catch (const DebugStopException&) + catch (...) 三层捕获，ReturnException/DebugStopException 直接 re-throw，catch(...) 执行 finally 后 re-throw。(2) **closeEvent 期间 pending pausedAt 信号导致 stale UI 更新（P1）**——`app/ide.cpp` onPausedAt 入口检查 !isRunning() && !isDebugPaused() 直接返回，丢弃 stale 信号，防止 Ide 析构时处理 pending 事件访问已析构成员。(3) **visitTryStmt L1634 注释错误（P2）**——修正注释：return 不执行 finally（三后端一致）；break/continue Interpreter 执行 finally（loopFlow_ 状态标志），VM 跳过 finally，三后端不一致为已知限制。(4) **Lexer 前导零 007 静默接受（P2）**——`lexer/Lexer.cpp` number() 添加前导零检测，source_[start_]=='0' && isAsciiDigit(peek()) 报错并消费整个数字序列，提供等价合法值建议。(5) **Parser 二维数组类型注解 int[][] 不支持（P2）**——`parser/Parser.cpp` parseTypeAnnotation 将单次 if 改为 while 循环消费连续 [] 后缀。(6) **typeMatch 多维数组类型注解未识别（P2）**——`interpreter/Interpreter.cpp` typeMatch 改用 substr 后缀比较 "[]"，剥离末尾 [] 后递归 typeMatch 每个元素，天然支持多维。(7) **GC roots 未覆盖 vmClosure upvalues（P2）**——`interpreter/GcManager.cpp` markValue 的 VAL_CLOSURE 分支增加 vmClosure->upvalues 遍历，对 isClosed=true 的 upvalue 的 value 字段调用 markValue。(8) **waitCallbacksIdle 无超时 spin-wait（P2）**——`debug/DebugController.cpp` + `debug/DebugEvaluator.h` 两处均添加 3 秒超时上限。(9) **断点切换在 Interpreter 调试会话期间不同步（P2）**——`gui/CodeEditor.h/cpp` 新增 breakpointsChanged() 信号，`app/ide.cpp` 连接此信号在 isDebugPaused() 状态下同步到 DebugController。(10) **BuiltinMethods executeBuiltinType 参数名 col 与函数体 column 不一致（P2，预先存在）**——`interpreter/BuiltinMethods.cpp` 统一参数名为 column。关键决策：RuntimeError finally 用 catch(...) 而非逐个捕获 / 多维数组用递归剥离而非扁平检查 / GC vmClosure 仅标记 isClosed=true 的 upvalue / 断点同步用 isDebugPaused 守卫 / waitCallbacksIdle 超时 3 秒 / stale 信号防御用状态检查而非 removePostedEvents。保留现状：break/continue finally 不一致（P1）/ 条件断点求值无超时（P1）/ onContentsChange 断点偏移（P1）/ super 上下文栈压入搜索类（P1）/ SandboxGuard 闭包 env 共享（P2）/ writeBackCapturedVars 局部重声明（P2）/ Formatter 插值注释（P2）。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide`+`minilang_tests` 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第四十二轮：GC 不变量修复 + break upvalue 关闭 + COW 异常安全 + UI 禁用遗漏 + QSettings 持久化 | 继续对 MiniLang IDE 项目进行深度 Bug 排查，聚焦三后端语义一致性、GC 不变量、COW 异常安全、UI 线程安全与进程退出持久化。共修复 9 项问题（P1 × 2，P2 × 6，P3 × 1）。(1) **GcManager collectCycle 后 aliveSet_ 未重建（P1 内存泄漏）**——`interpreter/GcManager.cpp` Phase 3 用 survivors 重建 `tracked_` 后 `aliveSet_` 未同步重建，下一轮 Phase 2 将 survivors 误判为"已销毁"跳过 sweep，survivors 变为不可达循环孤岛时永久泄漏；Phase 3 后用 tracked_（= survivors）重建 aliveSet_，维持 `aliveSet_ = tracked_ 中仍存活的对象集合` 不变量。(2) **Parser synchronize 缺少 TK_FROM（P1）**——`parser/Parser.cpp` 两处 switch 各添加 `case TK_FROM: return;`，import 语句 from 关键字缺失时作为同步点停止错误恢复。(3) **break 跳过 OP_CLOSE_UPVALUE（P2）**——`compiler/Compiler.cpp` + `compiler/IR.cpp` visitBreakStmt 直接发 OP_JUMP 跳到 breakTarget（在 OP_CLOSE_UPVALUE 之后）跳过 upvalue 关闭，导致 break 跳出循环时闭包捕获变 by-reference（三后端不一致）；LoopContext 增加 bodySlotBase + needCloseUpvalue 字段，visitBreakStmt 在 OP_JUMP 前发射 OP_CLOSE_UPVALUE（直接路径 + IR 路径同步修复，OP_CLOSE_UPVALUE bodySlotBase 关闭 slot >= bodySlotBase 的全部 open upvalues 含嵌套块变量）。(4) **COW ensureUnique registerTracked 异常安全（P2）**——`interpreter/Value.h` ensureUnique 先 release 旧引用 + transfer 所有权再 registerTracked，若 registerTracked 抛 bad_alloc 则 raw 泄漏 + 双重释放；将 registerTracked 移到 release 之前，若抛异常 unique_ptr 自动 delete cloned，旧引用计数不变。(5) **GcManager registerTracked aliveSet_ 异常安全（P2）**——`interpreter/GcManager.cpp` aliveSet_.insert 在 push_back 成功后抛 bad_alloc 时 tracked_ 含 obj 但 aliveSet_ 不含，破坏不变量；用 try/catch 包裹 insert，失败时 pop_back 回滚。(6) **VM 路径 formatAction 未禁用（P2）**——`app/ide.cpp` onVmRun/handleVmStepResult RUNNING 分支 + setVmStepActionsEnabled 添加 formatAction_ 禁用，对齐 setRunningState 策略。(7) **VM 路径 REPL 输入未禁用（P2）**——同上添加 replPanel_->setInputEnabled(false)，防止 VM 运行期间并发访问 Interpreter/VM 状态。(8) **forceStop _Exit(0) 前补 QSettings sync（P2）**——`app/WorkerManager.cpp` 两处 _Exit(0) 前添加 QSettings::sync()，防止 _Exit 跳过析构导致最近工作区/窗口布局/引导标记等未刷盘丢失。(9) **visitTryStmt 注释修正（P3）**——`compiler/Compiler.cpp` 注释"break/continue/return 不会执行 finally（三后端一致）"修正为准确描述：break/continue 时 Interpreter 执行 finally、VM 跳过（不一致），return 三后端均跳过（一致）。关键决策：break upvalue 关闭用 bodySlotBase 单次发射 / COW ensureUnique 将 registerTracked 前移而非回滚 / registerTracked 用 try/catch 回滚而非更换容器 / UI 禁用统一到 setVmStepActionsEnabled。保留现状：break/continue finally 不一致（P1 需 finally 续跳机制）/ 条件断点求值无超时（P1）/ onContentsChange 断点行号偏移（P1）/ Formatter 插值注释（P2 需 AST endLine）/ waitCallbacksIdle 无超时（P2）。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide`+`minilang_tests` 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第四十一轮：教学模块第三轮深度审计（教学内容一致性 + 防重复守卫 + 错误友好化 + 搜索高亮 + Markdown 占位符） | 对全部教学模块面板进行第三轮深度 Bug 审计，采用四模块并行 agent 策略，共发现约 38 个问题（P1 × 9，P2 × 17，P3 × 12）。本轮修复 P1 全部 + P2 主要项 + P3 关键项共 16 项。(1) **7 处教学内容错误（P1）**——CallStackPanel/IRTransformPanel/MemoryModelPanel/BytecodeTracePanel/VariableInspectorPanel/SyntaxProductionLibrary/BreakpointConditionPanel 的 `Point.new()` → `Point()`、`fun new` → `fun init`、`0 or "default"` 注释错误，对齐 MiniLang 构造方法必须命名为 init + `Point(...)` 直接调用类名触发构造 + or/and 短路语义（左操作数为假时返回右值）。(2) **4 面板浏览即完成映射缺失（P1）**——`app/ide.cpp` ensureTeachingPanelCreated 中 BytecodeTrace/CallStack/VariableInspector/BreakpointCondition 4 面板首次浏览未映射到 LearningPath 完成活动，补 markActivityCompleted 调用。(3) **op-priority-challenge 同步缺失（P1）**——op-priority-challenge 关卡未与 LearningPath 同步，补 markActivityCompleted。(4) **BugHunt 全题完成才发射 challengeSolved（P1）**——onTripleVerify 在未全部完成时即发射 challengeSolved，改为全部题目完成才发射。(5) **3 面板防重复提交守卫（P2）**——BugHuntPanel onRunVerify/onTripleVerify + TokenPuzzlePanel onCheckAnswer/onSkipLevel + LabManualPanel onSubmitChoiceExercise/onSubmitExpectedOutputExercise 无防重复守卫，processEvents 让渡控制权期间快速重复点击触发多次 runStringCaptureOutput + 多次 save() 磁盘 I/O + recordFailure 计数虚高；添加 busy_/submitting_ 守卫 + 按钮禁用/恢复 + LabManual 答对后禁用提交按钮 + setText("已通过")。(6) **LabManualPanel controller 为空友好提示（P2）**——`!ERROR: No controller` → 友好提示"运行环境未就绪，请先在主界面运行一次程序"，且仅在 controller 可用时才记录失败。(7) **TeachingTreePanel 搜索过滤高亮失效（P2）**——setCurrentPanel 在搜索过滤激活时直接 setCurrentItem(hidden item)，选中态被设置但用户不可见；跳转前重置搜索过滤（QSignalBlocker 避免 clear() 重入 onSearchChanged）。(8) **MarkdownRenderer 行内代码 `**` 误处理（P3）**——renderInline 中行内代码 `code` 先替换为 `<code>...</code>`，但随后的粗体正则仍会匹配 `<code>` 标签内的 `**`；占位符法让粗体/斜体/链接正则在纯净文本上执行。(9) **LearnerProgress 负数字段校验（P3）**——load() 中所有数值字段（attemptCount/levelStars/score/bestStars/spentMinutes/failCount）添加范围校验，与 recordScore/addSpentMinutes 等写入路径校验逻辑一致。关键决策：构造方法必须命名为 init / 防重复守卫用早返回+busy_ 标志而非 BlockQueuedConnection / 答对后禁用按钮而非依赖守卫 / controller 为空友好提示且不记录失败 / 跳转即重置搜索过滤 / 占位符法解决行内代码 `**` 误处理 / 负数校验与 recordXxx 截断逻辑一致。保留现状：BugHuntPanel e.what() 教学调试设计意图 / ProfileDashboardPanel errorMessage 诊断设计意图 / 三个面板 connect 缺 context 连接自身子控件风险极低 / GlossaryPanel relatedPanelFor 映射表完整 / BackendComparePanel source 未使用设计意图 / BreakpointConditionPanel modeText 逻辑正确 / VmStackSandboxPanel 步数上限已有多层保护 / VmStackSandboxPanel 页切换无 QTimer 且 VM 监听器已检查页索引。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide`+`minilang_tests` 构建通过，全量 1763/1763 测试通过（3 个 NestedLoop 并行 flaky 失败，单独运行全部通过） | [CHANGELOG](../CHANGELOG.md) |
| 第四十轮：教学模块第二轮深度审计（BoxedIntData 崩溃 + 教学作弊 + 状态机 + 异常防护 + 性能 + 动画冲突） | 继续对全部教学模块面板（40+ 面板文件）进行第二轮深度 Bug 审计，采用四模块并行 agent 策略，共发现约 40 个问题（P1 × 1，P2 × ~15，P3 × ~15）。本轮修复 15 项明确、低风险、真实可触发的问题（P1 × 1，P2 × 14）。(1) **VariableInspectorPanel BoxedIntData 崩溃（P1）**——`gui/VariableInspectorPanel.cpp` `valueToBitsHex` 对 BoxedIntData（type==VAL_INT 但 isPointer()==true，intVal() 返回超出 int48 范围的 int64）调用 `NaNBox::fromInt` 触发 `canEncodeInt` 失败 → `std::abort()` 崩溃；`case VAL_INT` 分支前判断 `v.isPointer()`，对 BoxedIntData 走堆指针占位符路径（与堆类型 default 分支一致）。(2) **TokenPuzzlePanel onSkipLevel 误发射 activityCompleted（P2）**——跳过关卡仍发射 `activityCompleted`，LearningPathPanel 误标记为已完成；移除 `emit activityCompleted(levelId(currentLevelIndex_))`，0 星持久化由 `markLevelStars(0)` 完成，下一关解锁由 `unlockNextLevel` 基于 `levelStars_` 完成。(3) **BugHuntPanel onTripleVerify 三后端空输出作弊（P2）**——学员删除所有 print 即可让 `outputConsistent` 平凡为 true 通关；增加 `hasOutput` 检查，要求至少一个后端有非空输出。(4) **BugHuntPanel onItemSelected 幂等保护失效（P2）**——`hintLevel_==0` 约束导致用户查看提示后再次点击同一题目，保护失效且 hintLevel_ 被重置为 0；移除 `hintLevel_==0` 约束，仅保留 `currentItemIndex_ == itemIndex` 早返回。(5) **AstBuilderToyPanel onVerifyWithRealParser 教学误导（P2）**——纯表达式包装为 `var __toy_tmp = <expr>;` 后真实 AST 根节点是 Block/VarDecl 而学员搭的是表达式树，未标注导致学员误以为错误；输出中明确标注包装行为。(6) **LabManualPanel exercisePassed_ 索引错位（P2）**——`exercisePassed_` 对所有练习类型（CHOICE + EXPECTED_OUTPUT）追加全局索引，但 `onSubmitChoiceExercise` 用 `choiceIndex`（仅 CHOICE 计数）索引导致错位；改用 `globalIdx` 替代 `choiceIndex`。(7) **LabManualPanel applyFolding inCodeBlock 状态泄漏（P2）**——折叠区内切换 `inCodeBlock` 会导致状态泄漏到折叠区外，未闭合代码块使 `inCodeBlock` 恒为 true，后续所有行被当作代码块跳过；折叠区内不切换 `inCodeBlock`。(8) **BytecodeTracePanel captureCurrentState 异常防护（P2）**——`v.toString()` 无 try/catch，与 CallStack/VariableInspector 不一致；对齐 try/catch 防护。(9) **DebugPanel updateVariables 异常防护（P2）**——`v.value.toString()` 无 try/catch，异常传播到 DebugCoordinator RCU 回调链；对齐 try/catch 防护。(10) **PipelineViewer populateSource O(n²) 性能（P2）**——`os.str().back()` 每次构造 string stream；用 `lastChar` 变量替代。(11) **PipelineViewer switchToStep 动画冲突（P2）**——同一 widget 多个 `QPropertyAnimation` 操作 `pos` 互相覆盖，`page->pos()` 在动画进行中返回中间值；同一步骤 early return + findChildren 停止已有 pos 动画。(12) **MemoryModelPanel nanBoxExamples 悬垂指针（P2）**——栈上 `int dummy` 作为指针示例，IIFE 返回后 dummy 出作用域，`b.bits` 持有悬垂栈地址；改用 `static int dummy`。(13) **app/ide.cpp WelcomeWizard 重复 UI 重建（P2）**——Step4 按钮 `emit learningPathRequested()` 同步触发 `showTeachingPanel`（第一次），`accept()` 后又无条件调用（第二次）导致 LearningPathPanel 被重建两次；移除 `learningPathRequested → showTeachingPanel` 连接，统一在 `exec()` 返回后调用一次。(14) **ProfileDashboardPanel paintEvent 硬编码坐标（P2）**——硬编码 `QRect(220, 200, width()-240, 200)`，splitter 拖动/窗口缩放时柱状图位置错位；改为基于 `resultTable_->geometry().bottom()` + widget 边界动态计算。(15) **VmStackSandboxPanel onTraceRunAll 重入守卫（P2）**——长循环期间 `processEvents` 让渡控制权，用户可点击 step/reset 触发重入；新增 `traceRunning_` 成员 + onTraceStep/onTraceRunAll/onTraceReset 三处守卫 + 禁用/恢复按钮。关键决策：BoxedIntData 走堆指针占位符而非扩展 NaN-box 编码 / 跳过≠完成移除信号而非加状态参数 / 三后端空输出退化解要求至少一端有输出 / exercisePassed_ 用全局索引而非选择题内序号 / 折叠区内不切换 inCodeBlock 状态而非重置 / PipelineViewer 用 lastChar 变量而非重新设计数据结构 / 动画冲突 findChildren 停止已有动画而非维护动画列表 / nanBoxExamples 用 static 延长生命周期而非改用堆分配 / WelcomeWizard 统一在 exec() 返回后展开 / ProfileDashboard 基于 table 几何动态计算 / VmStackSandboxPanel 用 traceRunning_ 守卫而非 QtConcurrent 架构改动。保留现状：SyntaxExplorerPanel onRunSample 同步阻塞（P1，需 QtConcurrent 架构改动）/ LabManualPanel onSubmitExpectedOutputExercise 同步阻塞（P2，同上）/ VmStackSandboxPanel onTraceRunAll 步数上限（P2）/ VmStackSandboxPanel 页切换按钮非互斥（P2，需 QButtonGroup 重构）/ BugHuntPanel onTripleVerify 空输出与 expectedOutput 匹配（P2，需结构化字段）/ TeachingTreePanel setCurrentPanel 搜索过滤下高亮失效（P2）/ GlossaryPanel relatedPanelFor 映射不完整（P3）/ MarkdownRenderer 行内代码内 `**` 误处理为粗体（P3）/ MarkdownRenderer 表格中间分隔行被当作数据行（P3）/ LearnerProgress load 不校验负数字段值（P3）/ BackendComparePanel runXxx source 参数未使用（P3）/ IRTransformPanel reloadCurrentIR 死代码（P3）/ BreakpointConditionPanel modeText VM STEP 间歇期显示"未运行"（P3）。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide`+`minilang_tests` 构建通过，全量 1763/1763 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十九轮：continue 方向性错误修正 + VM 面板数据竞争 + 闭包沙箱隔离 + Lexer 代理码点 + VmStepper hitCount | 继续对 MiniLang IDE 项目进行深度 Bug 排查，采用四模块并行 agent 审计策略。**最关键的发现**：第三十八轮的 continue 修复存在方向性错误——`continueTarget` 应在 `OP_CLOSE_UPVALUE` **之前**而非之后，导致 continue 仍然跳过 upvalue 关闭。本轮修正此错误并修复另外 8 项问题（P1 × 3，P2 × 6）。(1) **第三十八轮 continue 方向性错误修正（P1）**——`compiler/Compiler.cpp` visitWhileStmt + visitForStmt 的 `continueTarget`/`updateStart` 从 OP_CLOSE_UPVALUE 之后移到之前；`compiler/IR.cpp` visitWhileStmt + visitForStmt 的 `continueLabel` 从 `leaveBlockScope` 之后移到之前。栈式 VM 中跳转目标是"落地后从该点继续执行"，放在 OP_CLOSE_UPVALUE 之前使 continue 落到 OP_CLOSE_UPVALUE 上执行关闭后再 OP_LOOP。(2) **VariableInspectorPanel VM 模式数据竞争（P1）**——`gui/VariableInspectorPanel.cpp` `refreshLive` + `onVariableSelected` 的 VM 模式分支缺少 `isVmRunning()` 守卫，VM RUN 期间并发读取 `globalSlots_`/`globalNameToSlot_` 触发 UB；添加 `isVmRunning()` 守卫对齐 Interpreter 调试路径的 `isDebugPaused()` 模式。(3) **CallStackPanel VM 模式数据竞争（P1）**——`gui/CallStackPanel.cpp` `refreshLive` VM 模式分支缺少 `isVmRunning()` 守卫，VM RUN 期间并发遍历 `frames_` 触发迭代器失效；添加 `isVmRunning()` 守卫。(4) **deepCloneForSandbox 闭包浅拷贝（P2）**——`interpreter/Interpreter.cpp` 闭包直接 `return v` 浅拷贝，`writeBackCapturedVars` 通过 `const_cast` 绕过 COW 修改原始 capturedVars；改为 COW detach（非 const `capturedVars()` 触发 `ensureUnique`）+ 递归深拷贝容器值，body/closureEnv 共享安全。(5) **Lexer `\uXXXX` 代理码点（P2）**——`lexer/Lexer.cpp` `case 'u'` 接受 0xD800-0xDFFF 代理码点并编码为 3 字节 UTF-8 产生非标准 WTF-8；添加代理码点拒绝检查。(6) **VmStepper hitCount 过度递增（P2）**——`app/VmStepper.cpp` `checkBreakpointHit` 在返回 true 前递增 hitCount，C++ 短路求值导致过滤条件失败时 hitCount 已递增；改为纯查询，hitCount 递增移到 `runBatch` + `stepByMode` 过滤条件通过后。(7) **~DebugController 未调用 waitCallbacksIdle（P2）**——`debug/DebugController.cpp` 析构函数补 `waitCallbacksIdle()` 防御性等待锁外 callback。(8) **~DebugEvaluator 未调用 waitCallbackIdle（P2）**——`debug/DebugEvaluator.h` 析构函数从 `= default` 改为调用 `waitCallbackIdle()`。关键决策：continue 跳转目标在 upvalue 关闭之前 / VM 面板用 isVmRunning() 守卫而非加锁 / 闭包 COW detach 而非深拷贝整个 ClosureData / hitCount 拆分查询与计数职责 / Lexer 代理码点拒绝而非合并。验证"保留现状"项：Parser C 风格数组已修复 / Assignment 括号非 Bug / UOP_UNKNOWN 已修复 / nan/inf 已修复。保留现状：break/continue finally 不一致 / try-finally return / Formatter 插值注释 / break upvalue 关闭。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_tests` 构建通过，全量 1763/1763 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十八轮：三后端语义一致性深度审计（while continue + upvalue 关闭 + 自动构造 + 调试器断点重复触发 + 跨线程数据竞争） | 继续对 MiniLang IDE 项目进行深度 Bug 排查，采用四模块并行 agent 审计策略，重点排查三后端语义一致性、闭包/upvalue 生命周期、调试器状态机、跨线程数据竞争等方面。共发现约 35 个问题，本轮修复 7 项明确、低风险、真实可触发的问题（P1 × 5，P2 × 2）。(1) **while continue 跳过 OP_CLOSE_UPVALUE（P1）**——`compiler/Compiler.cpp` while 循环 continue 跳转目标在 OP_CLOSE_UPVALUE 之前，跳过 upvalue 关闭导致闭包捕获变 by-reference（三后端不一致）；新增 `continueTarget` 标记放在 OP_CLOSE_UPVALUE 之后，continue 跳转回填改为跳到 continueTarget；`compiler/IR.cpp` 同步新增 `continueLabel` 放在 `leaveBlockScope` 之后，对齐 for 循环的 continue 跳转目标（updateStart）。**注：第三十九轮发现此修复方向性错误，已修正——continueTarget 应在 OP_CLOSE_UPVALUE 之前。** (2) **visitVarDecl 自动构造缺失默认参数绑定（P1）**——`interpreter/Interpreter.cpp` `var f: Foo` 无初始化表达式但类型注解为类名时自动构造实例，原实现缺失 init 默认参数绑定，init 体引用参数时穿透到父环境；补充 init 默认参数绑定循环（对齐 `constructClassInstance`），包含 EnvGuard 临时切换到 closureEnv 求值默认值 + checkType 类型注解校验。(3) **visitVarDecl 自动构造缺失 closeCapturedVariables（P1）**——同上自动构造路径缺失 `closeCapturedVariables` 调用，闭包捕获变量丢失；`PrevEnvGuard` 改为 `InitEnvGuard`（析构时调用 `closeCapturedVariables`，对齐 `constructClassInstance`）。(4) **DebugController checkBreak updateLineTracking 顺序错误（P1）**——`debug/DebugController.cpp` `updateLineTracking` 在 `shouldPause` 之后执行，`shouldPauseAtBreakpoint` 命中时设 `crossedLine_=false` 被随后的 `updateLineTracking` 覆盖为 true，导致 resume 后同行下一个 AST 子表达式断点重复触发；将 `updateLineTracking` 移到 `shouldPause` 之前。(5) **DebugCoordinator callStackCallback 数据竞争（P1）**——`app/DebugCoordinator.cpp` `getCallStack()` 返回 const 引用，跨线程遍历期间 worker push_back/pop_back 导致迭代器失效；`interpreter/Interpreter.{h,cpp}` 新增 `getCallStackSnapshot()` 返回值拷贝，改用此方法。(6) **callInstanceMethod 默认参数无 RAII（P2）**——`interpreter/Interpreter.cpp` 默认参数路径手动 save/restore `currentEnv_`，evaluate 抛异常时残留为 cachedParentEnv；改用 RAII guard（对齐 EnvGuard 模式）。(7) **resume/step 清除 stopped_（P2）**——`debug/DebugController.cpp` resume/stepIn/stepOver/stepOut 清除 `stopped_` 可在 worker 响应 stop 的窗口内取消正在进行的 stop；4 方法添加 `if (stopped_) return;` 守卫，移除 `stopped_ = false;`。关键决策：continue 跳转目标对齐 for 循环 / 自动构造路径对齐 constructClassInstance / updateLineTracking 顺序修复而非标志位重置 / getCallStackSnapshot 值拷贝而非加锁 / stopped_ 粘性标志。保留现状：try-finally return 语义 / deepCloneForSandbox 浅拷贝 / Formatter 往返等价 / Parser C 风格数组 / VmStepper hitCount 时机等。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_tests` 构建通过，全量 1753/1753 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十七轮：教学模块系统审计与修复（GuidedTour UAF + listener 反注册 + Markdown 腐蚀 + autoTimer 数据竞争 + P2 视觉/性能） | 对全部教学模块面板（40+ 面板文件）进行系统性 Bug 审计，采用四模块并行 agent 策略，共发现约 45 个问题（P0 × 2，P1 × 10，P2 × ~30，P3 × 1）。本轮修复 P0 全部 + P1 关键项 + P2 主要项共 17 项。(1) **GuidedTour bubble_ UAF（P0）**——`gui/GuidedTour.{h,cpp}` `QWidget* bubble_` → `QPointer<QWidget>`，hideOverlay 中 `deleteLater()`+置 null 替代 `hide()`，`app/ide.cpp` onPanelGuidedTourRequested 连接 `finished→deleteLater`；根因 bubble_ 裸指针在 IDE 析构期 UAF，且多次 start/hideOverlay 累积泄漏（hide 不 delete）。(2) **IdeController listener 无反注册（P0）**——`app/IdeController.h` `addVmStateChangedListener` 增加 `void* owner` 参数 + `removeVmStateChangedListener(owner)` 延迟清除（标记 `cb.fn=nullptr` 而非 erase 避免迭代器失效），`VmStateChangedListener` 改为 `{void* owner; VmStateChangedCallback fn}` 结构；6 面板（BytecodeTrace/CallStack/VariableInspector/BreakpointCondition/MemoryModel/VmStackSandbox）`.h` 新增 `~Panel() override` + `.cpp` 析构函数调用 `controller_->removeVmStateChangedListener(this)`；根因 setController 注册捕获裸 this 的 lambda，面板析构后 controller 仍存活期间任何 notifyVmStateChanged 调用 UAF。(3) **MarkdownRenderer 关键字腐蚀（P1）**——`gui/MarkdownRenderer.cpp` 占位符法重写 highlightMiniLang：先用 `\x01{idx}\x02` 控制字符占位替代直接 replace，让所有正则匹配在无 span 标签的纯净文本上执行，最后统一替换占位符为 span；预编译关键字正则数组；根因关键字 "class" 匹配到已生成 `<span class="...">` 中的 class 属性导致 HTML 腐蚀。(4) **LabManualPanel choiceIdx 索引越界（P1）**——`gui/LabManualPanel.cpp` `choiceGroups_` 按 CHOICE 出现顺序追加，`onSubmitChoiceExercise` 参数语义改为选择题内序号，feedbackLabel objectName 改为 `feedback_choice_{idx}`；根因全局 exerciseIdx vs 选择题内序号混用。(5) **MemoryModelPanel 定时器降频失效（P1）**——showEvent 中 `start(500)` 覆盖 `setInterval(2000)` → `start()`。(6) **LearnerProgress load 校验（P1）**——`s <= 4` → `s <= LearningPathData::stageCount()` 接受 currentStage=5 全通关状态。(7) **CallStack/VariableInspector autoTimer 数据竞争（P1）**——`app/DebugCoordinator.h` 新增 `isPaused()` 转发 + `app/IdeController.h` `isDebugPaused()`；面板 refreshLive 增加 `isDebugPaused()` 守卫，仅暂停状态才调用快照接口；交换 isVmInitialized 优先级；toString/typeName 包裹 try/catch；VariableInspector onVariableSelected 恒真条件改用 `found` 标志；根因调试 resume 期间 autoTimer 触发 getDebugCallStack/getDebugVariableSnapshot 与 worker 线程并发访问解释器内部 unordered_map/vector 导致 UB。(8-15) **P2 批次**——LearningPathPanel rgba 数字参数 + markActivityCompleted 幂等 / GlossaryPanel anchorClicked 发射 termActivated / TeachingTreePanel 搜索清空恢复展开 / BackendComparePanel 三后端 processEvents / DebugPanel currentStack_ 截断 200 / VmStackSandboxPanel RegisterVM 标题+空状态+onTraceRunAll processEvents。关键决策：QPointer 替代 shared_ptr 适合 Qt 对象树 / listener 延迟清除避免迭代器失效 / 占位符法让正则在纯净文本执行 / isDebugPaused 链式转发保持 Facade 封装 / autoTimer 用 isDebugPaused 守卫而非停止定时器。保留现状：BugHuntPanel 空输出一致性 / AstBuilderToyPanel VarDecl 包装 / SyntaxExplorerPanel 同步阻塞 / ProfileDashboard 硬编码坐标 / MemoryModelPanel nanBoxExamples 悬垂指针 / LabManualPanel applyFolding 状态不一致等。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide`+`minilang_tests` 构建通过，全量 1763/1763 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十六轮：IDE UI/UX 七项优化（折叠按钮 + 输出面板 + 背景色 + 运行修复 + 切换动画） | 完成 7 项 IDE 界面与交互优化。(1) **折叠按钮+错误下划线**——`gui/CodeEditor.cpp` 折叠标记从方框+/-改为 VSCode 风格雪佛龙箭头（▷/▽）+ 抗锯齿 + 主题感知配色；错误下划线从 Qt::red 改为 Solarized 红 #DC322F + 淡红背景。(2) **closure_pattern 报错**——`samples/mini/closure_patterns.mini` + `higher_order.mini` 匿名函数表达式 `fun(){...}` 改为命名内部函数声明 + 引用返回（MiniLang 不支持 FunExpr）。(3) **输出面板 VSCode 风格**——`app/ide.cpp` `appendOutput` 从 Unicode 图标（▶ℹ✓⚠✗）改为文本级别前缀 `[Info]/[Done]/[Warn]/[Error]`，Plain 级无前缀无时间戳；outputTextEdit_ 调色板设 Solarized base3 背景。(4) **术语表跳转修复**——`gui/GlossaryPanel.cpp` 移除 `onTermSelected` 的 `emit termActivated`，仅保留双击/链接触发。(5) **教学切换动画**——`app/ide.cpp` `showTeachingPanel` 重构：dock 显隐移到 setCurrentIndex 之前、refresh 移到之后、slideInWidget 220ms→150ms。(6) **三面板运行修复**——`loadCodeIntoMainEditor` 补 `switchToTab(newIdx)` 解决 codeEditor_ nullptr；`restoreLayout` 默认高度 600→220 + 历史>500 迁移解决输出面板"全屏覆盖"。(7) **背景色覆盖**——`applyFluentStyle` 补全覆盖 centerStack/centerSplitter/welcomePage/replPanel/errorPageContainer/fileTreeContainer；`CodeEditor` 浅色 Base #ffffff→#FDF6E3。修复阻断构建的 `DebugController::waitCallbacksIdle` private→public（MSVC 将 access specifier 编入 mangled name）。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1763/1763 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十五轮：后续 28 项待处理问题修复（跨线程 UAF + GC roots + 一致性 + Lexer Unicode 转义 + LRU 缓存） | 继续处理第三十二轮系统审计标记的 28 项后续待处理问题，本轮修复其中 9 项明确、可独立修复的问题（P1 × 4，P2 × 5）。(1) **ReplPanel 异步 lambda 裸 self UAF（P1）**——`gui/ReplPanel.{h,cpp}` `QPointer<ReplPanel>` 替代裸指针 + `qApp` 作为 invokeMethod context + lambda 内 null 检查；根因 `QMetaObject::invokeMethod(self, lambda, Qt::QueuedConnection)` 投递的 lambda 在接收对象事件队列中，`~ReplPanel` 的 `wait()` 不处理事件队列，析构后回到事件循环才 dispatch → UAF。(2) **currentEnvironment 跨线程裸指针 UAF（P1）**——`interpreter/Interpreter.{h,cpp}` 新增 `currentEnvironmentShared()` 返回 shared_ptr 副本，`app/DebugCoordinator.cpp` variableCallback 改用此方法并链式持有 parent shared_ptr（`currentShared = currentShared->parent`），确保整条 parent 链不被析构。(3) **DebugCoordinator 析构竞态（P1）**——`debug/DebugController.{h,cpp}` + `debug/DebugEvaluator.h` 新增 `activeCallbackCount_` 原子计数 + `waitCallbacksIdle()` spin-wait（RCU 优雅期模式），cb() 期间 fetch_add（RAII guard）完成后 fetch_sub，析构清空 callback 后 spin-wait 直到计数归零；根因 copy-then-call-outside-lock 模式无法被析构清空操作等待。(4) **GC roots 不完整（P1）**——`interpreter/Interpreter.cpp` execute() GC roots 追加遍历 `savedClassRegistry.fields` + `closureEnv` + `savedModuleCache`，每类暂存状态都可能持有堆对象引用；根因原实现仅遍历 savedGlobalEnv，循环引用类字段/模块变量被误回收。(5) **closeUpvaluesFrom 不一致（P2）**——`compiler/RegisterVM.cpp` slot 越界补 Warning 对齐 StackVM 诊断口径。(6) **pathHash 跨平台（P2）**——`ast/ModuleIsolation.cpp` normalize 逻辑（`\`→`/` + 去 `./` 前缀）下沉到 pathHash 内部，三处调用者重复 normalize 保留（幂等操作）。(7) **Parser 尾逗号不一致（P2）**——`parser/Parser.cpp` 数组/字典/导入名称列表 3 处统一为模式 B（`while(match(COMMA) && !check(CLOSE))`），比模式 A（`if(check(CLOSE)) break` 在 do 体内）更内聚不易误删。(8) **Lexer Unicode 转义缺失（P2）**——`lexer/Lexer.cpp` 转义 switch 新增 `case 'u'`（4 位十六进制 Unicode→UTF-8 编码，支持 1/2/3 字节）和 `case 'x'`（2 位十六进制→字节）；新增 `String_HexAndUnicodeEscapes` 回归测试 7 用例。(9) **condAstCache 伪 LRU（P2）**——`app/IdeController.cpp` 改为 `list + unordered_map<key, list::iterator>` 经典 LRU，命中 splice move_to_front，超 32 条上限 pop_back；根因 `unordered_map::erase(begin())` 迭代顺序由哈希函数决定，与插入/访问顺序无关。关键决策：ReplPanel 用 QPointer+qApp 而非 QSharedPointer（QWidget 不能用 shared_ptr）/ currentEnvironmentShared() 而非修改返回类型（向后兼容）/ RCU 优雅期比 BlockingQueuedConnection 更轻量 / GC roots 追加 closureEnv / pathHash normalize 下沉更彻底 / Parser 尾逗号模式 B 更内聚 / list+map 经典 LRU。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_tests` 构建通过，全量 1763/1763 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十四轮：7 项教学面板与 IDE 体验问题修复（AST 构建器 + BugHunt 崩溃 + 性能仪表盘 + 代码旅程 + 学习中心返回） | 针对用户反馈的 7 项教学面板与 IDE 体验问题做集中修复。(1) **AST 构建器叶子节点错误嵌套**——`gui/AstBuilderToyPanel.cpp` `addNodeWithLabel` 根据 label 前缀判断是否为叶子节点（Number:/Identifier:），叶子节点添加后保持父节点选中（`tree_->setCurrentItem(current)`），非叶子节点保持原行为（切换到新子节点）；根因原实现无论添加什么节点都 `setCurrentItem(child)` 导致连续添加叶子时焦点累积下沉形成嵌套。(2) **BugHunt 模式崩溃（P0 栈溢出）**——`gui/BugHuntPanel.cpp` `refreshBugChips` 中 `isVisible()` 判定改为难度筛选条件判定（与 firstVisible 口径一致），避免构造期间父 widget 未 show() 时 isVisible() 恒返回 false 导致 refreshBugChips → onItemSelected → refreshBugChips 无限递归；`onItemSelected` 添加幂等保护（相同索引且 hintLevel_==0 时直接 return）。(3) **性能仪表盘类实例化三后端失败**——`gui/ProfileDashboardPanel.cpp` 源码 `fun new(px, py)` 改为 `fun init(px, py)`，MiniLang 三后端均查找名为 "init" 的构造方法，"new" 会被识别为普通方法导致实例化后字段未初始化。(4) **性能仪表盘数据量过大运算慢**——fib-recursion 从 fib(20) 降为 fib(15)；dict-access 从 10000 次降为 2000 次；title 和 description 同步更新。(5) **性能仪表盘左下角留白**——移除 `scenarioDesc_->setMaximumWidth(220)`；左栏布局改为 `addWidget(scenarioList_, 0)` + `addWidget(scenarioDesc_, 1)`，scenarioDesc_ 拉伸填满剩余空间。(6) **代码生命旅程描述与完成条件**——`gui/CodeJourneyInfoPanel.{h,cpp}` 移除 6 阶段访问跟踪（visitedStages_/kTotalStages/markStageVisited），新增 `journeyCompleted_` bool + `markCompleted()` + `showEvent` override，首次显示即标记完成并 emit journeyCompleted；`gui/LearningPathData.cpp` 标题从"🚀 代码生命旅程动画"改为"🚀 代码生命旅程"，描述从"一段 30 秒小动画..."改为"一张静态信息图...点进去看即算完成"；`app/ide.cpp` 连接 journeyCompleted → markActivityCompleted("code-journey")。(7) **学习中心返回编辑器**——`gui/TeachingPanelHeader.{h,cpp}` 新增「← 返回编辑器」按钮（backBtn_，objectName="teachingBackBtn"）+ `returnToEditorRequested` 信号；`app/ide.cpp` `wrapTeachingPanel` 中连接 returnToEditorRequested → showEditorArea；QSS 浅色背景（#FDF6E3）+ Solarized 风格 hover（#EEE8D5 + #268BD2 边框）。关键决策：AST 叶子节点焦点保持而非切换 / isVisible() 构造期间不可用改用业务条件判定 / MiniLang 构造方法必须命名为 init / 性能数据量平衡教学价值与响应性 / 完成条件匹配用户预期（点进去观看即完成）/ 返回入口在视线焦点处（TeachingPanelHeader）。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_tests` 构建通过，全量 1762/1762 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十三轮：IDE 三栏布局与教学面板 UI 6 项优化（面板切换动画 + 输出窗口全宽 + 欢迎页编辑器展开 + BugHunt 代码加载动画 + BugHunt 芯片栏 + LabManual 芯片栏） | 针对用户反馈的 6 项 IDE 三栏布局与教学面板 UI 问题做集中修复。(1) **面板切换不流畅且逻辑有误**——`app/ide.cpp` `onActivityChangedById` 中 `fadeInWidget` → `slideInWidget`（O(1) pos 动画替代 O(n) QGraphicsOpacityEffect）。(2) **输出窗口仅覆盖代码编辑区且把教学内容顶掉**——确认 `bottomContainer_` 已在 `mainLayout` 垂直布局（全宽不挤压教学）；`showBottomPanel`/`hideBottomPanel` 新增 `QPropertyAnimation` 驱动 `maximumHeight` 平滑展开/收起；`fadeInWidget` → `slideInWidget`。(3) **欢迎界面打开文件后代码编辑区未完全展开**——`ensureEditorVisible` 移除 `centerStack_->isHidden()` 前置条件，改为 `editorTabWidget_` wasHidden 即触发动画；动画结束后延迟 `centerStack_->hide()`。(4) **BugHunt 加载代码到编译器交互流程不佳**——`loadCodeIntoMainEditor` 教学模式下不调用 `ensureEditorVisible`（保留教学面板），改为 45%/55% 分栏动画；`onEditorTabCloseRequested` 教学模式下关闭最后一个标签时教学区平滑延展恢复（不回欢迎页）。(5) **BugHunt 目录区域占比过大，功能区域重叠**——`gui/BugHuntPanel.{h,cpp}` 4 栏 splitter（列表|变体|描述|代码）→ 芯片栏 + 2 栏 splitter（描述|代码）；15 个 Bug 芯片 + 7 个变体芯片水平排列，Solarized QSS 三态（默认/当前/难度色边框）；搜索框移至芯片栏右侧。(6) **实验手册内容区域拥挤，目录占比过大**——`gui/LabManualPanel.{h,cpp}` 3 栏 splitter（章节|内容|代码）→ 章节芯片栏 + 2 栏 splitter（内容|代码）；8 个章节芯片水平排列，Solarized QSS 二态（默认/当前）；释放 130px 目录宽度给内容阅读区。关键决策：slideInWidget 全面替代 fadeInWidget / bottomContainer_ 平滑高度动画用 maximumHeight 而非 fixedHeight / BugHunt 芯片栏复用 TokenPuzzlePanel 关卡芯片模式 / LabManual 章节芯片编号而非标题 / ensureEditorVisible 动画触发条件用 wasHidden 而非 centerStack_->isHidden()。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_tests` 构建通过，全量 1762/1762 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十二轮：全项目性能与 Bug 系统审计（四模块并行审计 + 14 项修复） | 对 MiniLang IDE 项目进行系统性性能检测与 Bug 排查，采用四模块并行 agent 审计策略（VM/IR 后端、Interpreter/Value、Parser/Lexer/Formatter、Debugger/GUI 线程安全），共发现 42 个问题（P1 × 17，P2 × 25）。本轮修复 14 项明确、低风险、真实可触发的问题（P1 × 8，P2 × 6）。(1) **VM 栈平衡**——`compiler/VMCalls.cpp` `executeMethodCall` 默认参数 3 处错误返回 + `executeCall` 类构造 `extraSlots<0` 错误返回，均未 popN 清理栈上 this+fields+args，与 BUG-VM-02 fix 普通路径不一致；补 `popN(1+fieldCount+argCount)`。(2) **IR 完整性**——`compiler/IR.cpp` `visitBinaryOp` default 分支仅 `assert(false)+Logger::Error`，Release 下不阻断控制流，悬空 vreg 返回污染 IRModule；新增 `hasError_=true` 让调用方中止。`TYPE_CHECK` lowering 不更新 `vregStackDepth_`，与 DUP/LOAD_MUTATED 不一致；补 `vregStackDepth_[src_vreg]=code.size()`。(3) **RegVM 快速失败**——`compiler/RegisterBytecodeBackend.cpp` `vregToReg` 溢出返回占位 reg=0 后 `lowerInstruction` 继续 case 体写入损坏字节码；入口加 `if (hasError_) return false`。(4) **Interpreter COW 语义**——`interpreter/InterpreterCalls.cpp` `writeBackCapturedVars` 用非 const `capturedVars()` 触发 COW，修改写到副本而非原件（与 `Environment::closeCapturedVariables` B1 fix 同一不变量）；应用 `const_cast<const Value&>` 绕过。`constructClassInstance` init 参数绑定缺 `checkType`（与 `callClosureValue` P1-4 fix 不一致，三后端语义分歧）；补 `checkType` 对齐。(5) **super 调用防御**——`interpreter/Interpreter.cpp` super 调用 `currentEnv_->set("this", std::move(obj))` 未检查返回值（`set(&&)` 契约要求 caller 检查）；补检查 + runtimeError。(6) **模块隔离作用域**——`ast/ModuleIsolation.cpp` finally 块未 `pushScope`（原注释"无 catch 变量"是误解），finally 内 VarDecl 污染外层作用域导致 `resolveToModuleTopLevel` 误判；与 catch 块对齐加 push/popScope。(7) **调试器停止语义**——`debug/DebugController.cpp` `stop()` 设 `running_=false` 使 `checkBreak` 行 33 静默 return 而非 throw `DebugStopException`（running_ 双重语义"已停止"+"未启动"）；移除，由 `reset()`/`prepareRun` 负责。(8) **Worker UAF**——`app/WorkerManager.cpp` `~WorkerManager` 析构和 `forceStop` 成功路径未 `disconnect(this)`（`stopForClose` BUG-IDE-15 fix 已 disconnect），`QThread::finished` 在 wait 返回前发射，QueuedConnection lambda 持 this 投递主线程队列，reset 后 UAF；三路径统一 disconnect。(9) **Lexer 列号一致性**——`lexer/Lexer.cpp` 插值片段闭合后 `startCol` 用字节偏移 `start_-lineStart_` 而非 `columnAt()`，多字节 UTF-8 字符导致列号偏移；改用 `columnAt(start_)`。(10) **Formatter 注释排序**——`formatter/Formatter.cpp` `std::sort` 非稳定排序，同行多注释顺序未定义；改 `std::stable_sort` + 列号次要键。(11) **Parser 边界检查**——`parser/Parser.cpp` `peek()` 直接索引无边界检查（`previous()` P0-14 fix 已有），依赖 TK_EOF 哨兵；补对称边界检查。关键决策：四模块并行审计 / P1 优先修复真实可触发问题 / writeBackCapturedVars 复用 closeCapturedVariables const_cast 模式 / stop() 不重置 running_ 消除双重语义 / WorkerManager 三路径 disconnect 对齐 / finally 块作用域与 catch 一致。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_tests` 构建通过，全量 1125/1125 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十一轮：教学面板 11 项问题排查与修复（Glossary/SyntaxExplorer 自动加载 + GuidedTour 跨页定位 + BugHunt 调试闭环 + ProfileDashboard 状态增强 + fadeInWidget 全局清理） | 针对用户反馈的教学面板 11 项问题做集中排查与修复。(1) **问题1 术语表切换异常**——`gui/GlossaryPanel.cpp` 修复切换逻辑。(2) **问题2 语法浏览器首次进入内容未加载**——`gui/SyntaxExplorerPanel.cpp` 实现首次进入自动加载。(3) **问题3 变量检测器/调用栈检查器/条件断点指引框定位混乱且页面空白**——`gui/{VariableInspector,CallStack,BreakpointCondition}Panel.cpp` 重写 createGuidedTour：只高亮始终可见的页切换按钮（pageLiveBtn_/pageLibraryBtn_），概念步骤用 nullptr（居中气泡）+ 内嵌完整示例代码（`<pre>` 块）；根因 GuidedTour `bubble_` 定位依赖 `target->mapTo(host_, QPoint(0,0))`，target 在 QStackedWidget 不可见页时返回错误坐标。(4) **问题4 Bug 狩猎模式缺乏有效交互**——`gui/BugHuntPanel.{h,cpp}` 增加「预测→观察→修复→验证」四步调试闭环：`debugStepsLabel_` 检查清单（✅/⬜）+ `predictionEdit_` 让用户先写下预期 + `onCodeModified()` 检测代码修改标记修复步 + `onTripleVerify()` 标记验证步；`showCurrentItem/showCurrentVariant` 重置调试状态；`refreshDebugSteps()` 显式呈现四步进度。(5) **问题5 异常流与 IR 优化回放切换失效**——`gui/IRTransformPanel.cpp` 移除回放页 3 处 fadeInWidget 调用（QGraphicsOpacityEffect 连续切换时 opacity 卡 0 内容空白）。(6) **问题6 性能仪表盘"运行中"状态不醒目**——`gui/ProfileDashboardPanel.{h,cpp}` 新增 `startStatusAnimation/stopStatusAnimation`：QTimer 400ms 循环圆点动画"运行中."→"运行中.."→"运行中..."，橙色背景（#F5A623）+ 白字 + bold，进度指示 [1/3] [2/3] [3/3]。(7) **问题7 性能仪表盘数据量大导致卡死+仅 interpret 后端可运行**——`runProfile()` 三后端串行执行时每个后端之间调用 `QApplication::processEvents()` 让 UI 重绘避免"未响应"；`renderResults()` 失败行显示错误原因（红色文字+tooltip）；`buildAnalysis()` 列出失败后端及错误信息；降低重场景迭代次数 fib-recursion/loop-sum 5→3。(8) **问题8 实验手册"本章练习"模块位置不当**——`gui/LabManualPanel.cpp` 练习区从中间列迁移至右侧样例代码下方形成"代码-练习"垂直对照布局，中间列改为全高 contentBrowser_。(9) **问题9 各模块切换卡顿**——`gui/{IRTransformPanel,VmStackSandboxPanel}.cpp` 移除剩余 fadeInWidget 调用（QListWidget/QTextBrowser 内容刷新无需动画）。(10) **问题10 类型教学库未实现自动加载**——修复为进入即加载。(11) **问题11 代码生命旅程动画缺少完成判断标准**——`gui/CodeJourneyInfoPanel.{h,cpp}` 新增 6 阶段访问跟踪：`QHash<QString,bool> visitedStages_` + `progressLabel_` 显示 ✅/⬜ 进度 + `markStageVisited()` 全部访问后变绿色"🎉 旅程完成！"+ `journeyCompleted` 信号；6 个 jump handler 调用 markStageVisited。关键决策：GuidedTour 跨页定位用 nullptr 居中气泡替代高亮不可见控件 / BugHunt 四步调试闭环而非自由交互 / 状态动画用 QTimer 而非 QPropertyAnimation / 三后端兼容性用 processEvents 而非多线程 / fadeInWidget 全局清理而非局部修复 / LabManual 练习区迁移至样例代码下方 / CodeJourney 完成判断用 6 阶段访问跟踪。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1762/1762 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第三十轮：5 教学面板新手指引（GuidedTour 集成）+ 教学入口库视觉显著性提升（TeachingTreePanel） | 完成两项子任务：(A) 为 5 个教学面板（BytecodeTrace / CallStack / VariableInspector / BreakpointCondition / BugHunt）接入新手指引 GuidedTour；(B) 提升 TeachingTreePanel 视觉显著性。(1) **5 面板 createGuidedTour 方法**——`gui/{BytecodeTrace,CallStack,VariableInspector,BreakpointCondition,BugHunt}Panel.{h,cpp}` 每个面板 `.h` 前向声明 `class GuidedTour;` + 公有方法 `GuidedTour* createGuidedTour(QWidget* host)`；`.cpp` 新增 `#include "gui/GuidedTour.h"` + 实现 `new GuidedTour(host, host)` + 5 步 `addStep(target, title, desc)`，目标取面板关键 widget（子页切换按钮/自动刷新复选框/主数据视图/教学库按钮/加载样例按钮）；BreakpointConditionPanel 第 5 步 target=nullptr 居中收尾；文案用 `QString::fromUtf8` 构造中文，不硬编码条目计数。(2) **TeachingPanelHeader 新增「新手引导」按钮**——`gui/TeachingPanelHeader.{h,cpp}` 新增信号 `guidedTourRequested(panelId)` + `tourBtn_` 成员；构造函数 helpBtn_ 与 learningPathBtn_ 间新增 tourBtn_（objectName="teachingTourBtn"），QSS 主题色 #268BD2 填充白字+600 字重区别于 helpBtn。(3) **Ide 连接信号 + 首次访问自动触发**——`app/ide.{h,cpp}` 新增 `onPanelGuidedTourRequested` 槽（先 ensureTeachingPanelCreated 懒加载，再分派 5 面板 createGuidedTour(this)->start()）；`showTeachingPanel` 末尾 `kAutoTourPanels`(5 ID) + QSettings `guidedTour/shown_<panelId>` 标记 + `QTimer::singleShot(0)` 延迟一帧启动（确保几何就绪后高亮定位准确）。(4) **TeachingTreePanel 视觉提升**——`gui/TeachingTreePanel.{h,cpp}` B1 顶部渐变标题横幅（🎓 学习中心，#268BD2→#1E6FA3 白字 padding 12px，emoji 用 UTF-8 字节序列 `\xF0\x9F\x8E\x93`）；B2 树 QSS（#FDF6E3 背景+item padding 6px 4px+4px 圆角+hover rgba(38,139,210,0.08)+选中 #268BD2 白字）；B3 搜索框（teachingSearchEdit + onSearchChanged 槽按包含匹配不区分大小写过滤叶子，无匹配隐藏整个分类）；B4 buildTree 展开前两个分类（入门导览+编译前端）替代仅展开第一个；B5 叶子节点 setToolTip「点击进入 <label>」。关键决策：createGuidedTour 返回 new 对象生命周期由 host 子 widget 关系托管 / QSettings+singleShot 避免重复弹出且几何就绪 / 文案不硬编码计数遵循 P2-C 原则 / tourBtn 主题色强调区别 helpBtn / 搜索隐藏无匹配分类避免空残留 / 展开前两分类平衡可见性与噪声 / 不修改 GuidedTour.h/.cpp 与 IdeController。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide` 构建通过 | [CHANGELOG](../CHANGELOG.md) |
| 第二十九轮：学习路径地图章节切换卡顿 + 偶发崩溃修复（QGraphicsOpacityEffect 移除 + 轻量滑入动画） | 针对用户反馈的学习路径地图两个稳定性问题做集中修复：(1) 点击章节时页面切换卡顿、无法顺畅切换；(2) 窗口偶发性崩溃。根因定位为 `LearningPathPanel::refresh()` 末尾调用的 `PanelAnimator::fadeInWidget(scrollContent_)` —— 该函数对含 100+ 子 widget 的 `scrollContent_` 施加 `QGraphicsOpacityEffect`，每帧 O(n) 离屏 pixmap 合成导致卡顿；`deleteLater()` 延迟删除旧 widget 时 effect 的 pixmap 缓存仍引用已销毁子 widget → QPainter use-after-free 崩溃。叠加 6 处冗余双重 `refresh()` 调用与搜索框逐字符触发重建，放大了动画堆积与崩溃概率。(1) **移除 fadeInWidget**——`gui/LearningPathPanel.cpp` `refresh()` 末尾移除 `PanelAnimator::fadeInWidget(scrollContent_)` 调用，添加详细注释说明崩溃机制；移除 `#include "gui/PanelAnimator.h"`，新增 `#include <QTimer>`。(2) **搜索框 250ms 防抖**——构造函数中搜索框 `textChanged` 信号改为 `QTimer` 防抖定时器（`searchDebounceTimer_`），避免逐字符触发 refresh 重建；`gui/LearningPathPanel.h` 新增 `QTimer* searchDebounceTimer_` 成员。(3) **fadeInWidget 安全加固**——`gui/PanelAnimator.h` `fadeInWidget` 调用前先 `findChildren<QPropertyAnimation*>()` 查找并停止该 widget 上正在运行的 opacity 动画，防止多动画堆积。(4) **新增 slideInWidget 轻量滑入动画**——`gui/PanelAnimator.h` 新增 `slideInWidget(QWidget*, int)`：通过 QPropertyAnimation 驱动 widget 的 `pos` 属性（从右侧 24px 偏移滑入到目标位置），不使用 QGraphicsOpacityEffect，无离屏 pixmap 合成（O(1) vs O(n)），不受子 widget 数量影响，动画结束后确保位置精确归位。(5) **showTeachingPanel 添加滑入动画**——`app/ide.cpp` `showTeachingPanel` 在 `centerStack_->setCurrentIndex(idx)` 后调用 `PanelAnimator::slideInWidget(newPanel)` 添加轻量过渡动画。(6) **清除 6 处冗余 refresh**——`app/ide.cpp` 清除 6 处 `showTeachingPanel("learning-path")` 后的冗余 `learningPathPanel_->refresh()` 调用（showTeachingPanel 内部已处理）：首次启动 WelcomeWizard 完成回调 2 处 / 「再次显示欢迎向导」菜单项回调 / `onActivityRequested` 的 welcome 分支回调 / `wrapTeachingPanel` 的 header `learningPathRequested` 回调 / `GuidedTour::finished` 回调；`onActivityRequested` 的 canonical 分支移除 `learning-path` 的冗余 refresh。关键决策：`slideInWidget` 而非修复 `fadeInWidget`（O(n) 离屏合成无法通过加固动画停止解决）/ 保留 `deleteLater()` 而非改用 `delete`（refresh 可能从 row->clicked 信号槽调用链中触发，立即删除信号发送者会 use-after-free）/ 搜索框 250ms 防抖（体感即时与性能平衡）/ `fadeInWidget` 保留但加固（仍被 bottomStack_/rightStack_/guidedTour 3 处使用且子树小）。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide` 构建通过，全量 1762/1762 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第二十八轮：TeachingPanelHeader 帮助窗口尺寸统一调大 + 自适应内容高度 + Solarized QSS 美化 | 针对教学面板「这是什么？」帮助信息窗口（`TeachingPanelHeader::showHelpDialog`）做可读性与视觉一致性升级。原实现正常情况仅设 `setMinimumSize(480, 360)`、兜底 `setMinimumSize(400, 200)`，无最大尺寸约束、无自适应内容高度，长文案需手动拖拽；兜底用 `BodyLabel` 不支持滚动。升级为「统一调大尺寸 + 自适应高度 + Solarized 亮色 QSS + 兜底也用 QTextBrowser」。(1) **统一调大窗口尺寸**——正常路径 `setMinimumSize(640, 480)` + `setMaximumSize(900, 700)` + 初始 `resize(680, 520)`；兜底路径 `setMinimumSize(500, 300)` + `setMaximumSize(800, 600)` + `resize(540, 340)`。(2) **自适应内容高度**——新增 `adjustHelpDialogSize(QDialog*, QTextBrowser*)` 私有方法：`QTextDocument::setTextWidth(viewportWidth)` 后读 `size().height()` 获取文档实际高度，`desiredHeight = docHeight + 80`，`qBound(minHeight, desiredHeight, maxHeight)` 钳制到 `[minimum, maximum]`；`layout()->activate()` 强制布局计算；viewport 宽度兜底推算 `dlg->width() - 40 - 24 - 2`，最终兜底 600px。(3) **兜底改用 QTextBrowser**——原 BodyLabel 不支持滚动，改为 QTextBrowser + `setVerticalScrollBarPolicy(Qt::ScrollBarAsNeeded)`，与正常路径一致。(4) **Solarized 亮色 QSS**——`QDialog#helpDialog` 背景 #FDF6E3；`QTextBrowser#helpBrowser` 背景 #FDF6E3 + 文字 #002B36 + #93A1A1 边框 + 6px 圆角 + 12px padding；「知道了」按钮保持 PrimaryButton 自带 Fluent 主色 #268BD2 不覆盖。(5) **内容增强**——HTML 末尾追加虚线分隔 + 「💡 提示：按 Esc 关闭，拖拽角落可调整窗口大小」（正常+兜底两路径）；窗口标题加 📖 emoji + 格式「📖 帮助 · <面板标题>」；emoji 用 UTF-8 字节序列构造（📖=`\xF0\x9F\x93\x96`，💡=`\xF0\x9F\x92\xA1`）避免 MSVC 编码问题；QDialog 默认支持 Esc 关闭。(6) **预存 Qt6 Bug 修复**——`gui/PanelAnimator.h` L144/L170 `QPropertyAnimation::target()` → `targetObject()`（Qt6 已移除 target()），inline 头文件 ninja 未检测依赖变化导致 .obj 未重编，本轮修改 TeachingPanelHeader.h 触发重编暴露。关键决策：`adjustHelpDialogSize` 在 `exec` 前调用而非 `contentsChanged` 信号（文案静态加载）/ viewport 宽度兜底推算避免宽度 0 导致高度高估 / 兜底也用 QTextBrowser 确保长文本可滚动 / 按钮保持 PrimaryButton 不覆盖 QSS 避免冲突 / emoji 用 UTF-8 字节序列保证可移植性 / 不修改 helpDocs() 文案仅改 HTML 模板。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide` 构建通过 | [CHANGELOG](../CHANGELOG.md) |
| 第二十七轮：VM 栈沙盒面板新增「真实字节码追踪」子页（教学透明化） | 针对 VM 栈沙盒面板（VmStackSandboxPanel）做教学价值增强，从「单页黑盒栈状态机」升级为「双子页透明教学」结构。原面板仅有栈沙盒页（前端模拟栈状态机 + 末尾黑盒式 StackVM 验证），学习者无法观察真实字节码的逐指令执行过程。(1) **双子页 QStackedWidget 结构**——构造函数重构：顶部新增子页切换按钮栏 `pageSandboxBtn_`（栈沙盒）+ `pageTraceBtn_`（真实字节码追踪），`setCheckable(true)` 互斥高亮 QSS 选中态 #268BD2 填充+白字+bold；原栈沙盒单页内容完整保留包装为 `sandboxPage` QWidget 加入 `pageStack_`（index 0）；新增 `tracePage_` QWidget（index 1）由 `buildTracePage()` 构建。(2) **追踪页 UI 布局**——`buildTracePage()`：顶部工具条「📦 编译并加载」+ IP 指示器 QLabel（`IP: N OpCode: OP_XXX`）+ 状态标签；中部水平 QSplitter 左侧 `bytecodeList_`（QListWidget 每行 `[IP] OpCode operand` Consolas 字体）+ 右侧垂直 QSplitter（操作数栈 `traceStackView_` / 寄存器视图 `registerTable_` / 程序输出 `traceOutputEdit_`）；底部按钮栏「▶ 单步执行」/「⏩ 运行到底」/「🔄 重置」；QSS `#bytecodeList` 选中项 #268BD2 蓝色背景白字 / `#registerTable` Fluent 风格 #EEE8D5 表头+#93A1A1 网格线。(3) **真实字节码加载**——`loadBytecodeFromCurrentLevel()` 通过 `controller_->runLexer+runParser+runCompiler` 编译当前关卡源码（复用 `levelToMiniLangSource()`）；StackVM 模式遍历 `lastCompileResult().mainChunk` 调用 `disassembleInstruction(offset)` 填充列表（offset 自动推进），`bytecodeOffsets_` 记录字节偏移；RegisterVM 模式遍历 `compiler().getLastRegisterResult().mainChunk` 用 `regOpName(op)` + 操作数字节构造行文本，`instructionSizeAt(offset)` 推进；鼠标悬停 tooltip 从 `BytecodeTraceLibrary::opCodeDocs()` 查找 OpCode 语义说明。(4) **单步执行可视化**——`onTraceStep()` 调 `controller_->vmStep()` 根据 VmStepResult（OK/FINISHED/ERROR/NOT_READY）更新状态；`onTraceRunAll()` 循环 vmStep 直到非 OK（上限 100000 步防死循环）显示总步数；`onTraceReset()` 调 `vmReset()` 重置 IP 归零；注册 `addVmStateChangedListener` 回调 VM 状态变更时若在追踪页自动 `refreshTraceViews()`。(5) **指令级追踪高亮**——`refreshTraceViews()`：IP 指示器实时显示 `IP: N OpCode: OP_XXX`；字节码列表三态高亮 已执行浅绿 `QColor(220,240,220)` / 当前 IP 浅蓝 `QColor(200,220,255)` +bold / 未执行白底；`bytecodeOffsets_` 查找 currentIp_ 对应行 `scrollToItem(PositionAtCenter)` 自动滚动；操作数栈 `getVmStack()` 反向遍历显示（栈顶标注「← 栈顶」），RegisterVM 模式显示 `R{n} = value`。(6) **寄存器视图**——`refreshRegisterTable()` QTableWidget 2 列，RegisterVM 模式 32 行 R0-R31 从 `getVmStack()` 读寄存器窗口 `Value::toString()` 格式化；StackVM 模式 1 行提示「无寄存器，操作数在栈顶」tooltip 提示切换引擎。(7) **沙盒页教学联动**——`executeOp()` 末尾 `sandboxOpHintLabel_` 显示「→ 沙盒 PUSH_INT 对应真实 OpCode: OP_INT」映射（`buildSandboxToRealOpMap()` 静态 QHash 10 条：PUSH_INT→OP_INT / ADD→OP_ADD / SUB→OP_SUBTRACT / MUL→OP_MULTIPLY / DIV→OP_DIVIDE / MOD→OP_MODULO / NEG→OP_NEGATE / PRINT→OP_PRINT / HALT→OP_RETURN 等）；`onVerifyWithRealStackVM()` 末尾启用 `gotoTraceBtn_`「📊 查看追踪」按钮（初始禁用验证后激活）点击 `switchToTracePage()` 跳转。(8) **IdeController 绑定**——新增 `controller_` 成员 + `setController()` 方法（对齐 BytecodeTracePanel 模式），`app/ide.cpp` `registerLazyPanel("vm-sandbox")` lambda 增加 `vmStackSandboxPanel_->setController(controller_)` 调用；不修改 IdeController.h（任务约束），仅消费已有 inline 转发 API。关键决策：保留栈沙盒状态机不破坏原沙盒页完整保留为子页 1 / 复用 BytecodeTraceLibrary::opCodeDocs() 不重复造轮子 / 双后端兼容根据 isVmRegisterMode 分派加载路径 / gotoTraceBtn 初始禁用形成「验证→查看追踪」教学引导链 / VM 状态变更监听仅在追踪页可见时触发刷新避免无谓刷新。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide` 构建通过 | [CHANGELOG](../CHANGELOG.md) |
| 第二十六轮：教学游戏面板关卡切换控件视觉升级（芯片栏 + QSS 美化） | 针对 3 个教学游戏面板（TokenPuzzlePanel / AstBuilderToyPanel / VmStackSandboxPanel）的关卡切换控件做视觉识别度与可操作性升级。原实现三面板均用裸 QComboBox（无 objectName、无 QSS、无悬停反馈），关卡状态（锁定/当前/已完成）缺乏视觉区分。升级为「QComboBox 保留 + 上方关卡芯片按钮栏」双轨方案。(1) **关卡芯片按钮栏**——三面板 buildUi() 在 QComboBox 上方新增独立一行 QHBoxLayout，每个关卡一个 QPushButton（`objectName="levelChip"`，`setFixedSize(48, 32)`，字号 11px）；芯片点击 → `levelCombo_->setCurrentIndex(i)` 触发 `onLevelChanged → loadLevel → refreshLevelChips` 完成双向同步；`setToolTip` 显示关卡标题与目标/教学点。(2) **QSS Solarized 风格美化**——三面板构造函数 setStyleSheet：`QComboBox#levelCombo` #FDF6E3 背景+#93A1A1 边框+4px 圆角 hover #268BD2；`QPushButton#levelChip` #EEE8D5 背景+4px 圆角 hover #268BD2+#E5F3FB；`[current='true']` #268BD2 主题色填充+白字+bold；`[locked='true']` #EDEDED 灰色+#AAA 灰字；动态属性通过 `setProperty`+`style()->unpolish()/polish()` 触发 QSS 重评估。(3) **三态语义区分 refreshLevelChips()**——TokenPuzzlePanel（有解锁机制）：locked 读 `QStandardItemModel::item(i)->isEnabled()`，locked 显示 🔒+不可点击，current 且已获星显示「关卡号 ⭐」，unlockNextLevel() 末尾调用刷新；AstBuilderToyPanel（全解锁）：locked 恒 false，current 且已完成（`completedLevels_` 含该 level）显示 ⭐，markCurrentCompleted() 末尾刷新；VmStackSandboxPanel（全解锁）：locked 恒 false，current 且已完成（查 `LearnerProgressStore::getLevelStars("level-N")>=0`）显示 ⭐，onCheck() 通关后刷新。(4) **emoji 用 UTF-8 字节序列**——🔒=`\xF0\x9F\x94\x92`(U+1F512)，⭐=`\xE2\xAD\x90`(U+2B50)，避免 MSVC 源码编码问题。关键决策：保留 QComboBox+芯片栏双轨不破坏现有信号接线与解锁逻辑 / dynamic property+polish/unpolish 触发 QSS 重评估 / 三面板 locked 语义差异化（TokenPuzzlePanel 真实解锁机制，另两面板恒 false 保留 QSS 备扩展）/ 已完成标记复用各面板既有数据源（levelStars_/completedLevels_/LearnerProgressStore）不引入冗余。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide` 构建通过 | [CHANGELOG](../CHANGELOG.md) |
| 第二十五轮：编译管线可视化面板 UI 美化（PipelineViewer） | 针对编译管线可视化面板（PipelineViewer）做全面 UI 美化，把原本「无样式 Qt 默认按钮 + 单层信息 + 仅 220ms 淡入」的扁平展示升级为「5 阶段 Solarized 主题色 + emoji 图标导航 + header 标题条 + 淡入叠加水平滑动」的结构化视觉叙事。(1) **5 阶段主题色体系**——`gui/PipelineViewer.{h,cpp}` 新增 4 个静态助手 `stageColor/stageIcon/stageTitle/stageDesc`：源码石墨灰 #586E75 / Token 海蓝 #268BD2 / AST 森林绿 #859900 / IR 紫色 #6C71C4 / 字节码橙色 #CB4B16（取自 Solarized 亮色色板）；emoji 图标用 UTF-8 字节序列构造（📄/🔢/🌳/⚙/📦）避免 MSVC 源码编码问题。(2) **按钮样式优化**——`makeStepBtn` lambda 增加阶段主题色 QSS：未选中态 #FDF6E3 浅色背景 + 1.5px 阶段色边框 + 阶段色文字，选中态阶段色填充 + 白字，hover #EEE8D5，pressed 同选中态；按钮 objectName=stepBtnN 便于 QSS 定位，最小宽度 80→96，cursor PointingHandCursor。(3) **布局优化**——步骤导航条容器从 QWidget 改为 QFrame + QSS 圆角背景（#EEE8D5 base2 + #93A1A1 边框 + 8px 圆角）；阶段间箭头从「→」改为 chevron「›」（U+203A）22pt 字号带目标阶段主题色（暗示流向）；内容区 QStackedWidget 外层 1px 边框 + 6px 圆角 + #FDF6E3 背景。(4) **动画过渡增强**——`switchToStep` 保留 `PanelAnimator::fadeInWidget` 220ms 淡入，新增 `QPropertyAnimation` 驱动 `pos` 属性水平滑动：根据切换方向（step>=oldStep 前进 +24px / step<oldStep 后退 -24px）220ms OutCubic 缓动回到原位，利用 QStackedLayout 在 setCurrentIndex 后子控件 geometry 不再被覆盖的特性安全叠加位移动画。(5) **信息层级展示**——`wrapPage` lambda 把每个内容控件包装到「header + content」垂直布局容器；header 是 QLabel 富文本：左侧 4px 阶段主题色竖线 + #EEE8D5 背景 + 「emoji 标题   描述」三段式布局（标题 14px 600 主题色，描述 11px #657B83）；内容控件保持原有 populate* 数据源不变仅套背景/边框/圆角 QSS。(6) **状态条美化**——新增 `updateStatusBar()` 私有方法集中管理状态条样式与文本（reloadCurrentStep / onCursorPositionChanged 改调用此方法），状态条加 📌 emoji 图标前缀 + 左侧 4px 当前阶段主题色竖线（随阶段切换变色）+ #EEE8D5 背景 + 4px 圆角。关键决策：emoji 用 UTF-8 字节序列构造保证可移植性 / 水平滑动用 `pos` 属性而非 maximumWidth（animateShowHorizontal 是宽度展开不适合页面切换）/ 方向感动画让用户感知管线前进后退 / header 用 QLabel 富文本 + QSS border-left 比自定义 paintEvent 简洁 / stageColor 与 TeachingTheme::learningStageColor 共用 Solarized 色板但映射不同独立维护。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide` 构建通过 | [CHANGELOG](../CHANGELOG.md) |
| 第二十四轮：教学模块 P2 项（复核版收尾） | 依据【MiniLang教学模块分析_复核与优化建议.md】完成 P2 三项收尾：(1) **P2-C 帮助文案魔法数字**——`gui/TeachingPanelHeader.cpp` 引入 `gui/LearningPathData.h`，新增 `learningPathSummaryText()` 从 `LearningPathData::stageCount()` + `activities().size()` 动态派生「N 个阶段 M 个活动」文案（替代硬编码「5 个阶段 21 个活动」，实际 28 活动）；`bug-hunt` 文案改为「三档分级挑战（初阶/中阶/高阶）」替代「7 类历史 Bug」描述。(2) **P2 长章节折叠**——`gui/LabManualPanel.{h,cpp}` 新增 `foldBtn_`「📂 折叠次要章节」按钮 + `foldMinorSections_` 状态 + `onToggleFold()` 槽 + `isMinorSection(headingText)` 静态方法（14 关键字：进阶/思考题/常见错误/延伸/参考/挑战/习题/FAQ/附录/深入等）+ `applyFolding(markdown)` 状态机扫描 Markdown（仅识别 `##` level==2 可折叠，跟踪围栏 ```/~~~ 代码块状态避免误判代码内 `#`），折叠模式保留次要章节标题 + 「▶ 此节已折叠」提示行让 TOC 链接仍可点击，`showCurrentChapter()` 在生成 TOC 前调用 applyFolding。(3) **P2 错误码优先匹配**——`common/Diagnostic.h` Diagnostic struct 新增 `std::string code` 字段（默认空，向后兼容）+ 6 参构造重载；DiagnosticBag 新增带 code 的 `addError/addWarning/addInfo` 4 参重载；`gui/ErrorHintEngine.{h,cpp}` 新增 4 参 `enrichErrorMessage(msg, code, category, scopeVars)` 重载，code 非空时优先查 `kCodeToHint` 表（9 条简单 tag → 固定教学提示映射：missing-semicolon/unbalanced-paren/unbalanced-brace/division-by-zero/index-out-of-bounds/unexpected-token/arity-mismatch/recursion-depth/null-access），code 空/未知/需 msg 提取变量名（undefined-variable/undefined-function/type-mismatch）回退到 3 参子串匹配版本；`app/ide.cpp` `displayDiagnostics()` 循环内改为 4 参版本传 `diag.code`。(4) **测试**——`tests/TestErrorHintEngine.cpp` 新增 `ErrorHintEngineCodeMatching` 套件 8 用例（EmptyCodeFallsBackToSubstring / KnownCodeMissingSemicolon / KnownCodeUnbalancedParen / KnownCodeDivisionByZero / KnownCodeRecursionDepth / UnknownCodeFallsBackToSubstring / UndefinedVariableCodeFallsBackForSpelling / LocalizedMsgStillMatchesViaCode）。关键决策：applyFolding 保留次要章节标题让 TOC 链接仍可点击 / isMinorSection 关键字表而非正则避免编译开销 / 仅 `##` 可折叠避免过度碎片化 / Diagnostic code 可选默认空保持向后兼容 / kCodeToHint 只放不需 msg 提取变量的简单 tag 复用 3 参逻辑 / displayDiagnostics 是唯一接入 diag.code 的点（runtime 异常路径保持 3 参）。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1762/1762 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第二十三轮：IDE 体验优化 5 项（启动性能 + 视觉一致性 + 工具栏修复） | 针对用户反馈的 5 个 IDE 体验问题做集中修复：(1) **IDE 启动慢优化**——`app/ide.cpp` 移除构造函数末尾冗余 `applyFluentStyle()` 调用（`Theme::setThemeMode` 已触发 `onThemeModeChanged → applyFluentStyle`，原二次调用白白重新生成 ~510 行 QSS）；`app/ide.h` 新增 `teachingPanelFactories_`（QMap<QString,std::function<void()>>）+ `ensureTeachingPanelCreated` 方法，`ide.cpp` 用 `registerLazyPanel` lambda 替换原 20 个 `makeTeachingPanel` 直接构造，面板构造（含信号接线）延迟到首次 `showTeachingPanel()` 访问时执行，启动时仅构造 `TeachingTreePanel` 导航树。(2) **欢迎页字体未居中**——`TitleLabel`/`CaptionLabel` 的 `setAlignment(Qt::AlignCenter)` 移到 `setFont()` 之后（`setFont` 触发的 `fontChangeEvent` 可能重置 alignment）+ QSS `qproperty-alignment: 'AlignCenter'` 兜底。(3) **视图菜单移除教学面板子菜单**——删除 4 个子菜单（teachBeginnerMenu/teachFrontendMenu/teachEngineMenu/teachAdvancedMenu 共 ~60 个 toggleAction 项，与 `TeachingTreePanel` 树形导航功能重复）；保留全部 19 个教学面板快捷键改用 `registerTeachingShortcut` lambda 注册 `QShortcut`（Ctrl+1~9/Ctrl+Shift+1~9/F5~F12 等），快捷键触发时若已在对应面板则切回编辑器。(4) **字节码调试结束后按钮重复修复**——`onVmStop()` 末尾新增 `showVmButtons(false)`（原实现未隐藏 VM 按钮容器，退出字节码调试后工具栏残留 5 个 VM 按钮+分隔线）；`showDebugButtons(true)`/`showVmButtons(true)` 互斥——进入时主动隐藏对方按钮组（`debugButtonContainer_`+`debugSepAction_` / `vmButtonContainer_`+`vmSepAction_`），确保工具栏任一时刻只显示一套调试按钮。(5) **断点圆点/条件断点弹窗/代码补全弹窗样式重设计**——`gui/CodeEditor.cpp` `LineNumberArea::paintEvent` 断点绘制改为 `painter.save()`+`setRenderHint(Antialiasing)` 局部抗锯齿 + 三层绘制（半透明外发光晕 / 径向渐变主圆点+1.2px 描边 / 条件断点附加金色外环），色值从纯 `Qt::red`/`QColor(255,165,0)` 改为 `#E43B44`/`#F5A623`，半径 6→7；`contextMenuEvent` 用自定义 `QDialog`（420×180，Fluent 色板 QSS：`#FDF6E3` 背景+`#268BD2` 主色按钮+4px 圆角+Consolas 等宽输入框）替代 `QInputDialog::getText`；`CodeEditor` 构造函数 `completer_->popup()->setStyleSheet()` 应用 Fluent 风格 QSS（`#FDF6E3` 背景+`#93A1A1` 边框+6px 圆角+4px item 圆角+`#268BD2` 选中色+8px 自定义滚动条）。关键决策：懒加载工厂 map 集中注册 / `setAlignment`+QSS 双重保险 / `QShortcut` 解耦菜单与快捷键 / show 函数内部互斥比调用点 hide 更不易遗漏 / 抗锯齿局部开启避免行号文字模糊 / 自定义 `QDialog` 可完全控制布局且可扩展。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide`+`minilang_tests` 均构建成功，全量 1762/1762 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第二十二轮：教学模块 P0+P1 项（复核版） | 依据【MiniLang教学模块分析_复核与优化建议.md】完成 P0 三项 + P1 四项落地：(1) **P0-A 术语表跳转信号接线**——`gui/GlossaryPanel.{h,cpp}` 新增 `relatedPanelFor(term)` 静态方法（30 术语映射到相关面板）+ `termActivated(term, relatedPanel)` 信号；`app/ide.cpp` 连接 `termActivated → onJumpToPanel`，实现术语→关联面板一键跳转。(2) **P0-B Bug-Hunt 三后端判分与回写**——`gui/BugHuntPanel.{h,cpp}` 新增 `challengeSolved(challengeId)` 信号；`onTripleVerify()` 末尾三后端输出一致且匹配 expectedBehavior 时发射；`app/ide.cpp` 连接 `challengeSolved → markActivityCompleted("bug-hunt")`。(3) **P0-D LabManual EXPECTED_OUTPUT 自动判分**——经审读确认已在第二十轮 P0-1 闭环中实现（`runSampleRequested → 运行 → 输出对照`），本轮仅验证。(4) **P1-E rightDock 白名单保留**——`app/ide.cpp` `showTeachingPanel()` 新增 `kKeepRightDockPanels` 白名单（pipeline/ir-transform/bytecode-trace/memory-model/backend-compare），保留配套右侧视图。(5) **P1-F2/F 多面板活动映射 + visited-* 活动**——`gui/LearningPathData.cpp` 阶段零末尾新增 6 个浏览型活动（visited-glossary/pipeline/memory-model/bytecode-trace/exception-flow/closure-inspector，共 28 活动）；`app/ide.cpp` `kBrowsePanelToActivity` 映射浏览型面板进入即 `markActivityCompleted`；`gui/PanelCatalog.cpp` `canonicalPanelId()` 新增 6 个 visited-* → 真实面板 ID 映射。(6) **P1-F12 常见错误速查与 ErrorHintEngine 联动**——`gui/ErrorHintEngine.{h,cpp}` 新增 `ErrorPattern` struct（tag/title/buggyCode/category）+ `errorPatterns()` 返回 12 条错误模式（missing-semicolon/unbalanced-paren/...）；`gui/LabManualPanel.cpp` 处理 `buggy:tag` URL scheme → emit `runSampleRequested(buggyCode)` + `showCurrentChapter()` 末尾动态追加「🔧 常见错误速查」段从 errorPatterns() 生成表格；`tests/TestErrorHintEngine.cpp` +4 测试（PatternsAreNonEmpty/TagsAreUnique/KnownTagsPresent/CategoriesAreValid）。(7) **P1-F7 细粒度压扁修复**——三游戏面板 connect 改为 `areAllLevelsCompleted({子关卡列表})` 全部完成才 `markActivityCompleted(父活动)`。(8) **Qt6 兼容性预存 Bug 修复**——`gui/IRTransformPanel.cpp` + `gui/PipelineViewer.cpp` 的 `QRegExp`（Qt6 已移除）→ `QRegularExpression`（`indexIn` → `match` + `hasMatch` + `captured`），修复 `fatal error C1083: 无法打开包括文件: "QRegExp"`。关键决策：visited-* 为轻量打卡非学习目标 / `buggy:` scheme 复用 runSampleRequested 闭环 / errorPatterns 集中管理单一数据源 / areAllLevelsCompleted 避免单关完成即标记整体 / canonicalPanelId 显式映射 visited-* 而非前缀匹配。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1754/1754 测试通过（155 秒） | [CHANGELOG](../CHANGELOG.md) |

### 2026-07-07 变更摘要

| 类别 | 内容 | 详情 |
|------|------|------|
| 第二十一轮：教学模块 P2-3 学情画像多维度进度数据 | 依据【MiniLang教学模块分析与改进建议.md】完成 P2-3（功能 9），将学习进度从「打卡式」升级为「学情画像」：(1) **P2-3a LearnerProgress 多维数据**——`gui/LearnerProgress.{h,cpp}` LearnerProgress struct 新增 4 字段（score 0-100 / bestStars 历史最佳 / spentMinutes 累计耗时 / failCount 失败次数）+ WeakPoint struct + 10 个 API（recordScore 截断[0,100]保留历史最佳 / getScore / getBestStars / addSpentMinutes 累加 / getSpentMinutes / recordFailure / getFailCount / getWeakPoints 筛选未完成且尝试≥3或失败≥1 按 failCount→attemptCount 降序 / estimatedRemainingMinutes 未完成且已解锁之和 / totalSpentMinutes）+ load/save/reset 同步 4 字段。(2) **P2-3b LearningPathPanel 薄弱点+剩余时间**——`gui/LearningPathPanel.{h,cpp}` 新增 weakPointsLabel_（warning 色）+ remainingLabel_（success 色）；refresh() 调用 getWeakPoints 显示第 1 个薄弱点+失败次数（多个时 tooltip 列出全部）+「⏱ 剩余 ~X 分钟 · 已用 Y 分钟」；buildActivityRow 增强时间显示（~X 分钟 / 已用 Y）+ 得分/星级徽章 + 已解锁活动 rich tooltip。(3) **P2-3c LabManualPanel 练习接入得分**——`gui/LabManualPanel.cpp` onSubmitChoiceExercise 答对 recordScore(100,1)+save / 答错 recordFailure+save；checkAllExercisesPassed 全章通过升级 3 星 recordScore(100,3)+save。(4) **P2-3d 测试**——`tests/TestLearningPathAudit.cpp` +8 测试（套件 4 LearningPathRichProgressAudit：RecordScoreKeepsBest / RecordScoreClampsOutOfRange / SpentMinutesAccumulates / RecordFailureIncrementsCount / WeakPointsDetection / EstimatedRemainingMinutesExcludesCompletedAndLocked / LoadSaveRoundTripPreservesRichFields / ResetClearsRichFields）。(5) **MarkdownRendererAudit 测试适配**——P2-2 为标题渲染添加 `id="anchor"` 属性导致 6 个测试 `contains("<hN>text</hN>")` 精确匹配失败；新增 headingMatches 辅助函数用 QRegularExpression 匹配 `<hN(?:\s+[^>]*)>text</hN>` 接受可选 id 属性，更新 6 个测试。关键决策：score 截断[0,100]保留历史最佳 / 薄弱点筛选策略（未完成+尝试≥3或失败≥1）/ estimatedRemainingMinutes 排除已完成和未解锁 / headingMatches 正则既严格又兼容可选属性。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，1728/1729 测试通过（1 perf 占位符 Not Run） | [CHANGELOG](../CHANGELOG.md) |
| 第二十轮：教学模块 P0 三项改进 | 依据【MiniLang教学模块分析与改进建议.md】完成 P0 优先级三项改进：(1) **P0-1 教学闭环打通（F5/F13）**——`app/ide.cpp` `showTeachingPanel()` 引入 `kKeepBottomDockPanels` 白名单（lab-manual/bug-hunt/syntax-explorer/backend-compare/ir-transform/profile-dashboard 显示时不再隐藏 bottomDock_）；`gui/LabManualPanel.{h,cpp}` 新增 `runSampleRequested` 信号 + `runBtn_`「▶ 运行示例」按钮 + `onRunSample()` 槽；app/ide.cpp 连接信号→`loadCodeIntoMainEditor`+`onRun()` 实现「看教学样例 → 一键运行 → 底部输出」闭环。(2) **P0-2 游戏进度持久化（F7）**——`gui/LearnerProgress.h` LearnerProgress struct 新增 `levelStars` 字段（key="token-puzzle-N"/"ast-toy-level-N"/"level-N"，value=-1 未完成 / 0 跳过 / 1-3 星级）；`LearnerProgressStore` 新增 `markLevelStars`（保留历史最佳）/`getLevelStars`/`areAllLevelsCompleted` 3 API + load/save/reset 同步；TokenPuzzlePanel 构造时加载持久化星级并按已完成关卡解锁下一关 UI，onCheckAnswer/onSkipLevel 写入 store；AstBuilderToyPanel 构造时加载已完成关卡，markCurrentCompleted 写入 store（统一 3 星）；VmStackSandboxPanel onCheck 通关写入 store（统一 3 星）；app/ide.cpp 三个 connect 简化（移除 `levelId.section('-', 0, 1)` 复杂映射，统一改为只转发粗粒度活动 ID）。(3) **P0-3 错误信息友好化（F10/F11）**——`app/IdeController.{h,cpp}` 新增 `getReplScopeVariableNames()` 公共接口（返回 Interpreter 作用域链所有可见变量名，去重）；app/ide.cpp 三处 appendError 接入 ErrorHintEngine（runtimeError 收集 scopeVars + enrichErrorMessage；displayDiagnostics parse 错误 enrichErrorMessage 空 scopeVars；VM ERROR case enrichErrorMessage runtime 空 scopeVars）；gui/ReplPanel.cpp worker 线程 RuntimeError/std::exception 捕获点收集 scopeVars + ErrorHintEngine 增强后投递 REPL 输出区，emit 信号携带原始 msg 由主线程 handler 二次增强避免双重增强。关键决策：白名单而非黑名单 / markLevelStars 保留历史最佳 / AST 玩具+VM 沙盒统一 3 星 / ReplPanel 双路径各自增强一次避免重复附加教学提示 / parse 阶段 scopeVars 为空（MiniLang 未定义变量/函数是运行时错误）。MSVC 19.51 + Qt 6.10.3 + Ninja 构建 `minilang_ide`+`minilang_tests` 均成功，全量 1730/1730 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第十九轮：7 项教学面板修复与优化 | 完成用户指定的 7 项教学面板修复与优化任务：(1) **任务2 三栏布局**——`app/ide.h/.cpp` 新增 `QSplitter* centerSplitter_`（[centerStack_ | editorTabWidget_] 水平布局），`showTeachingPanel/showEditorArea/ensureEditorVisible/onEditorTabCloseRequested` 适配三栏切换，F3/Shift+F3 快捷键 + `syncViewMenuChecks` 兼容 `centerInEditorMode_`。(2) **任务3 新手引导补全**——`gui/TeachingPanelHeader.cpp` 补全 glossary（术语表）面板帮助文案，覆盖 20 个教学面板。(3) **任务4 VM 沙盒配色统一**——`gui/VmStackSandboxPanel.cpp` 替换 `stackList_`/`outputEdit_` 两处硬编码 `#101820` 黑色为 `TeachingTheme::surface()`/`surfaceHover()`。(4) **任务5 条件断点场景加载**——`gui/BreakpointConditionPanel.cpp/.h` 修复 8 个场景的 MiniLang 语法错误（print 语句括号 / 类构造 ClassName() / len() 方法）+ 新增 `loadSampleBtn_` 按钮 + `loadSampleRequested` 信号 + `currentSampleCode_` 成员 + `setController` 从 inline 改为 .cpp 实现（注册 vmStateChanged 监听器）。(5) **任务6 性能仪表盘运行剖析**——`gui/ProfileDashboardPanel.cpp` 修复 6 个场景的 MiniLang 语法错误。(6) **任务7 教学字号调节**——`app/ide.h/.cpp` 新增 `applyTeachingFontSize()` 方法（递归遍历 centerStack_ 所有页面，统一设置 QTextBrowser/QListWidget/QTextEdit 字号，基准 `codeFontSize_ + 2`，范围 [9,34]）。(7) **预存编译错误修复**——`gui/GlossaryPanel.cpp`（字符串拼接 `\n"` 重复）/ `gui/SandboxLevels.cpp`（缺 include）/ `gui/TokenPuzzleData.cpp`（未转义 ASCII 双引号→「」）/ `gui/BreakpointConditionPanel.cpp`（`PushButton`→`QPushButton`）/ `gui/AstToyLevels.cpp`（`findByLevel` 未实现 → LNK2019）。关键教训：ninja 不自动检测头文件变化导致的依赖 .obj 重编译，添加新成员后须手动删除过期 .obj 避免 heap corruption。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1730/1730 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 第十八轮审计批次 8：Parser 错误恢复 + 三后端类型注解一致性 | 修复 2 P1 + 3 P2 共 5 项 Bug：(1) **BUG-PARSER-SYNC-1（P1）** synchronize() 无条件 advance() 吞掉同步关键字（var/fun/class/...），导致缺分号时下一条声明整体从 AST 丢失；修复：advance 前添加同步关键字预检查 switch。(2) **BUG-PARSER-SYNC-2/4（P1/P2）** block() 仅识别 catch 作为块边界，遗漏 finally（try-finally）和 else（if-else）；修复：添加 FINALLY/ELSE 中断 + block() 结尾三分支处理（`}` 正常消耗 / catch/finally/else/EOF 跳过 / 其他记录诊断不抛错）。(3) **BUG-PARSER-SYNC-3（P2）** synchronize() switch 缺少 TK_FINALLY。(4) **BUG-PARSER-MSG-1（P2）** consume() 错误消息不含实际 token；修复：拼接为"期望 ';' 但得到 'var'"。(5) **BUG-TYPE-1（P1）** 函数返回类型注解仅 Interpreter 检查，StackVM/RegisterVM 静默通过；修复：Compiler/IR 新增 currentFunctionReturnType_ 成员 + visitFunDecl 设置 + visitReturnStmt 发射 OP_TYPE_CHECK/TYPE_CHECK IR。新增 10 个回归测试（Parser 6 + ThreeEngines 4），MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，1720/1721 测试通过（1 perf 占位符 Not Run）。已知 gap：BUG-TYPE-2 参数类型检查 + BUG-TYPE-3 错误消息不一致暂不实施 | [CHANGELOG](../CHANGELOG.md) |
| 第十七轮审计批次 7：寄存器分配 P0 修复 + 模块状态泄漏 + Formatter 往返等价性 | 修复 1 P0 + 1 P1 + 4 P2 共 6 项 Bug：(1) **P0-REGALLOC** `RegisterBytecodeBackend` vreg 不复用导致 32 寄存器溢出（72 条指令程序即触发），改为线性扫描寄存器分配（collectVRegLastUse + releaseDeadVRegs + freeRegs_ 复用），正确处理 MEMBER_SET/INDEX_SET 的 obj vreg 隐式使用（通过 lastMutatedReceiverReg_ 延伸到 WRITEBACK_*）；(2) **P1-D** Formatter 单语句体强制花括号破坏往返幂等；(3) **P2-A** Interpreter moduleMtimes_ 跨会话泄漏；(4) **P2-B** Compiler compileViaIR/compileViaRegisterIR 失败路径未清理模块状态；(5) **P2-E** 条件断点求值器未设置 boundInstance_；新增 1 回归测试 + 更新 1 测试预期。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，1710/1711 测试通过（1 perf 占位符 Not Run） | [CHANGELOG](../CHANGELOG.md) |
| 第十六轮架构优化：观察者模式解耦 + 三后端差分测试（OPT-1 / OPT-2） | 针对用户提出的两项架构优化建议实施：(1) **OPT-1 GUI 与引擎层耦合**——`IdeController` 新增 `vmStateChanged` 观察者 API（`addVmStateChangedListener(std::function<void()>)` + `notifyVmStateChanged()`），所有 VM/调试状态变更点（vmStep/vmStepByMode/setVmBreakpoints/vmStop/vmReset/setBreakpoints/setBreakpointCondition/setupDebug）调用 `notifyVmStateChanged()` 通知订阅面板；5 个面板（CallStackPanel/VariableInspectorPanel/BytecodeTracePanel/MemoryModelPanel/BreakpointConditionPanel）在 `setController` 中订阅监听器，QTimer 从 500ms→2000ms 降频作为兜底刷新；构造函数中 3 个信号→观察者桥接（vmRunPaused/pausedAt/workerFinished）；VM 方法保持 inline（MagicCommands.cpp 依赖内联符号）；保留 `IdeController` Facade 未拆分 EngineFacade+GuiAdapter（影响面大且测试目标链接约束复杂）。(2) **OPT-2 三后端维护成本**——新增 `tests/TestThreeEnginesFuzz.cpp` 模板化随机程序差分回归测试，11 个测试套件共 368 用例（ArithmeticFuzz 60 / ControlFlowFuzz 40 / FunctionFuzz 40 / RecursionFuzz 30 / ClosureFuzz 30 / StringFuzz 30 / ArrayFuzz 30 / ClassFuzz 30 / ErrorPathFuzz 30 / LogicFuzz 30 / DeterministicBaseline 8），固定种子 `mt19937_64`（kFuzzSeed=0x4D4C416E67000000ULL）保证可复现；`expectThreeAgree` 差分断言策略：Interpreter vs StackVM 严格断言，RegisterVM 编译失败时宽容跳过（已知 IR lowering 限制）；顺带修复 `ReplPanel.cpp` 的 `finallyCount` 未声明 Bug。MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide`+`minilang_tests` 均构建成功，全量 1709/1709 测试通过（186 秒） | [CHANGELOG](../CHANGELOG.md) |
| 第十四轮教学面板布局重构（树形导航 + centerStack_ 切换 + 全局字号） | 按用户需求将 19 个教学面板从右侧独立 dock 迁移到中央 `centerStack_` 子页，新建 `gui/TeachingTreePanel.h/.cpp` 树形导航组件替代 `LearningHubDialog` 弹窗模式。点击 ActivityBar「学习」图标 → 左侧显示可折叠树形导航（4 大分类：🌱 入门导览 / 🔤 编译前端 / ⚙️ 执行引擎 / 🏗️ 深入实战 + 顶部「📝 代码编辑器」入口 + 22 叶子节点）→ 点击叶子节点切换中央区域到对应教学面板，点击「代码编辑器」切回编辑器+输出面板。默认仅展开第一个分类（实验手册在「深入实战」分类下，折叠隐式实现「实验手册默认不打开」）。新增 `panelToStackIndex_`（QMap<QString,int>）映射 panelId→centerStack_ 索引实现 O(1) 切换；`showTeachingPanel` 主动隐藏 bottomDock_/rightDock_（教学面板独立展示不需配套），`showEditorArea` 不强制恢复尊重用户布局。提取 `makeTeachingToggle` lambda 替代 19 个重复 connect 块；`syncTeachingAction` lambda 按 `centerStack_->currentIndex()` 同步勾选。同时修复字号调节仅作用于当前编辑器的问题：引入 `codeFontSize_`（默认 11pt）+ `applyCodeFontSizeToAllEditors()` 按 delta 遍历所有 editorTabs_，新标签页创建时自动继承全局字号。MSVC 19.51 + Qt 6.10.3 + Ninja 构建通过，全量 1693/1693 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 工程配置与文档一致性修复（build） | 修复构建系统配置与文档不一致：(1) CMake 最低版本 3.20→3.25（对齐 CMakePresets.json schema v6）；(2) 移除 `.gitmodules` 中 googletest/ads/QFluentKit 的 `branch=` 字段避免 submodule 偏离 gitlink；(3) Dockerfile 改用 aqtinstall 安装 Qt 6.10.3 + 修正 QTDIR 指向 `/opt/qt6/6.10.3/gcc_64`；(4) CI Unix build-test 增加 `-DMINILANG_WERROR=ON -DMINILANG_ENABLE_LTO=ON` 对齐 Windows；(5) `.clang-tidy` modernize-use-nullptr 修正 + `.gitattributes` 清理冗余 eol；(6) README/CONTRIBUTING 文档同步：Preset 名称、输出路径、CMake 最低版本、Qt 经验证版本、CMake 选项说明表、configure.bat 行为描述、统一测试数量为 1699 | [CHANGELOG](../CHANGELOG.md) |

### 2026-07-06 变更摘要

| 类别 | 内容 | 详情 |
|------|------|------|
| 第十三轮新手友好度优化（P2-6/P2-7/Glossary 接入） | 三项收尾工作：(1) P2-6 面板淡入动画——`showBottomPanel`/`showRightPanel` 引入 `wasClosed` 局部变量缓存 dock 初始状态，仅在隐藏→显示分支调用 `PanelAnimator::fadeInWidget(stack->currentWidget(), DURATION_MS)` 150ms 平滑淡入当前 Pivot 子页，对齐 AGENTS.md「150ms 平滑淡入」约束，避免每次切 tab 都重复动画；(2) P2-7 教学面板快捷键——为 16 个 `viewXxxAction_` 调用 `setShortcut(...)`，按功能分组分配 `Ctrl+Shift+1~9`（编译前端 1-3 + 执行引擎 4-9）+ `Alt+1~5`（条件断点/异常流/闭包/性能/实验手册）+ `Ctrl+Shift+P/J/G`（学习路径/代码旅程/术语表），覆盖全部教学面板；(3) Glossary 接入——`ide.h` 添加 `#include "gui/GlossaryPanel.h"` + glossaryDock_/glossaryPanel_/viewGlossaryAction_ 三个成员，`ide.cpp` 在 codeJourneyDock_ 后插入 dock 创建+wrapTeachingPanel 包装+RightDockWidgetArea 注册+setDockMinSize 批量设置，创建 viewGlossaryAction_ + `Ctrl+Shift+G` + `teachBeginnerMenu->addAction` + `syncViewMenuChecks` 同步 + `onActivityRequested("glossary")` 路由；`LearningHubDialog.cpp` 入门导览分组添加 Glossary 卡片（3→4 项）；`showHelpDialog` 第 4 组 8 项快捷键更新为实际绑定值 + tipLabel 追加更多快捷键提示；关键决策：`wasClosed` 防止 toggleView 后 isClosed() 返回值变化影响判断、快捷键按功能分组对应 4 子菜单、`Ctrl+Shift+P` 重分配给学习路径地图（原编译管线从未实际绑定）；MSVC 19.51 + Qt 6.10.3 + Ninja `minilang_ide`+`minilang_tests` 均构建成功，1693/1693 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| GlossaryPanel 术语表面板 | 新建 `gui/GlossaryPanel.h` / `.cpp` 教学辅助面板，集中展示 30 条核心术语（NaN-boxing / COW / upvalue / SSA / lowering / mark-sweep / RefCounted / BytecodeChunk / IRModule / ClosureData 等，覆盖内存模型/编译/运行时/类型系统/模块系统/综合 10 大类），按字母排序 + 顶部 QLineEdit 搜索框（28px + 四字段大小写不敏感包含过滤）+ 左 QListWidget（minWidth 200）+ 右 QTextBrowser（MarkdownRenderer 渲染详情）+ `term:` anchor 链接跨术语跳转；`setRowHidden` 保留选中状态；`termActivated` 信号；纯静态数据不依赖 IdeController，复用 MarkdownRenderer / TeachingTheme / PanelAnimator，风格参考 ExceptionFlowPanel；仅新增 2 文件 + 修改 cmake/minilang_core.cmake，不修改 app/ide.cpp（UI 接线由用户手动完成），不加入测试目标；MSVC 19.51 + Qt 6.10.3 `GlossaryPanel.cpp.obj` 编译成功（1.1MB） | [CHANGELOG](../CHANGELOG.md) |
| LearningPathPanel 搜索框与 Locked tooltip | 为 `gui/LearningPathPanel`（功能 6）添加两项 UX 增强：(1) 顶部 topBar 新增 QLineEdit 搜索框（placeholder「搜索活动...」+ 清除按钮 + 200px 最大宽度 + TeachingTheme 主题色 QSS），`textChanged` 直连 `refresh()` 重新构建活动列表；`buildStageCard` 活动循环内按 `id/title/description` 三字段 `Qt::CaseInsensitive` 包含匹配过滤，不匹配活动 `continue` 跳过（阶段卡片仍渲染保持 5 阶段视觉结构）；(2) `buildActivityRow` 末尾 `else`（!unlocked 锁定）分支新增 tooltip：遍历 `activity.prerequisites` 调用 `LearningPathData::findById` 查找前置活动标题（nullptr 防御），非空时 `setToolTip(mlTr("需要先完成：%1 才能解锁").arg(titles.join(", ")))`，空时回退 `mlTr("此活动尚未解锁")`；QPushButton 即使 `setEnabled(false)` 仍可显示 tooltip；仅修改 gui/LearningPathPanel.h 与 .cpp 两个文件，MSVC 19.51 + Qt 6.10.3 构建验证通过（`minilang_ide.exe` 链接 + windeployqt 部署成功），测试目标不链接 LearningPathPanel.cpp UI 改动不影响测试套件 | [CHANGELOG](../CHANGELOG.md) |
| 文件拖放支持与外部修改监听 | Ide 主窗口新增 `dragEnterEvent` / `dropEvent` 接受从 Windows 资源管理器拖入的 `.mini` / `.ml` 文件（已打开则切换标签，否则新开标签加载）；`QFileSystemWatcher` 监视当前打开的文件，外部编辑器修改后异步弹框（`QMessageBox` `WA_DeleteOnClose`）询问是否重新加载（Yes 则 `loadFileIntoTab` 从磁盘重读）；`selfSaving_` 标志 + 500ms `QTimer` 自动复位跳过 IDE 自身保存触发的 fileChanged；支持 Vim/某些编辑器"删除+重建"文件模式（100ms 延迟重试 `addPath`）；一次只盯当前活动标签文件，切换标签/关闭最后标签时更新/清空监视；仅修改 app/ide.h 与 app/ide.cpp 两个文件，MSVC 19.51 + Qt 6.10.3 `ide.cpp.obj` 编译成功 | [CHANGELOG](../CHANGELOG.md) |
| CodeEditor 右键上下文菜单 | 为 CodeEditor 添加自定义右键上下文菜单，在 QPlainTextEdit 默认菜单基础上追加项目特化操作（注释切换 / 块注释 / 格式化 / 跳行 / 查找替换）与调试相关操作（断点切换 / 编辑断点条件 / 运行到光标）；新增 `contextActionRequested(const QString&)` 信号作为编辑器与上层（Ide）的解耦通道，接入工作由后续在 app/ide.cpp 完成；复用 createStandardContextMenu 保证 Undo/Redo/Cut/Copy/Paste/Select All 行为一致；不改 keyPressEvent 等已有快捷键实现；仅修改 gui/CodeEditor.h 与 gui/CodeEditor.cpp 两个文件，MSVC + Qt 6.10.3 构建通过 | [CHANGELOG](../CHANGELOG.md) |
| MarkdownRenderer 扩展 + LabManualContent 内容丰富 | (1) MarkdownRenderer 新增 3 种语法：任务列表 `- [ ]`/`- [x]`（带复选框 + ✓ 符号）/ 引用块 `>`（左侧蓝色边框 + 浅灰背景）/ 表格 `\| col \|` + 分隔行（斑马纹 + 表头背景）+ CSS `<style>` 块注入（标题蓝色 + 底边框/代码块圆角 6px + 边框/行内代码红色/表格斑马纹/引用块蓝色边框/字体 Segoe UI + 微软雅黑 + 行高 1.6）+ 代码块语言标签显示；(2) LabManualContent 8 章每章新增 4 类区块：📋 学习清单（任务列表 6 项）/ 💡 小贴士（引用块 2 条）/ ⚠️ 常见错误（表格 3 行）/ 🤔 思考题（有序列表 4 个），合计约 2665 字新增内容；(3) TestMarkdownRendererAudit 新增 7 个测试（TaskList/Blockquote/Table/Stylesheet/Fragment/LangLabel），更新 2 个旧测试适配新 HTML 结构；1693/1693 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| IDE 窗口样式美化 | 5 项 QSS 美化（4 项实施 + 1 项跳过）：(1) TeachingPanelHeader 渐变背景（#eaf3fc→#ffffff）+ 底部分隔线 + padding 8/12px + 标题加粗 + 按钮 hover 圆角淡蓝；(2) 教学面板容器卡片化（objectName=teachingPanelCard + WA_StyledBackground + 8px margins + panelQss 新增 4px 圆角/1px 边框/白底）；(3) 标题栏 qlineargradient 渐变（#fafafa→#f0f0f0）+ 移除未用 titleBg 变量；(4) 工具栏跳过（项目无 QToolBar，标题栏已融合）；(5) 状态栏字号 12→11px + QLabel padding 6→8px；仅改 QSS 与 objectName，不改 widget 结构；MSVC + Qt 6.10.3 构建通过 | [CHANGELOG](../CHANGELOG.md) |
| BugHunt + CallStack/Variable/BytecodeTrace 表情符号增强 | 4 个文件共 211 处表情：BugHuntLibrary（15 题 × 8 字段 ≈ 120 处，难度标识 🌱🔧🚀 + Bug 类型 🔍📛🔗📜🏗️🐞📦⚙️）+ BugHuntVariantLibrary（7 变体 × 5 字段 = 35 处，🔬🔗📜🔧📦🐞⚙️）+ CallStackPanel（6 场景 × 3 字段 = 18 处，📞🔄📦🎯⚠️🔁）+ VariableInspectorPanel（10 类型 × 2 字段 = 20 处，🔢📝📊📦🔗）+ BytecodeTracePanel（18 OpCodeDoc × semantics = 18 处，📜🔢📍🔄📞📊🔗📦）；严格不动 id/sourceCode/expectedFrames/expectedVariables/opCodeName/字段名；三批合计 14 文件 473+ 表情覆盖全部 15 个教学内容数据文件 | [CHANGELOG](../CHANGELOG.md) |
| 实验手册头歌风格 + 字号调节 + 启动优化 + Markdown 渲染 | 用户 8 项改进综合实施：(1) 彻底删除深色主题（TeachingTheme.h 暗色分支硬编码为亮色）+ logo SVG 修复（main.cpp QIcon 加载）；(2) LabManualPanel 头歌风格三栏布局（章节列表 | Markdown 内容 | 样例代码编辑器，QSplitter 150:400:400）；(3) 代码区/实验手册字号调节（CodeEditor::changeFontSize Ctrl+=/-/0 + LabManualPanel SpinBox 8-24pt）；(4) 所有教学内容 Markdown 渲染（新增 gui/MarkdownRenderer.h/.cpp 轻量渲染器 + 9 面板接入 markdownToHtmlFragment + 修复 3 面板 HTML 注入 + 15 单元测试）；(5) IDE 启动优化（QSS 替换不追加 lastAdsQss_ 缓存 + ScrollBar isFluentScrollBar 防重分配 + MarkdownRenderer 6 正则 static const + LabManualPanel 首次 showEvent 渲染 + 5 面板 QTimer showEvent/hideEvent）；(6) 学习路径地图跳转（onActivityRequested 21 活动 ID 路由）；(7) 19 教学面板初始尺寸调大（setMinimumSizeHintMode + 最小尺寸）；(8) 5 含 QTimer 面板卡顿修复（hideEvent 停止 QTimer 避免空转）；1686/1686 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 教学内容数据文件表情符号增强 | 7 个教学内容数据文件可读字段批量添加表情前缀：BreakpointConditionPanel（8 断点场景 title+description 🔴🎯🛑⚙️）+ SyntaxProductionLibrary（11 语法产生式 title+description+naturalLanguage 📝🔀🔄🔁⚙️🏛️🛡️📦🔤📚🔢）+ IRTransformPanel（8 lowering title+desc + 3 opt title+desc + 15 replay passName 🔢⚙️✨🧹📋🔍🔄）+ TokenPuzzleData（5 关卡 teachingPoint+hint 🔤💡）+ AstToyLevels（6 题目 teachingPoint+hint 🌳💡）+ SandboxLevels（5 关卡 goal+teachingPoint+hint 🎮💻💡）+ LearningPathData（22 活动 title+description 👋🚀🧩🌳💻📖📜🏆⚖️📊🐛🎯📍）；严格不动 id / sourceCode / initialCode / expectedOutput / expectedTokens / expectedAst / activityId / nextModuleId / 字段名 / 数字字段；现有测试（id 非空/唯一 + 字段非空 + 结构化字段）完全兼容 | [CHANGELOG](../CHANGELOG.md) |
| 教学面板表情符号增强 | 3 个教学面板静态库字段添加表情前缀：MemoryModelPanel（NaN-box 7 示例 title+description / RefCount 4 场景 title+description / GcPhase 6 阶段 title+description / GC 动画 4 阶段 description / 堆对象 6 类型 description）+ ClosureInspectorPanel（8 闭包场景 title+description+captureType+teachingNote / 6 upvalue 阶段 description）+ ExceptionFlowPanel（8 异常场景 title+description+teachingNote / 6 传播阶段 description）；严格不动 id / sourceCode / sampleCode / capturedVars / propagationPath / stackEffect / steps 等结构化字段；表情按场景分类（内存 📦🔢🎭 / GC 🧹🟢🛡️ / 闭包 🔗🔄📦 / 异常 ⚠️🚨🛡️✨🌊 / 教学 💡），现有测试（id 非空/唯一 + 字段非空）完全兼容 | [CHANGELOG](../CHANGELOG.md) |
| 教学面板 Markdown 渲染接入 | 7 个教学面板散文式字段统一通过 MarkdownRenderer::markdownToHtmlFragment 渲染：CallStackPanel（s.description/s.teachingNote）/ VariableInspectorPanel（e.teachingNote/e.heapLayout）/ BytecodeTracePanel（d.semantics）/ ClosureInspectorPanel（s.description/s.teachingNote/p.description，附带 HTML 注入修复）/ ExceptionFlowPanel（同前）/ MemoryModelPanel（b.description/phase.description）/ SyntaxExplorerPanel（p.description/p.naturalLanguage，naturalLanguage 移除 `<pre>` 改 markdown）；保留 id/title/ebnf/operandFormat/stackEffect/sampleCode 单行字面字段不变；移除手动 `<p>` 包裹避免嵌套；minilang_ide 构建成功 | [CHANGELOG](../CHANGELOG.md) |
| P2 视觉一致性收尾 | 8 项独立任务批量处理：T1 LearningPathPanel/WelcomeWizard 5 阶段色抽取到 TeachingTheme::learningStageColor()（三处面板阶段色统一）；T2 输出面板 errFilter 4 按钮改 TeachingTheme 语义色 + const char* 保留加注释；T3 applyFluentStyle 14 色变量走新增 14 个 TeachingTheme::ide*() 函数；T4 文件树 SP_DirIcon/SP_FileIcon → Fluent FOLDER/DOCUMENT/CODE 图标；T5 Welcome 标题/副标题 QLabel → TitleLabel/CaptionLabel（按钮保留 QPushButton 维持暗色主题 Claude DS 品牌色）；T6 QGroupBox 全局样式表统一 Fluent 外观；T7 CodeJourneyInfoPanel 12 处 Material 色清理（6 阶段色 + 6 浅色背景）；T8 showHelpDialog 7 色 isDark 三元改 TeachingTheme，1668/1668 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| P3 功能体验增强 | 5 项低优先级 UX 增强：M6 状态栏选中字符数显示（app/ide.cpp 第 6 个 permanent QLabel statusSelectionLabel_，复用 cursorPositionChanged→updateStatusBar 信号链，仅 hasSelection() 时显示「选中 N 行 M 字符」）；M8 CodeEditor Ctrl+D 选中相同词（无选择 WordUnderCursor / 有选择 find 下一个匹配，单光标简化版）+ Ctrl+Shift+K 删除当前行（LineUnderCursor + 吞掉行尾换行符，beginEditBlock 单次 undo）；M9 LearningPathPanel 键盘导航（面板 StrongFocus + activityRows_ 收集 unlocked QPushButton 行 + keyPressEvent Up/Down 循环高亮 + Enter 触发 click，highlightActivityRow 通过 navHighlight 属性 + 追加样式实现蓝色边框 + ensureWidgetVisible 自动滚动）；M10 PipelineViewer tokenTable_ 复制支持（CustomContextMenu 右键菜单「复制单元格/整行」+ QShortcut Ctrl+C 复制当前单元格 + SingleSelection 行为可预测）；M11 BugHuntPanel 题目搜索框（QLineEdit searchEdit_ + listContainer 包装 itemList_ 加入 splitter + textChanged setRowHidden 按文本小写包含过滤），1668/1668 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 主题系统完整性增强 | 3 项 P1 同主题任务一批次完成：M5 调用栈帧跳转源码（DebugPanel gotoLineRequested 信号 + onStackFrameSelected emit + ide.cpp 连接 codeEditor_->gotoLine，调试时点击栈帧自动跳转源码行）；DebugPanel 主题化接入（7 处硬编码颜色 #616161/#ffffff/#e5e5e5/#cfe4f5 全部替换为 TeachingTheme 主题色板 textSecondary/surface/border/primary().lighter(160)，新增 applyThemeStyles 集中管理 + onThemeModeChanged 监听主题切换刷新）；3 教学面板 HTML 重建（CodeJourneyInfoPanel buildJourneyHtml 重建 / IRTransformPanel 按当前页签分发 populate / BreakpointConditionPanel populateScenarioDetail 刷新，主题切换时跟随新主题），1668/1668 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 编辑器核心功能增强 | CodeEditor 4 项基础功能补齐 + 工作区 UX 增强：H1 Tab/Shift+Tab 多行缩进（每行行首插入/移除 4 空格，beginEditBlock 单次 undo）+ 单行 Tab 插入 4 空格 + 回车自动复制上一行缩进（{ 结尾加一级）；H2 括号匹配高亮（光标停在 ([{ 或 )]} 时半透明黄色高亮对端，bracketSelections_ 成员由 highlightCurrentLine 合并避免覆盖查找/错误/执行行高亮，cursorPositionChanged 触发）；H3 Ctrl+G 跳转行（editMenu 菜单项 + QInputDialog，1~blockCount 范围校验）；H4 Ctrl+/ 注释切换（选中范围每行行首切换 //，智能检测全注释则批量移除）；状态栏新增 statusEngineLabel_ 显示 ⚙ 引擎名（engineCombo_ 信号同步）；文件树加 fileTreeFilterEdit_ 搜索框（递归过滤，叶子按文件名匹配目录按可见子项显隐）+ 右键菜单 +3 项（复制路径/相对路径/在资源管理器中显示）；错误列表加 4 checkable 过滤按钮（错误/警告/信息/提示，复用 Qt::UserRole+3 级别 setHidden），1668/1668 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 教学面板 QFluentKit 控件统一迁移 | 19 个教学面板原生 QWidget 控件批量迁移到 QFluentKit：8 个面板主操作按钮（运行/检查/刷新/加载）→ PrimaryPushButton（BugHunt/LearningPath/IRTransform/TokenPuzzle/AstBuilderToy/VmStackSandbox/MemoryModel/BackendCompare/ProfileDashboard/LabManual/SyntaxExplorer）；19 个面板标题/状态 QLabel → TitleLabel/CaptionLabel/StrongBodyLabel（区分顶部标题/状态副标题/强调数据）；wrapTeachingPanel() 新增 applyFluentScrollBars lambda 替换所有 QAbstractScrollArea 子类的滚动条为 QFluentKit ScrollBar（className 检查防重复）；3 个硬编码颜色热点（IrViewer #f8f8f8 / CodeJourneyInfoPanel #f5f5f5 / VmStackSandboxPanel 标签背景）替换为 TeachingTheme::surface() 等主题色板；2 个 `<pre>` 代码块背景（BreakpointConditionPanel/IRTransformPanel）同样替换；保留全部信号连接（PrimaryPushButton 继承 QPushButton）、不动 CodeEditor.cpp 与主题切换逻辑、工具箱/导航按钮保留原生 QPushButton 避免视觉过载，1668/1668 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 主题切换系统真实化 | 亮/暗主题切换完整实现：标题栏 TransparentToolButton 主题切换按钮（CONSTRACT 图标，亮色月亮/暗色太阳）+ Theme::onThemeModeChanged 信号响应（重算 QSS + 更新图标 + 通知所有 CodeEditor setDarkTheme）+ QSettings 持久化（theme_mode 字段，默认 light）+ styles.qss 资源注册清理（applyFluentStyle 集中管理）+ VmStackPanel 样式迁移到 applyFluentStyle 主题自适应 + 全编辑器标签主题同步（原仅更新当前活跃编辑器）；修复 3 个预存构建阻断（ide.h 5 个缺失成员声明 + QClipboard include + tests QFluent include 路径/链接），1668/1668 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| IDE UX 增强 | 状态栏引擎显示 + 文件树搜索与右键菜单扩展 + 错误列表类型过滤：状态栏新增第 5 permanent QLabel 显示 `⚙ <引擎名>`，监听 engineCombo_ currentIndexChanged 同步（controller_ 检查前更新保证无 controller_ 也生效）；文件树上方加 QLineEdit 过滤框（placeholder「搜索文件...」+ 清除按钮），textChanged 递归 setHidden 过滤（叶子按文件名匹配、目录按可见子项决定显隐）；文件树右键菜单 +3 项（复制路径/复制相对路径/在资源管理器中显示，Windows 用 explorer /select,）；错误列表上方加 4 checkable QToolButton（错误/警告/信息/提示，checked 填充语义色），applyErrorFilter 复用 Qt::UserRole+3 级别 setHidden，appendError 末尾调用保证新增项遵循过滤；全部改动集中在 app/ide.h/.cpp，1668/1668 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 教学导航增强 | 学习中心对话框 + 教学面板统一标题栏 + TeachingTheme 主题色板 + WelcomeWizard Step 4 + 首次展开：ActivityBar「学习」入口 → LearningHubDialog 4 分组卡片导航（入门导览/编译前端/执行引擎/深入实战）；TeachingPanelHeader 19 面板「这是什么？」帮助文案 + 「学习路径」跳转；gui/TeachingTheme.h 集中色板（primary/textSecondary/surface/border/accent 等，跟随 Theme::isDark() + themeColor() 自适应）；WelcomeWizard 新增 Step 4 学习路径推荐（5 阶段彩色卡片 + 「开始学习 ✓」按钮）；首次用户向导完成后自动展开 LearningPathPanel；3 处 WelcomeWizard 创建点信号连接一致；CallStack/VariableInspector/BytecodeTrace 3 面板新增 fadeInWidget 子页动画；视图菜单 24 项平铺重构为 4 子菜单 + 学习中心入口（Ctrl+Shift+L），1668/1668 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| WelcomeWizard 主题化 | 欢迎向导 QFluentKit 主题化迁移 + IDE 构建修复：按钮迁移到 PrimaryPushButton/PushButton（Step1 开始探索/Step4 开始学习→PrimaryPushButton；跳过/prev/next→PushButton；运行按钮保留 QPushButton 用 TeachingTheme::success() 着色）；标签迁移到 TitleLabel/CaptionLabel；硬编码颜色（#0078d4/#5a5a5a/#666/#8a8a8a/#3060c0/#107d58 等）替换为 TeachingTheme 主题色板（primary/textSecondary/textHint/surface/warning/success），暗色主题自适应；修复两个预存 IDE 构建阻断（QFluentKit 组件头文件 include 路径缺失 + TeachingPanelHeader.h titleLabel_ 类型 QLabel*→StrongBodyLabel* latent bug）；无行为变更，4 步流程/信号/tokenSpans_ 保持不变 | [CHANGELOG](../CHANGELOG.md) |
| 第四档 UI 接线 | 5 个教学面板 IDE 集成（CodeJourneyInfoPanel / AstBuilderToyPanel / TokenPuzzlePanel / VmStackSandboxPanel / LearningPathPanel）：8 触点完整注册（ide.h include + 成员变量 + view QAction + dock 创建 + 信号连接 + 启动隐藏 + connectDockSave + syncViewMenuChecks）；跨面板信号路由（onJumpToPanel 6 目标 + onActivityRequested 21 活动 ID + 3 游戏 activityCompleted→markActivityCompleted 含 ID 映射）；纯静态面板模式（无 setController），1668/1668 测试通过 | [CHANGELOG](../CHANGELOG.md) |
| 功能 2 降级 | 代码生命旅程静态信息图（CodeJourneyInfoPanel）：原方案 30 秒动画与语言特性紧耦合维护成本高，降级为静态 HTML 信息图 + 6 个跳转按钮；展示 `print(1 + 2 * 3);` 完整生命旅程（源码→Token→AST→IR→字节码→输出），强调"括号改变 AST 结构"与"三后端一致性"；QTextBrowser 渲染 + jumpToPanelRequested 信号；不依赖 IdeController / 引擎层，无测试要求（纯静态信息图），minilang_ide 构建验证通过 | [CHANGELOG](../CHANGELOG.md) |
| 教学面板 E2E | 教学面板端到端交互测试（TestTeachingPanelsE2E）：8 个面板（ExceptionFlow / ClosureInspector / MemoryModel / IRTransform / CallStack / VariableInspector / BytecodeTrace / BreakpointCondition）实例化 + 子页切换 + 列表 → 详情联动 + Library 一致性 + QTimer 在 nullptr controller 下不触发；QApplication 懒初始化（Meyers 单例）+ 防御性 stopAllTimers + nullptr controller 安全路径验证；55 新增测试用例，9 套件 | [CHANGELOG](../CHANGELOG.md) |
| 功能 9 | Bug 狩猎分级题库重构（BugHuntDifficulty）：BugHuntItem 新增 difficulty 字段（BEGINNER/INTERMEDIATE/EXPERT），原 10 道重分级（4 进阶 + 6 专家）+ 新增 5 道入门级阅读理解型（BUG-READ-01~05：整数除法 / 块作用域 / Formatter 展开 / 循环 var / COW refCount）；BugHuntPanel 新增 4 互斥难度筛选按钮（默认入门级），Qt::UserRole 记录实际索引；16 新增测试用例，1 套件 | [CHANGELOG](../CHANGELOG.md) |
| 功能 1 | Welcome 向导 / 首次启动导览（WelcomeWizard）：QDialog + QStackedWidget 3 步交互式导览（角色选择 / Token 高亮 / 字节码模拟运行），首次启动 QSettings 持久化标记 + "再次显示欢迎向导"菜单项；纯前端模拟（QTimer 延迟输出 "Hello!"），不依赖 IdeController / 引擎层；无测试要求（依赖 Qt Widgets），minilang_ide 构建验证通过 | [CHANGELOG](../CHANGELOG.md) |
| 功能 11 | REPL %magic 命令系统（MagicCommands）：10 个命令（help/version/disassemble/ir/ast/tokens/memory/profile/compare/reset），ReplPanel::executeLine 开头检测 % 前缀路由到 MagicCommands::handle；MagicCommands.cpp 加入 MINILANG_CORE_SOURCES（仅依赖 IdeController 内联方法 + minilang_core 符号），13 新增测试用例，2 套件 | [CHANGELOG](../CHANGELOG.md) |
| 功能 5 | VM 栈沙盒（VmStackSandboxPanel）：5 关卡由浅入深（1+2 / 1+2*3 / (1+2)*3 / "hello" / 自由模式），内置 std::vector<std::string> 栈状态机模拟（不依赖真实 VM），核心教学时刻关卡 2 vs 关卡 3 同指令不同顺序验证"运算顺序由指令决定"；SandboxLevels.cpp 独立编译单元，18 新增测试用例，1 套件 | [CHANGELOG](../CHANGELOG.md) |
| 功能 4 | AST 节点搭建玩具（AstBuilderToyPanel）：6 道题由浅入深（42 / 1+2 / 1+2*3 / (1+2)*3 / print(x) / var x=1+2;），核心教学时刻题 3 vs 题 4 验证"括号改变 AST 结构"；QTreeWidget + 工具箱 + 严格拓扑比对；AstToyLevels.cpp 独立编译单元，17 新增测试用例，1 套件 | [CHANGELOG](../CHANGELOG.md) |
| 功能 3 | 交互式 Token 拼图游戏（TokenPuzzlePanel）：5 关卡（var x=42; / print("hello"); / x>0 and y<10; / 注释 / a[0]=b["key"]+1;），难度 1-3，星级评分（0 提示=⭐⭐⭐ / 1=⭐⭐☆ / 2+=⭐☆☆ / 跳过=☆☆☆），关卡解锁机制；TokenPuzzleData.cpp 独立编译单元（仅依赖 std::string/std::vector），QStandardItemModel 控制关卡启用态；12 新增测试用例，1 套件 | [CHANGELOG](../CHANGELOG.md) |
| 审计批次 5 | Lexer 边界（插值循环上限 + 列号一致性）+ Parser 错误恢复（类型注解预检 + classDecl try/catch + 错误数量上限 + 深度检查时机）+ Formatter 往返等价性（插值注释游标 + MemberAccess 括号 + NaN/Infinity + nullptr 防御，2 P1 + 13 P2 Bug，27 新增测试用例，14 套件） | [CHANGELOG](../CHANGELOG.md) |
| 性能基准 + 热点优化 | Release 模式基准建立（3 基准 × 3 后端）+ RegisterVM `reg()` 内联 + `callCache_` 内联缓存 | [CHANGELOG](../CHANGELOG.md) |
| 功能 6 + 功能 12 | 学习路径地图（5 阶段 21 活动 + JSON 进度持久化 + 智能推荐）+ 错误信息友好化增强（Levenshtein 拼写建议 + 7 种错误模式匹配 + ReplPanel presentation 层集成，27 新增测试用例，5 套件） | [CHANGELOG](../CHANGELOG.md) |
| 功能 13 | 代码模板 / Snippets 系统（7 模板 + Tab 展开 + 占位符导航 + Ctrl+T 列表，10 新增测试用例，2 套件） | [CHANGELOG](../CHANGELOG.md) |
| Top 3 高价值改进 | IR 优化回放 + 内存模型动画 + Bug 狩猎交互式修复（37 新增测试用例，5 套件） | [CHANGELOG](../CHANGELOG.md) |
| 审计批次 4 | 异常处理闭包 + cleanup wrap 残留 + RegisterVM 类定义检查 + VM 闭包崩溃（1 P0 + 4 P1 + 4 P2 Bug） | [CHANGELOG](../CHANGELOG.md) |
| REPL 修复 | BUG-REPL-AUDIT-9：REPL 首次 import 未设置模块加载器导致必败（P1） | [CHANGELOG](../CHANGELOG.md) |
| 调试器修复 | BUG-DBG-AUDIT-2：VM 模式断点 hitCount 永远为 0（P1） | [CHANGELOG](../CHANGELOG.md) |

### 2026-07-05 变更摘要

| 类别 | 内容 | 详情 |
|------|------|------|
| 审计批次 3 | finally 语法下游同步 + GC 漏洞 + 断点偏移 + 类型检查覆盖（1 P1 + 5 P2 Bug） | [CHANGELOG](../CHANGELOG.md) |
| 审计批次 2 | 三后端继承一致性 + REPL 防御性 + IR 优化统一 + GUI 线程安全（16 P2 Bug） | [CHANGELOG](../CHANGELOG.md) |
| 审计批次 1 | finally 语法 + 循环继承检测 + 闭包根标记 + super 运行时错误 + IR 优化防御 | [CHANGELOG](../CHANGELOG.md) |
| 教学面板（第二波） | MemoryModelPanel / IRTransformPanel / ProfileDashboardPanel | [架构文档](architecture.md#教学面板架构) |
| 教学面板（第三波） | CallStackPanel / VariableInspectorPanel / BytecodeTracePanel | [架构文档](architecture.md#教学面板架构) |
| 教学面板（第二档+第四档） | BreakpointConditionPanel / ProfileDashboard 指令计数 / 动画增强 | — |
| 教学面板（第三档） | ExceptionFlowPanel / ClosureInspectorPanel | — |
| 模块隔离 | ModuleTopLevelRenamer AST 重写，三路径统一 | [ADR-002](adr/ADR-002-triple-backend.md) |
| VM 条件断点 | slot→name 映射，局部变量注入条件求值器 | [ADR-005](adr/ADR-005-debug-consistency.md) |
| 跨模块审计修复 | 20 个 Bug（3 P1 + 17 P2） | — |
| 性能优化 | Unity Build / QtCharts 替换 / VM profiling / IR CSE+循环展开 | [ADR-003](adr/ADR-003-ir-layer.md) |
| 工程优化 | Doxygen 文档 / i18n / const 正确性审计 | [贡献指南](../CONTRIBUTING.md) |

### 测试状态

- 全量测试：**1699 个测试注册**（194 测试套件，全部通过）
- 功能 11 REPL magic 命令套件：13/13 全部通过（MagicCommandsLibraryAudit/MagicCommandsHandlerAudit）
- 功能 3 Token 拼图套件：12/12 全部通过（TokenPuzzleLibraryAudit）
- 功能 4 AST 搭建玩具套件：17/17 全部通过（AstToyLibraryAudit）
- 功能 5 VM 栈沙盒套件：18/18 全部通过（SandboxLibraryAudit）
- 审计批次 5 套件：27/27 全部通过（AuditBatch5LexerInterpColumn/LexerCommentColumn/LexerInterpLoop/ParserTypeAnn/ParserClassRecovery/ParserErrorLimit/ParserDepthCheck/FmtInterpComment/FmtMemberCall/FmtClosingBraceComment/FmtUnaryUnknown/FmtNaN/FmtInterpNullptr/Consistency）
- 功能 6 学习路径套件：15/15 全部通过（LearningPathDataAudit/LearningPathProgressAudit/LearningPathPrereqAudit）
- 功能 12 错误提示引擎套件：12/12 全部通过（ErrorHintEngineSpelling/ErrorHintEnginePatterns）
- 功能 13 代码模板套件：10/10 全部通过（CodeSnippetLibraryAudit/CodeSnippetEngineAudit）
- 教学面板套件：92/92 全部通过
- 审计批次 1 套件：27/27 全部通过（AuditBatch1Finally/Inherit/CloseUpvalue/GcRoot/Super/OptIR/Print）
- 审计批次 2 套件：16/16 全部通过（AuditBatch2Inherit/AuditBatch2IRopt）
- 审计批次 3 套件：16/16 全部通过（AuditBatch3FormatterFinally/ModuleIsolationFinally/TypeCheckerCoverage/GcLeak）
- 审计批次 4 套件：21/21 全部通过（AuditBatch4CatchUpvalue/CleanupTryDepth/RegVMClassCheck/FormatterAudit/VmClosureCrash/Consistency）
- Top 3 改进方向套件：37/37 全部通过（IROptReplayAudit/MemoryAnimLibraryPhases/MemoryAnimLibraryTypes/MemoryAnimLibraryConsistency/BugHuntVariantAudit）
- 构建：MSVC + Qt 6.10.3 msvc2022_64 + Ninja

## 项目结构

```
ide/
├── app/              # 应用层（IdeController, DebugCoordinator, main.cpp）
├── ast/              # AST 节点定义 + ModuleIsolation
├── compiler/         # 编译器（Compiler, Bytecode, IR, VM, RegisterVM）
├── gui/              # GUI 面板与组件（教学面板、编辑器、调试器）
├── interpreter/      # 树遍历解释器 + NaN-boxing Value
├── lexer/            # 词法分析器
├── parser/           # 语法分析器
├── samples/mini/     # MiniLang 示例程序（15 个 .mini 文件）
├── tests/            # 单元测试与审计测试（1699 用例）
├── cmake/            # CMake 模块
├── scripts/          # 构建脚本（build_ide.bat, run_tests.bat）
└── CMakeLists.txt    # 顶层构建配置
```
