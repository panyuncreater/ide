#include "gui/LabManualPanel.h"

// ============================================================
// LabManualContent — 实验手册内容数据
// ------------------------------------------------------------
// 8 个实验章节，每章对应一个核心教学主题，从项目特性出发设计。
// Markdown 内容通过 QTextBrowser 渲染（Qt 内置子集）。
// ============================================================

const std::vector<LabChapter>& LabManualContent::chapters() {
    static const std::vector<LabChapter> kChapters = {
        LabChapter{
            "lab-01", "实验 1：词法分析 — 从字符到 Token",
            "# 实验 1：词法分析 — 从字符到 Token\n\n"
            "## 🏃 迷你实验（3 分钟）\n"
            "**目标**：感受\"源码被切成了有类型的片段\"\n\n"
            "**步骤**：\n"
            "1. 在编辑器中输入：`var x = 1;`\n"
            "2. 打开\"编译管线可视化面板\"，切到 Token 步骤\n"
            "3. 数一数 Token 表中有几个 token\n"
            "4. 改成 `var x = 1; // 注释` 再看 token 数变化\n\n"
            "💡 源码被切成了有类型的片段——关键字 var / 标识符 x / 等号 = / 数字 1 / 分号 ;\n\n"
            "✅ **迷你实验验证**：你看到 5 个主流 token 了吗？（注释不计入主流）\n\n"
            "---\n\n"
            "## 📋 学习清单\n"
            "- [ ] 理解词法分析器的职责：把字符流切成 Token 流\n"
            "- [ ] 完成迷你实验，亲眼看到 5 个主流 token\n"
            "- [ ] 掌握 Token 的 5 大类型（关键字/标识符/字面量/运算符/分隔符）\n"
            "- [ ] 理解注释为什么单独存储在 lexer.comments()\n"
            "- [ ] 通过验证断言：注释不计入主流\n"
            "- [ ] 尝试进阶挑战：UTF-8 多字节字符的列号计数\n\n"
            "## 目标\n"
            "理解词法分析器的职责：将源代码字符串切分为有意义的 Token 序列，并分离注释。\n\n"
            "## 关键概念\n"
            "- **Token 类型**：关键字 / 标识符 / 字面量 / 运算符 / 分隔符\n"
            "- **注释分离**：MiniLang 将 TK_LINE_COMMENT/TK_BLOCK_COMMENT 从主流分离到 lexer.comments()，供 Formatter 保留注释\n"
            "- **列号计算**：按 UTF-8 码位计数（columnAt）\n"
            "- **DoS 防护**：MAX_SOURCE_SIZE / MAX_TOKEN_COUNT / MAX_INTERP_DEPTH\n\n"
            "> 💡 **小贴士**：注释 token 不计入主流，是为了让 Formatter 能正确还原注释位置——直接丢弃会让格式化后注释全部消失。\n\n"
            "> 💡 **小贴士**：列号按 UTF-8 码位计数而非字节数，所以一个中文字算 1 列，而不是 3 列。\n\n"
            "## 实验步骤\n"
            "1. 在编辑器中输入以下代码：\n"
            "```\nvar x = 42; // 行内注释\nprint(x);\n```\n"
            "2. 打开\"编译管线可视化面板\"，切到 Token 步骤\n"
            "3. 观察 Token 表：每行包含 type/lexeme/line/column 字段\n"
            "4. 验证：注释 token 不在主流中（在 lexer.comments() 中）\n\n"
            "## 验证断言\n"
            "- Token 表中应有 5 个主流 token：var / x / = / 42 / ;\n"
            "- print(x); 对应 4 个 token：print / ( / x / ) / ;\n"
            "- 注释 token 不计入主流\n\n"
            "## ⚠️ 常见错误\n"
            "| 错误现象 | 原因 | 解决方案 |\n"
            "| --- | --- | --- |\n"
            "| Token 数比预期多 1 | 把注释算进了主流 | 检查 lexer.comments()，注释单独存储 |\n"
            "| 列号偏移 | 多字节字符按字节计数 | 使用 columnAt 按 UTF-8 码位计数 |\n"
            "| 输入 `$` 后报错但程序继续 | 词法错误标记 ERROR token 后续扫描 | 这是错误恢复，不是崩溃 |\n\n"
            "## 进阶\n"
            "- 尝试输入非法字符（如 $）观察错误 token\n"
            "- 尝试输入多字节 UTF-8 字符（如 \"é\"）观察列号计数\n\n"
            "## 🤔 思考题\n"
            "1. 为什么注释要单独存储到 `lexer.comments()` 而不是直接丢弃？想想 Formatter 的需求。\n"
            "2. 如果把 `var` 改成 `Var`（大写），词法分析会把它识别成什么 token？为什么？\n"
            "3. `1.5e10` 这个字面量会被切成几个 token？试着预测并验证。\n"
            "4. 输入 `\"未闭合字符串` 会发生什么？错误如何传播给 Parser？",
            "var x = 42; // 行内注释\nprint(x);"
        },
        LabChapter{
            "lab-02", "实验 2：递归下降解析 — 从 Token 到 AST",
            "# 实验 2：递归下降解析 — 从 Token 到 AST\n\n"
            "## 🏃 迷你实验（5 分钟）\n"
            "**目标**：感受\"括号改变树的结构\"\n\n"
            "**步骤**：\n"
            "1. 在编辑器中输入：`print(1 + 2 * 3);`\n"
            "2. 打开\"编译管线可视化面板\"，切到 AST 步骤\n"
            "3. 观察 AST：* 节点在 + 节点的右子树（因为乘法优先）\n"
            "4. 改成 `print((1 + 2) * 3);` 再看 AST 结构\n\n"
            "💡 同样的数字和运算符，仅括号不同就导致完全不同的树结构！\n"
            "   - 第一种：+ 在根，* 在右子树 → 1 + (2*3) = 7\n"
            "   - 第二种：* 在根，+ 在左子树 → (1+2) * 3 = 9\n\n"
            "✅ **迷你实验验证**：两次运行结果分别是 7 和 9 吗？\n\n"
            "---\n\n"
            "## 📋 学习清单\n"
            "- [ ] 理解递归下降解析器的工作方式\n"
            "- [ ] 完成迷你实验，观察括号如何改变 AST\n"
            "- [ ] 掌握优先级链（assignment → or_ → ... → primary）\n"
            "- [ ] 理解 synchronize() 错误恢复机制\n"
            "- [ ] 通过验证断言：AST 结构与优先级一致\n"
            "- [ ] 尝试进阶：观察语法错误的恢复行为\n\n"
            "## 目标\n"
            "理解 Parser 的工作方式：手写递归下降，按优先级链解析表达式。\n\n"
            "## 关键概念\n"
            "- **优先级链**：assignment → or_ → and_ → equality → comparison → term → factor → unary → call → primary\n"
            "- **错误恢复**：synchronize() 在出错后跳到下一个同步点（语句边界）继续解析\n"
            "- **深度防护**：MAX_PARSE_DEPTH / MAX_BLOCK_DEPTH 通过 DepthGuard RAII 守卫防止 C++ 栈溢出\n"
            "- **AST 节点**：33 个 NodeType，ASTNode 基类含 line/column/nodeType + accept(Visitor&)\n\n"
            "> 💡 **小贴士**：优先级链的顺序决定运算符结合性——链越靠后优先级越高。factor 比 term 靠后，所以乘法比加法先结合。\n\n"
            "> 💡 **小贴士**：synchronize() 不是回滚，而是\"跳过坏 token 继续找下一个语句\"——这样能一次性报告多个错误。\n\n"
            "## 实验步骤\n"
            "1. 在编辑器中输入：\n"
            "```\nvar x = 1 + 2 * 3;\n```\n"
            "2. 打开\"编译管线可视化面板\"，切到 AST 步骤\n"
            "3. 观察 AST 树形结构：BinaryOp(+) 左 VarDecl(=x), 右 BinaryOp(*) 左 2 右 3\n"
            "4. 验证：乘法比加法优先级高，* 节点在 + 节点的右侧子树\n\n"
            "## 验证断言\n"
            "- AST 根节点是 Block\n"
            "- 第一个子节点是 VarDecl\n"
            "- VarDecl 的 initializer 是 BinaryOp(+)\n"
            "- + 的右子节点是 BinaryOp(*)\n\n"
            "## ⚠️ 常见错误\n"
            "| 错误现象 | 原因 | 解决方案 |\n"
            "| --- | --- | --- |\n"
            "| AST 根不是预期的 BinaryOp | 优先级链顺序理解错 | 检查 expression() 调用链 |\n"
            "| 错误后只报 1 个错 | synchronize 跳得不够远 | 检查同步点是否包含 `;` `}` |\n"
            "| 深嵌套表达式栈溢出 | 缺少 DepthGuard 守卫 | 每层递归用 RAII 守卫 |\n\n"
            "## 进阶\n"
            "- 修改为 `var x = (1 + 2) * 3;` 观察括号如何改变 AST 结构\n"
            "- 输入语法错误（如 `var = ;`）观察错误恢复\n\n"
            "## 🤔 思考题\n"
            "1. 为什么 `1 - 2 - 3` 的结果是 -4 而不是 2？想想左结合和右结合的区别。\n"
            "2. `var x = 1, 2, 3;` 会触发什么错误？synchronize 会跳到哪里继续解析？\n"
            "3. 如果把优先级链里 equality 和 comparison 互换位置，`1 < 2 == true` 的 AST 会变成什么样？\n"
            "4. `MAX_PARSE_DEPTH` 设为多少合适？太小会怎样，太大会怎样？",
            "var x = 1 + 2 * 3;\nprint(x);"
        },
        LabChapter{
            "lab-03", "实验 3：树遍历解释器 — Visitor 模式执行 AST",
            "# 实验 3：树遍历解释器 — Visitor 模式执行 AST\n\n"
            "## 🏃 迷你实验（5 分钟）\n"
            "**目标**：感受\"解释器 = 递归遍历 + 执行\"\n\n"
            "**步骤**：\n"
            "1. 在编辑器中输入：`print(42);`\n"
            "2. 点击运行 → 看到 `42`\n"
            "3. 打开 AST 面板 → 看到 `Print → NumberLiteral(42)`\n"
            "4. 改成 `print(3 + 4);` → 看 AST 变成 `Print → BinaryOp(+, 3, 4)`\n"
            "5. 运行 → 看到 `7`\n\n"
            "💡 这就是解释器的全部工作：遍历 AST，遇到 Print 就打印，遇到 BinaryOp 就计算！\n\n"
            "✅ **迷你实验验证**：你看到了 7 吗？\n\n"
            "---\n\n"
            "## 📋 学习清单\n"
            "- [ ] 理解解释器 = 递归遍历 + 执行\n"
            "- [ ] 完成迷你实验，看到 print(3+4) 输出 7\n"
            "- [ ] 掌握 Visitor 模式与 visit 方法\n"
            "- [ ] 理解 Environment 链式作用域\n"
            "- [ ] 通过验证断言：fib 序列正确\n"
            "- [ ] 尝试进阶：3 层嵌套闭包与 REPL 续行\n\n"
            "## 目标\n"
            "理解解释器的执行模型：通过 Visitor 模式递归遍历 AST 节点求值。\n\n"
            "## 关键概念\n"
            "- **Visitor 模式**：每个 AST 节点类型对应一个 visit 方法\n"
            "- **Environment**：链式作用域，boundInstance_ 缓存优化\n"
            "- **DebugController**：断点 / 单步 / 条件求值 / 沙箱隔离\n"
            "- **REPL**：续行判断（未闭合 {/(/[/字符串/块注释/try 缺 catch）\n\n"
            "> 💡 **小贴士**：Visitor 模式把\"对节点的操作\"从\"节点本身\"分离——新增节点类型不用改旧代码，新增操作也不用动节点。\n\n"
            "> 💡 **小贴士**：Environment 用链表实现作用域，boundInstance_ 缓存避免每次方法调用都遍历整个链查找 this。\n\n"
            "## 实验步骤\n"
            "1. 在编辑器中输入：\n"
            "```\nfun fib(n) {\n    if (n < 2) return n;\n    return fib(n - 1) + fib(n - 2);\n}\nfor (var i = 0; i < 10; i = i + 1) {\n    print(fib(i));\n}\n```\n"
            "2. 点击\"调试\"按钮（F6），设置断点在 fib 函数体首行\n"
            "3. 单步进入 / 跳过 / 跳出，观察调用栈\n"
            "4. 设置条件断点 `n == 5`，验证只在 n=5 时暂停\n\n"
            "## 验证断言\n"
            "- fib(0)=0, fib(1)=1, fib(2)=1, ..., fib(10)=55\n"
            "- 调用栈顶帧行号显示当前执行行（BUG-DBG-1 修复后）\n"
            "- 条件断点求值不污染程序状态（沙箱保存/恢复所有可变状态）\n\n"
            "## ⚠️ 常见错误\n"
            "| 错误现象 | 原因 | 解决方案 |\n"
            "| --- | --- | --- |\n"
            "| fib 输出全是 0 | 漏了 return 关键字 | 检查 if 分支是否 `return n` |\n"
            "| 条件断点污染状态 | 沙箱未恢复变量 | 验证 DebugController 保存/恢复所有可变状态 |\n"
            "| 闭包捕获值错误 | 误以为捕获的是值 | upvalue 是引用而非快照，注意循环捕获 |\n\n"
            "## 进阶\n"
            "- 尝试 3 层嵌套闭包（参见 BUG-UV-1）\n"
            "- 在 REPL 中输入多行代码观察续行判断\n\n"
            "## 🤔 思考题\n"
            "1. 解释器执行 `fib(10)` 时调用栈最深处有多少层？为什么会触发 MAX_INTERP_DEPTH 防护？\n"
            "2. Environment 的 boundInstance_ 缓存为什么能加速方法调用？不缓存会怎样？\n"
            "3. REPL 是怎么判断\"用户还没输入完，需要继续读下一行\"的？列举至少 3 种续行场景。\n"
            "4. 条件断点 `n == 5` 求值时，如果改了 n 的值会发生什么？沙箱如何防止？",
            "fun fib(n) {\n    if (n < 2) return n;\n    return fib(n - 1) + fib(n - 2);\n}\nfor (var i = 0; i < 10; i = i + 1) {\n    print(fib(i));\n}"
        },
        LabChapter{
            "lab-04", "实验 4：栈式字节码 VM — 编译与执行",
            "# 实验 4：栈式字节码 VM — 编译与执行\n\n"
            "## 🏃 迷你实验（5 分钟）\n"
            "**目标**：感受\"每条指令操作栈顶\"\n\n"
            "**步骤**：\n"
            "1. 在编辑器中输入：`print(1 + 2);`\n"
            "2. 打开\"字节码轨迹面板\"，看到 3 条核心指令：\n"
            "   - `OP_INT 1`（压入 1）\n"
            "   - `OP_INT 2`（压入 2）\n"
            "   - `OP_ADD`（弹出两个，相加，压回 3）\n"
            "   - `OP_PRINT`（弹出并打印）\n"
            "3. 点击\"单步\"按钮，观察栈如何变化\n\n"
            "💡 栈式 VM 就是 push 和 pop 的游戏——每条指令操作栈顶！\n\n"
            "✅ **迷你实验验证**：单步到 OP_ADD 后，栈顶是否变成 3？\n\n"
            "---\n\n"
            "## 📋 学习清单\n"
            "- [ ] 理解字节码编译与栈式 VM 执行\n"
            "- [ ] 完成迷你实验，看到栈顶在 OP_ADD 后变成 3\n"
            "- [ ] 掌握核心 OpCode（OP_INT/OP_ADD/OP_PRINT）\n"
            "- [ ] 理解操作数栈 push/pop 模型\n"
            "- [ ] 通过验证断言：x == 3\n"
            "- [ ] 尝试进阶：嵌套左值的栈平衡\n\n"
            "## 目标\n"
            "理解字节码编译与栈式 VM 执行：AST → BytecodeChunk → VM。\n\n"
            "## 关键概念\n"
            "- **OpCode**：~60 个栈式操作码（OP_CONSTANT/OP_ADD/OP_CALL 等）\n"
            "- **操作数栈**：定长 Value[1024]，push/pop 模型\n"
            "- **BytecodeChunk**：code（字节流）+ constants（常量池）+ lines（行号映射）\n"
            "- **常量池去重**：类型严格匹配 + 数值相等（BUG-CP-1 修复后）\n"
            "- **全局槽位**：编译期分配，运行时通过 slot 索引访问\n\n"
            "> 💡 **小贴士**：栈式 VM 的核心就是 push 和 pop——每条指令要么压栈，要么弹栈，要么既弹又压。栈深度永远 >= 0。\n\n"
            "> 💡 **小贴士**：常量池去重用类型严格匹配 + 数值相等，所以 int(0) 和 float(0.0) 不会共享索引（BUG-CP-1 修复）。\n\n"
            "## 实验步骤\n"
            "1. 在编辑器中输入：\n"
            "```\nvar x = 1 + 2;\nprint(x);\n```\n"
            "2. 打开\"编译管线可视化面板\"，切到字节码步骤\n"
            "3. 观察反汇编：\n"
            "   - OP_INT 1\n"
            "   - OP_INT 2\n"
            "   - OP_ADD\n"
            "   - OP_DEFINE_VAR 0\n"
            "   - OP_GET_VAR 0\n"
            "   - OP_PRINT\n"
            "4. 切换到 VM 模式，单步执行观察栈变化\n\n"
            "## 验证断言\n"
            "- 编译后字节码长度合理（约 6-8 字节）\n"
            "- 常量池去重：相同字面量共享索引\n"
            "- 栈式 VM 执行后 x == 3\n\n"
            "## ⚠️ 常见错误\n"
            "| 错误现象 | 原因 | 解决方案 |\n"
            "| --- | --- | --- |\n"
            "| 栈泄漏（栈深度持续增长） | 表达式语句未 POP 返回值 | 检查每个 emit 路径的 push/pop 配对 |\n"
            "| 嵌套左值 `a[0][1]=x` 错位 | 栈平衡计算错 | 参见 BUG-CP-2 的修复 |\n"
            "| 常量池索引错乱 | int(0) 与 float(0.0) 共享 | 类型严格匹配去重 |\n\n"
            "## 进阶\n"
            "- 尝试嵌套左值 `a[0][1] = x;` 观察栈平衡（参见 BUG-CP-2）\n"
            "- 启用 IR 路径观察字节码差异\n\n"
            "## 🤔 思考题\n"
            "1. 为什么操作数栈固定为 1024 而不是动态扩容？想想 DoS 防护。\n"
            "2. `OP_ADD` 既要处理 int 又要处理 float/string，编译期怎么知道生成哪种？\n"
            "3. 表达式语句 `1 + 2;` 的返回值如果不 POP 会怎样？栈会泄漏多少？\n"
            "4. 字节码的 lines 数组有什么用？为什么调试器需要它？",
            "var x = 1 + 2;\nprint(x);"
        },
        LabChapter{
            "lab-05", "实验 5：寄存器式 VM — 三地址码与 IR",
            "# 实验 5：寄存器式 VM — 三地址码与 IR\n\n"
            "## 🏃 迷你实验（5 分钟）\n"
            "**目标**：感受\"不同后端的指令集不同\"\n\n"
            "**步骤**：\n"
            "1. 在编辑器中输入：`print(1 + 2);`（与实验 4 相同的代码）\n"
            "2. 打开\"三后端对比面板\"，点击运行\n"
            "3. 对比 StackVM 与 RegisterVM 的字节码：\n"
            "   - StackVM：`OP_INT 1 / OP_INT 2 / OP_ADD / OP_PRINT`（栈式）\n"
            "   - RegisterVM：`LOAD_CONST r1, 1 / LOAD_CONST r2, 2 / ADD r1, r1, r2 / PRINT r1`（三地址码）\n\n"
            "💡 同样的代码，不同后端用完全不同的指令集表达！\n\n"
            "✅ **迷你实验验证**：两个后端的输出都是 3 吗？\n\n"
            "---\n\n"
            "## 📋 学习清单\n"
            "- [ ] 理解 IR 中间表示与寄存器式 VM\n"
            "- [ ] 完成迷你实验，对比 StackVM 与 RegisterVM\n"
            "- [ ] 掌握 52 个 IROp 与 ~50 个 RegOp\n"
            "- [ ] 理解虚拟寄存器（SSA-like）分配\n"
            "- [ ] 通过验证断言：y == 9\n"
            "- [ ] 尝试进阶：观察 DCE 与复制传播效果\n\n"
            "## 目标\n"
            "理解 IR 中间表示与寄存器式 VM：AST → IR → RegBytecode → RegisterVM。\n\n"
            "## 关键概念\n"
            "- **IROp**：52 个 IR 指令（LOAD_CONST/ADD/CALL 等）\n"
            "- **虚拟寄存器**：SSA-like，nextVReg 分配\n"
            "- **RegOp**：~50 个三地址码操作码\n"
            "- **32 虚拟寄存器**：R0-R31，零堆分配（std::array<Value, 32>）\n"
            "- **优化 pass**：常量折叠 / 死代码消除 / 复制传播\n\n"
            "> 💡 **小贴士**：寄存器 VM 用三地址码 `ADD r1, r2, r3`，相比栈式 `OP_ADD` 减少了 push/pop 开销，但指令更长。\n\n"
            "> 💡 **小贴士**：虚拟寄存器是 SSA-like 的——每个值有唯一 vreg，写一次读多次，这让优化 pass 更容易分析。\n\n"
            "## 实验步骤\n"
            "1. 在编辑器中输入：\n"
            "```\nvar x = 1 + 2;\nvar y = x * 3;\nprint(y);\n```\n"
            "2. 切换到 RegisterVM 模式（视图菜单）\n"
            "3. 打开\"编译管线可视化面板\"，切到 IR 步骤\n"
            "4. 观察 IR 指令：\n"
            "   - v0 = LOAD_CONST 1\n"
            "   - v1 = LOAD_CONST 2\n"
            "   - v2 = ADD v0, v1\n"
            "   - v3 = LOAD_CONST 3\n"
            "   - v4 = MUL v2, v3\n"
            "5. 启用优化观察 v2 是否被常量折叠\n\n"
            "## 验证断言\n"
            "- IR 中应看到 vreg 分配与三地址码结构\n"
            "- 寄存器 VM 执行后 y == 9\n"
            "- 优化启用后 IR 指令数减少\n\n"
            "## ⚠️ 常见错误\n"
            "| 错误现象 | 原因 | 解决方案 |\n"
            "| --- | --- | --- |\n"
            "| 寄存器溢出（>32） | 表达式太复杂 | 检查 nextVReg 分配或拆分表达式 |\n"
            "| 优化后行为变化 | 常量折叠误删副作用 | DCE 必须保守，只删纯表达式 |\n"
            "| IR 与 StackVM 结果不一致 | 三后端语义差异 | 跑 ConsistencyDiff 测试 |\n\n"
            "## 进阶\n"
            "- 启用复制传播观察 v0/v1 是否被消除\n"
            "- 尝试死代码（如 `var unused = 5;`）观察 DCE 效果\n\n"
            "## 🤔 思考题\n"
            "1. 寄存器 VM 用 32 个虚拟寄存器，超了怎么办？真实 CPU 也只有 16-32 个寄存器。\n"
            "2. SSA 形式为什么让常量折叠和 DCE 更容易实现？换成名-值形式会怎样？\n"
            "3. `var x = 1 + 2; var y = x * 3;` 启用常量折叠后 IR 还剩几条指令？\n"
            "4. 复制传播如何消除 `v0 = LOAD_CONST 1; v1 = v0` 中的 v1？",
            "var x = 1 + 2;\nvar y = x * 3;\nprint(y);"
        },
        LabChapter{
            "lab-06", "实验 6：三后端一致性 — 语义等价验证",
            "# 实验 6：三后端一致性 — 语义等价验证\n\n"
            "## 🏃 迷你实验（3 分钟）\n"
            "**目标**：感受\"三条路径结果一致\"\n\n"
            "**步骤**：\n"
            "1. 在编辑器中输入：`print(7 / 2);`\n"
            "2. 打开\"三后端对比面板\"，点击运行\n"
            "3. 查看 Interpreter / StackVM / RegisterVM 三个输出\n\n"
            "💡 三条路径都输出 `3`（整数除法截断向零，三后端统一）\n\n"
            "✅ **迷你实验验证**：三个后端的输出都是 3 吗？\n\n"
            "---\n\n"
            "## 📋 学习清单\n"
            "- [ ] 理解三后端一致性约束\n"
            "- [ ] 完成迷你实验，看到三后端都输出 3\n"
            "- [ ] 掌握整数除法截断向零、and/or 短路语义\n"
            "- [ ] 理解 ConsistencyDiff 差分测试\n"
            "- [ ] 通过验证断言：三后端输出一致\n"
            "- [ ] 尝试进阶：触发已知差异观察错误消息\n\n"
            "## 目标\n"
            "理解三后端一致性约束：同一 MiniLang 源码在 Interpreter / StackVM / RegisterVM 三条路径上必须产生相同语义结果。\n\n"
            "## 关键概念\n"
            "- **三后端一致性**：整数除法截断向零 / and-or 短路返回原值 / 类型注解强制 / super 调用语义\n"
            "- **ConsistencyDiff 测试**：190 个差分测试用例验证三后端等价\n"
            "- **已知差异**：非方法上下文 super 错误消息文本不同（错误类型相同，行为一致）\n"
            "- **错误消息归一化**：差分时归一化错误类型而非文本\n\n"
            "> 💡 **小贴士**：整数除法截断向零——`7 / 2 = 3` 而 `-7 / 2 = -3`（不是 -4）。三后端必须一致。\n\n"
            "> 💡 **小贴士**：and/or 短路返回操作数原值而非布尔——`0 or \"default\"` 返回 \"default\"，`5 and 0` 返回 0。\n\n"
            "## 实验步骤\n"
            "1. 在编辑器中输入：\n"
            "```\nprint(7 / 2);\nprint(7.0 / 2);\nprint(0 or \"default\");\nprint(5 and 0);\n```\n"
            "2. 打开\"三后端并行对比面板\"\n"
            "3. 点击\"运行对比\"按钮\n"
            "4. 观察三列输出是否一致\n\n"
            "## 验证断言\n"
            "- 三后端都输出：3 / 3.5 / 0 / 0\n"
            "- 整数除法 7/2 三后端一致为 3\n"
            "- 浮点除法 7.0/2 三后端一致为 3.5\n"
            "- and/or 短路返回操作数原值（非布尔）\n\n"
            "## ⚠️ 常见错误\n"
            "| 错误现象 | 原因 | 解决方案 |\n"
            "| --- | --- | --- |\n"
            "| 三后端结果不一致 | 某后端漏改语义 | 跑 ConsistencyDiff 190 用例 |\n"
            "| 错误消息文本不同 | 各后端独立实现 | 差分时归一化错误类型而非文本 |\n"
            "| super 在非方法上下文行为不同 | 已知差异 | 文档化或统一错误消息 |\n\n"
            "## 进阶\n"
            "- 尝试触发已知差异（非方法上下文 super）观察错误消息差异\n"
            "- 修改源码观察哪些情况下三后端可能不一致（如默认参数表达式）\n\n"
            "## 🤔 思考题\n"
            "1. 为什么 `7 / 2` 是 3 而不是 3.5？MiniLang 怎么区分整数除法和浮点除法？\n"
            "2. `0 or \"default\"` 为什么返回 \"default\" 而不是 true？这种语义有什么用？\n"
            "3. 修改 StackVM 后如何确保 RegisterVM 也一致？手动检查还是跑差分测试？\n"
            "4. ConsistencyDiff 有 190 个用例，设计差分测试时应该覆盖哪些边界？",
            "print(7 / 2);\nprint(7.0 / 2);\nprint(0 or \"default\");\nprint(5 and 0);"
        },
        LabChapter{
            "lab-07", "实验 7：内存模型 — NaN-boxing 与 COW",
            "# 实验 7：内存模型 — NaN-boxing 与 COW\n\n"
            "## 🏃 迷你实验（5 分钟）\n"
            "**目标**：感受\"写时复制真正复制了\"\n\n"
            "**步骤**：\n"
            "1. 在编辑器中输入：\n"
            "```\nvar a = [1, 2, 3];\nvar b = a;\nb[0] = 99;\nprint(a[0]);\nprint(b[0]);\n```\n"
            "2. 运行 → 看到 `1` 和 `99`\n"
            "3. 打开\"内存模型面板\"，观察 ArrayData 的引用计数和 COW detach 时机\n\n"
            "💡 a 和 b 最初共享同一个 ArrayData（refCount=2）；b[0]=99 触发 COW detach，b 拷贝出新的 ArrayData，从此 a 和 b 独立\n\n"
            "✅ **迷你实验验证**：a[0] 是 1，b[0] 是 99 吗？（写时复制真正复制了）\n\n"
            "---\n\n"
            "## 📋 学习清单\n"
            "- [ ] 理解 NaN-boxing 8 字节 Value 编码\n"
            "- [ ] 完成迷你实验，看到 COW 写时复制\n"
            "- [ ] 掌握 RefCounted 侵入式引用计数\n"
            "- [ ] 理解 COW detach 时机（refCount > 1 时复制）\n"
            "- [ ] 通过验证断言：a 与 b 独立\n"
            "- [ ] 尝试进阶：嵌套数组的深浅拷贝\n\n"
            "## 目标\n"
            "理解 MiniLang 的内存模型：Value 8 字节 NaN-boxing，堆类型侵入式 RefCounted，数组/字典 Copy-On-Write。\n\n"
            "## 关键概念\n"
            "- **NaN-boxing**：8 字节 Value，tag bits / payload 分段编码\n"
            "- **int48 内联存储**：int 在 int48 范围内联存储，超范围自动装箱\n"
            "- **RefCounted**：侵入式引用计数基类，数组/字典/字符串/InstanceData 都继承\n"
            "- **COW**：写前检查独占所有权，refCount > 1 时复制\n"
            "- **GcManager sweep**：周期性清理 refCount==0 的对象防 UAF\n\n"
            "> 💡 **小贴士**：NaN-boxing 用 8 字节装下所有类型——指针、int48、bool、null 都编码在一个 double 大小的空间里，省内存又省分配。\n\n"
            "> 💡 **小贴士**：COW 不是每次都深拷贝——只有 refCount > 1 时才复制，独占所有权时直接改，零开销。\n\n"
            "## 实验步骤\n"
            "1. 在编辑器中输入：\n"
            "```\nvar a = [1, 2, 3];\nvar b = a;          // 共享所有权\nb.push(4);          // COW 触发，b 拥有新副本\nprint(a.len());     // 3\nprint(b.len());     // 4\n```\n"
            "2. 运行观察输出\n"
            "3. 修改为 `var b = a; b[0] = 99;` 验证 COW 写时复制\n"
            "4. 测试大数组传递观察引用计数（暂无可视化，需通过性能感知）\n\n"
            "## 验证断言\n"
            "- a 与 b 共享同一 ArrayData 直到第一次写\n"
            "- 任何修改 b 的操作触发 COW，a 不受影响\n"
            "- 字符串拼接（+）任一操作数为字符串即触发\n\n"
            "## ⚠️ 常见错误\n"
            "| 错误现象 | 原因 | 解决方案 |\n"
            "| --- | --- | --- |\n"
            "| a 被 b 的修改污染 | COW detach 未触发 | 检查 refCount > 1 判断 |\n"
            "| UAF（访问已释放对象） | GcManager sweep 过早 | 验证 refCount 真为 0 才清理 |\n"
            "| int48 溢出 | 大整数超范围 | 自动装箱为堆对象 |\n\n"
            "## 进阶\n"
            "- 尝试嵌套数组 `[[1,2],[3,4]]` 观察深拷贝 vs 浅拷贝\n"
            "- 测试字符串索引 UTF-8 多字节字符（参见 BUG-INTP-3）\n\n"
            "## 🤔 思考题\n"
            "1. NaN-boxing 为什么用 NaN 的 payload 而不是普通 union？想想 double 与指针的区分。\n"
            "2. COW 在 `var b = a; b[0] = 99;` 时复制了整个数组，如果数组很大性能如何？\n"
            "3. 嵌套数组 `[[1,2],[3,4]]` 的 COW 是深拷贝还是浅拷贝？内层数组会共享吗？\n"
            "4. GcManager sweep 周期性清理 refCount==0 的对象，为什么不直接在 refCount 归零时释放？",
            "var a = [1, 2, 3];\nvar b = a;\nb.push(4);\nprint(a.len());\nprint(b.len());"
        },
        LabChapter{
            "lab-08", "实验 8：Bug 狩猎 — 从历史 Bug 学习",
            "# 实验 8：Bug 狩猎 — 从历史 Bug 学习\n\n"
            "## 🏃 迷你实验（5 分钟）\n"
            "**目标**：感受\"Bug 隐藏在细节中\"\n\n"
            "**步骤**：\n"
            "1. 打开\"Bug 狩猎面板\"\n"
            "2. 选择难度筛选中的\"入门级\"\n"
            "3. 选一道入门级题目（如 BUG-READ-03 格式化预测）\n"
            "4. 不看提示，先自己预测代码运行结果\n"
            "5. 点击\"运行\"对比期望行为\n\n"
            "💡 入门级题目不需要看代码，重点是培养 Debug 直觉——预测→对比→理解差异\n\n"
            "✅ **迷你实验验证**：你的预测与实际行为一致吗？\n\n"
            "---\n\n"
            "## 📋 学习清单\n"
            "- [ ] 理解高频 Bug 模式（栈不平衡/UAF/竞态等）\n"
            "- [ ] 完成迷你实验，预测入门级行为\n"
            "- [ ] 掌握三后端对称性审计\n"
            "- [ ] 理解 RAII guard 构造顺序\n"
            "- [ ] 通过验证断言：识别反模式\n"
            "- [ ] 尝试进阶：在主编辑器重现 Bug\n\n"
            "## 目标\n"
            "通过项目自身沉淀的真实 Bug 训练调试能力：阅读代码、预测行为、定位根因。\n\n"
            "## 关键概念\n"
            "- **高频 Bug 模式**：栈不平衡 / UAF / 泄漏 / 语义不一致 / 竞态 / 错误传播 / 边界条件\n"
            "- **三后端对称性审计**：修改某后端时同步检查其他两个后端\n"
            "- **错误路径栈平衡**：每个 error return 点必须 popN 清理栈\n"
            "- **RAII guard 构造顺序**：必须在状态修改前构造以捕获正确的 saved 状态\n\n"
            "> 💡 **小贴士**：\"push 在检查之前\"是经典反模式——先 push 再检查条件，失败时忘记 pop，栈就泄漏了。\n\n"
            "> 💡 **小贴士**：RAII guard 必须在状态修改前构造——如果先改状态再构造 guard，guard 捕获的就是已污染的状态，恢复时恢复错了。\n\n"
            "## 实验步骤\n"
            "1. 打开\"Bug 狩猎\"面板\n"
            "2. 选择 BUG-CP-1（常量池去重陷阱）\n"
            "3. 阅读背景：int(0) 与 float(0.0) 共享常量索引的后果\n"
            "4. 在内嵌编辑器中预测输出\n"
            "5. 点击\"运行验证\"查看实际输出\n"
            "6. 逐步查看提示，最后查看答案\n"
            "7. 重复 5-10 道题，覆盖不同 Bug 模式\n\n"
            "## 验证断言\n"
            "- 能区分 Value::equals()（数值相等）与类型严格相等\n"
            "- 理解三后端对称性约束：修改指令布局时必须同步更新 instructionSizeAt\n"
            "- 能识别\"push 在检查之前\"反模式\n"
            "- 理解条件断点沙箱必须保存/恢复所有可变状态\n\n"
            "## ⚠️ 常见错误\n"
            "| 错误现象 | 原因 | 解决方案 |\n"
            "| --- | --- | --- |\n"
            "| 栈泄漏 | push 后忘记 pop | 每个 error return 点必须 popN |\n"
            "| UAF | GcManager sweep 过早 | 验证 refCount 真为 0 才清理 |\n"
            "| 沙箱恢复不全 | guard 构造太晚 | 在状态修改前构造 RAII guard |\n\n"
            "## 进阶\n"
            "- 完成 10 道题后尝试在主编辑器中重现某个 Bug\n"
            "- 阅读 project_memory.md 中的\"Lessons Learned\"加深理解\n\n"
            "## 🤔 思考题\n"
            "1. 为什么\"push 在检查之前\"是反模式？画出一个会泄漏的具体场景。\n"
            "2. RAII guard 的构造顺序为什么必须在状态修改前？如果后构造会怎样？\n"
            "3. BUG-CP-1 中 int(0) 与 float(0.0) 共享常量索引会导致什么后果？\n"
            "4. 修改某后端时，如何用三后端对称性审计发现遗漏？列举检查清单。",
            "var a = 7 / 2;\nvar b = 7.0 / 2;\nprint(a);\nprint(b);"
        }
    };
    return kChapters;
}
