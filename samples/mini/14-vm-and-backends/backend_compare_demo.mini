// 三后端一致性演示：同一段代码在三个后端输出完全相同
// 在「三后端对比面板」中运行对比

// 整数除法：截断向零
print(7 / 2);          // 3（三后端一致）
print(-7 / 2);         // -3（三后端一致）

// and/or 短路：返回操作数原值
print(0 or "default"); // "default"
print(5 and 0);        // 0

// 混合运算
print(10 / 3);         // 3
print(10.0 / 3);       // 3.333...
