// main.mini - 主程序，演示跨文件import
import "math_utils.mini";
import "string_utils.mini";
import "shapes.mini";

// ---- 使用 math_utils ----
print("=== Math Utils ===");
print("add(3, 4) = " + add(3, 4));
print("multiply(5, 6) = " + multiply(5, 6));
print("factorial(5) = " + factorial(5));
print("fibonacci(10) = " + fibonacci(10));
print("PI = " + PI);

// ---- 使用 string_utils ----
print("");
print("=== String Utils ===");
print(greet("MiniLang"));
print(repeat("*", 20));
print("isEmpty('') = " + isEmpty(""));
print("isEmpty('hi') = " + isEmpty("hi"));
print(padLeft("42", 5, "0"));

// ---- 使用 shapes ----
print("");
print("=== Shapes ===");
var c = Circle(5);
print(c.describe());

var r = Rectangle(4, 6);
print(r.describe());

// ---- 数组/字典 ----
print("");
print("=== Data Structures ===");
var shapes = [Circle(3), Rectangle(2, 5), Circle(7)];
for (var i = 0; i < shapes.len(); i = i + 1) {
    print(shapes[i].describe());
}

var config = {"version": "1.0", "author": "MiniLang"};
print("config version: " + config["version"]);

print("");
print("All tests passed!");
