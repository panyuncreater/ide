# 2026-06-07 工作日志

## 上下文压缩
- 将三轮修复历史、项目架构、关键文件、设计决策、OpCode一致性检查法全部写入 MEMORY.md
- 项目状态：2.2基础30/30 + 2.3扩展15/15 全部实现
- 三轮"检查→修复"完成，所有17个源文件通过MinGW语法检查

## 项目优化审查
- 调度 gstack-investigator 执行健康检查
- 发现4高+7中+5低共16个优化项
- 最紧急：VM::peek()无边界检查、VM::pop()错误被吞、VMCallFrame裸指针悬空风险
- 最大收益：Value结构体重构为std::variant（内存/拷贝↓80%+）
- 中期优化：string_view引用、numericOp枚举分发、OpCode名称映射统一、ip→row映射表
- 详细报告已输出在对话中

## 优化修复完成（7项）
1. **hasError_标志**：runtimeError设置hasError_=true，execute/stepOnce检测后终止 → 修复pop()错误被吞
2. **peek()边界检查**：越界返回static nullSentinel引用+报运行时错误
3. **VM自持mainChunk_副本**：initExecution拷贝mainChunk到VM成员，帧指针不再悬空
4. **热路径const引用**：12处`std::string name =`改为`const std::string& name =`
5. **numericOp枚举分发**：字符串比较→int枚举switch，消除5次strcmp
6. **opCodeName()统一映射**：Bytecode.h新增，VmStackPanel 50行map→1行调用
7. **ipToInstrIndex预计算**：BytecodeChunk::buildIpMap() + instructionSize()，highlightBytecodeLine从O(n)→O(1)
- 全部修改文件通过MinGW g++语法检查
